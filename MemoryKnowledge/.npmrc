# This directory is nested in OpenClaw workspace middle.pnpm The parent will be identified before reading this file. workspace,
# Therefore, when installing or adding dependencies, you must explicitly pass --ignore-workspace(See root for details README).
# codegraph 1.2.0 Published under minimumReleaseAge;Skip age verification when installing independently.
minimum-release-age=0
