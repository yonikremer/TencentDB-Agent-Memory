{
  "MemoryProxy/src/knowledge/core-client.ts": {
    "20": " * TTL cache（2026-08-20）：kernel 偶发 timeout（现网 33% 命中率），会阻塞 prewarm。\n",
    "21": " * 加 30s in-memory cache + stale-if-error 兜底：cache miss 后 fetch 失败时返回\n",
    "22": " * 上次成功结果，避免 injection 空。env TDAI_KNOWLEDGE_CACHE_TTL_MS 可覆盖；0 关闭。\n",
    "78": "  // TTL cache + stale-if-error 兜底表\n",
    "96": "   * TTL cache + stale-if-error 包装。\n",
    "97": "   *  - hit（未过期）→ 直接返回 cached\n",
    "98": "   *  - miss/expired → 调 doFetch，成功写入 cache + lastGood\n",
    "99": "   *  - doFetch 抛异常或返回 null/undefined → 若 lastGood 存在则返回它并 warn\n",
    "100": "   *  - cacheTtlMs === 0 → 直通\n",
    "101": "   * doFetch 语义：成功返回值（可以是空数组），失败返回 null 或抛异常。\n",
    "205": "   * 按 knowledge_id 批量联查明细（Proxy per-agent 路径）。\n",
    "206": "   * 服务鉴权（bearer + service-id），无需 user-key。空 ids → []。\n",
    "208": "   * @param opts.serviceId Per-call override；带 spaceId 的调用方必须传。\n",
    "228": "      // env=null 表示 HTTP/decode 失败 → 传播 null 让 _cachedFetch 走 stale 逻辑；\n",
    "229": "      // env.data.items 为 [] 是\"合法空结果\"（正常缓存）。\n",
    "237": "   * 取某 agent 被绑定的 knowledge asset_id（= knowledge_id）集合。\n",
    "238": "   * 走内核 meta `/v3/meta/agent-fixed-asset/list-with-detail`（ForCaller，需 user-key）。\n",
    "239": "   * 过滤 asset_type ∈ {llm_wiki, code_graph}。失败/无 user-key → []。\n",
    "241": "   * @param opts.serviceId Per-call override；带 spaceId 的调用方必须传。\n",
    "251": "    // userKey 不入 key —— 同 agent 的 fixed-asset 绑定是 agent 级视图。\n",
    "257": "        // asset_types 服务端过滤 —— 无关类型（skill / chat_memory）不再占分页额度，\n",
    "258": "        // 避免 wiki/code_graph 被 skill 挤出默认 limit=20 后拿不到。\n",
    "259": "        // 依赖 core commit da04d194 (feat/meta asset_types filter)。\n"
  },
  "MemoryProxy/src/langfuse.ts": {
    "1": " * Langfuse LLM trace 上报模块（官方 SDK 方式）。\n",
    "3": " * 使用 Langfuse 官方 SDK（@langfuse/tracing + @langfuse/otel）上报 LLM 调用。\n",
    "5": " * 核心语义：**一个 trace = 一个会话里的一次用户输入（一个 turn）**。\n",
    "6": " *   - 同一 turn 内的工具循环（model → tool → model → …）会产生多次 upstream 请求，\n",
    "7": " *     它们共享同一个确定性 traceId，因此在 Langfuse 里归并到同一个 trace 下，\n",
    "8": " *     每次 LLM 调用是该 trace 下的一个 generation observation。\n",
    "9": " *   - traceId 由 `sessionKey + turnSeq` 经 SHA-256 派生（确定性），与官方\n",
    "10": " *     `createTraceId(seed)` 的算法逐字节一致（取 SHA-256 hex 前 32 位）。\n",
    "12": " * 跨请求归并的机制：\n",
    "13": " *   一个 turn 的多次请求是彼此独立的 HTTP handler 调用，没有共享的 async context。\n",
    "14": " *   因此通过 `startObservation(..., { parentSpanContext: { traceId, ... } })` 显式把\n",
    "15": " *   每个 generation 挂到已知的确定性 traceId 下（SDK 内部走\n",
    "18": " * 设计原则：\n",
    "19": " *   - Fire-and-forget：span 由 LangfuseSpanProcessor 异步批量导出\n",
    "20": " *   - 配置缺失 / SDK 初始化失败时 graceful degradation（全部 no-op）\n",
    "21": " *   - 与 Opik 上报完全独立（各用各的 traceId）\n",
    "31": "// 生命周期\n",
    "36": "// OpenTelemetry NodeSDK 实例（用于优雅关闭时 flush）。\n",
    "41": " * 初始化 Langfuse 上报。在 server 启动时调用一次，后续调用为 no-op。\n",
    "42": " * 返回是否成功启用。\n",
    "55": "    // BatchSpanProcessor 的 maxQueueSize 只能通过 OTel env var 注入（Langfuse 构造器不透传）。\n",
    "56": "    // 在 SDK 初始化前设好，确保 BatchSpanProcessor 读到我们的值。\n",
    "71": "      flushAt: lf.flushAt || undefined,           // 每批最大 span 数\n",
    "72": "      flushInterval: lf.flushInterval || undefined, // 定时 flush 间隔（秒）\n",
    "95": " * 优雅关闭 Langfuse 上报，确保所有待发送 span 已 flush。\n",
    "111": "// 确定性 turn traceId\n",
    "115": " * 由 `sessionKey + turnSeq` 派生确定性 traceId（32 位小写 hex）。\n",
    "117": " * 与官方 `createTraceId(seed)` 算法一致：SHA-256(seed) 的 hex 取前 32 位。\n",
    "118": " * 同一 turn 内每次请求都用相同 (sessionKey, turnSeq) → 得到相同 traceId →\n",
    "119": " * 在 Langfuse 中归并到同一个 trace。\n",
    "127": "// 上报：generation observation\n",
    "131": " * Langfuse turn-trace 上下文 —— 同属一个 turn（一个 Langfuse trace）的所有\n",
    "132": " * generation 共享的 trace 级属性。由 handler 构造后传给各上报函数。\n",
    "135": "  /** 该 turn 的确定性 traceId（langfuseTurnTraceId 生成）。 */\n",
    "137": "  /** turn 序号（countHumanTurns）—— 也用于 ClickHouse 按 turn 聚合。 */\n",
    "139": "  /** trace 名。 */\n",
    "141": "  /** trace userId（一般为 keyId）。 */\n",
    "143": "  /** trace sessionId（会话隔离键；可据此聚合多个 turn）。 */\n",
    "146": "   * trace 级标签 —— 只放该 turn 内稳定的维度（protocol / stream / session），\n",
    "147": "   * 不含随请求变化的路由标签，避免同一 turn 的工具循环请求互相覆盖（last-write-wins）。\n",
    "151": "   * 本次请求的 observation 级附加标签 —— 随请求变化，写入 generation 的\n",
    "152": "   * observation metadata，而非 trace 级 tags。宿主默认不填充。\n",
    "156": "   * 去噪后的最新用户问题 —— 仅在该 turn 首次人类输入请求非空，工具循环延续时为 \"\"。\n",
    "157": "   * 用作 trace 级 input。\n",
    "162": "/** 一次 LLM 调用的上报参数（挂到指定 turn trace 下的 generation）。 */\n",
    "164": "  /** 所属 turn 的确定性 traceId（langfuseTurnTraceId 生成）。 */\n",
    "166": "  /** observation 名称（一般为模型名，或 `[internal] <model>`）。 */\n",
    "168": "  /** 模型名。 */\n",
    "170": "  /** ISO 8601 开始时间。 */\n",
    "172": "  /** ISO 8601 结束时间。 */\n",
    "174": "  /** generation 输入（messages 数组或字符串）。 */\n",
    "176": "  /** generation 输出（assistant message 或字符串）。 */\n",
    "178": "  /** 原始 usage 对象（会被归一化为 Langfuse usageDetails）。 */\n",
    "180": "  /** observation 级别（默认 DEFAULT；失败时传 ERROR）。 */\n",
    "182": "  /** 状态信息（一般用于 ERROR，描述失败原因）。 */\n",
    "184": "  // ── trace 级属性（同一 turn 的多次调用应传相同值；last-write-wins）──\n",
    "185": "  /** trace 名。 */\n",
    "187": "  /** trace userId（一般为 keyId）。 */\n",
    "189": "  /** trace sessionId（会话隔离键；Langfuse 上可据此聚合多个 turn）。 */\n",
    "191": "  /** trace 标签。 */\n",
    "194": "   * trace 级 input —— 仅在该 turn 的\"首次人类输入\"请求传入（传 turn 最初的用户问题）。\n",
    "195": "   * 工具循环延续请求应留空，避免把带 tool_result 的请求体覆盖成 trace input。\n",
    "196": "   * 内部路由等子步骤也应留空，避免污染 trace 级输入。\n",
    "200": "   * trace 级 output —— 传该 turn 的最终回答。同一 turn 多次调用 last-write-wins，\n",
    "201": "   * 因此最后一次（turn 收尾）的输出会成为 trace output。\n",
    "204": "  /** trace 级 metadata。 */\n",
    "206": "  /** observation 级 metadata。 */\n",
    "211": " * 派生一个合法的 phantom parent spanId（16 位 hex，非零）。\n",
    "212": " * 用于把 generation 挂到确定性 traceId 下。同一 traceId 始终得到同一 spanId，\n",
    "213": " * 因此一个 turn 内所有 generation 的 parent 一致（指向同一个不存在的 root span，\n",
    "214": " * Langfuse 据此把它们都视为该 trace 下的顶层 observation）。\n",
    "221": " * 归一化原始 LLM usage → Langfuse usageDetails（Record<string, number>）。\n",
    "223": " * Token 口径与 ClickHouse 的 `buildClickHouseRow` 保持一致（此处独立复刻，不跨模块依赖），\n",
    "224": " * 覆盖 Anthropic / OpenAI / DeepSeek 三种 usage 格式：\n",
    "225": " *   - Anthropic(TokenHub)：`input_tokens` 已排除 cache，总输入 = input + cache_read + cache_write，\n",
    "226": " *     且响应无 `total_tokens`，需回退为 prompt + completion。\n",
    "227": " *   - OpenAI / DeepSeek：`prompt_tokens` 即含 cache 的总输入，通常也带 `total_tokens`。\n",
    "229": " * 输出遵循 Langfuse 惯例（各分项之和 = total，避免与内置 cost 计算重复计数）：\n",
    "230": " *   - `input`：未命中缓存的输入（= 总输入 − cache_read − cache_write），按 input 单价计费\n",
    "231": " *   - `cache_read_input_tokens` / `cache_creation_input_tokens`：缓存读 / 写\n",
    "232": " *   - `output`：输出\n",
    "233": " *   - `total`：总 token（= 总输入 + 输出）\n",
    "235": " * 修复要点：此前 `total = input_tokens + output_tokens` 对 Anthropic 会漏掉 cache token\n",
    "236": " * （cache 常占绝大多数），导致 Langfuse 的 total 少一到两个数量级。\n",
    "251": "  // 总输入（含缓存）：OpenAI/DeepSeek 取 prompt_tokens；\n",
    "252": "  // Anthropic 无 prompt_tokens，用 input_tokens(已排除 cache) 加回 cache_read + cache_write。\n",
    "256": "  // 总 token：优先上游给的 total_tokens，否则 prompt + completion（prompt 已含缓存）。\n",
    "259": "  // 未命中缓存的输入（分项之和 = total，不与 cache_* 重复计数）。\n",
    "272": "/** 取数值字段（非数值按 0），与 ClickHouse 的 `num()` 口径一致。 */\n",
    "277": "/** 把任意值转为可写入 OTel 属性的字符串。 */\n",
    "283": " * 上报一次 LLM 调用：在指定 turn trace 下创建一个 generation observation，\n",
    "284": " * 并把 trace 级属性写到该 span 上（SDK 会据此设置所属 trace 的字段）。\n",
    "286": " * 失败静默（仅 debug 日志），绝不影响业务请求。\n",
    "315": "    // trace 级属性：直接写 OTel 属性，SDK 会传播到所属 trace。\n",
    "323": "    // trace 级 input/output 与 observation 级解耦：仅在显式传入时写。\n",
    "324": "    // 首次人类输入请求传 traceInput（turn 最初的问题）；收尾请求传 traceOutput。\n",
    "344": "/** 一次失败请求的上报参数（上游错误 / 转发失败）。 */\n",
    "346": "  /** turn 上下文。 */\n",
    "348": "  /** observation 名称（一般为模型名）。 */\n",
    "350": "  /** ISO 8601 开始时间。 */\n",
    "352": "  /** ISO 8601 结束时间。 */\n",
    "354": "  /** 该请求的输入 messages（用于排查）。 */\n",
    "356": "  /** HTTP 状态码（转发异常时可缺省）。 */\n",
    "358": "  /** 失败描述（如错误体片段或 \"timeout/error\"）。 */\n",
    "360": "  /** 额外标签（如 [\"error\"]）。 */\n",
    "362": "  /** observation 级 metadata。 */\n",
    "367": " * 上报一次失败请求：在所属 turn trace 下创建一个 ERROR generation。\n",
    "368": " * 不设 trace 级 input/output（失败不代表 turn 的最终结果），仅记录该次失败本身。\n",
    "390": "    // trace 级 input 仍记录用户问题（便于在失败 trace 上看到原始诉求）；不写 output。\n"
  },
  "MemoryProxy/src/mem-command/commands/create-skill.ts": {
    "1": " * mem:create-skill — 手动强制归档当前 session buffer\n",
    "3": " * 文案约定：只告知用户\"归档已触发 / Skill 提取中\"这类概念，不暴露内部\n",
    "4": " * task_id、archive_key、文件路径等。想排障时看 `data` 字段或后端日志。\n",
    "25": "    messageText = `❌ 本次对话归档失败：${result.error ?? \"未知错误\"}`;\n",
    "27": "    messageText = `⚠️ 本次对话暂无可归档内容，请继续对话后再试`;\n",
    "29": "    messageText = `✅ 本次对话已归档成功，Skill 提取中`;\n",
    "42": "    // task_id / archive_key 仍保留在结构化数据里 —— 面板 / 日志 / e2e 可读，\n",
    "43": "    // 只是不再拼进用户可见文案。\n"
  },
  "MemoryProxy/src/mem-command/commands/create-task.ts": {
    "1": " * mem:create-task — 从当前会话上下文生成 Task 并绑定到本 session。\n",
    "3": " * 交互（对齐 TAPD 需求\"用户确认\"闸门）：\n",
    "5": " *   ┌ session 未绑真实 task：\n",
    "6": " *   │    无参数 → LLM 生成 title+desc → 直接落库+绑定（无需 confirm）\n",
    "7": " *   │    有参数 → title = 用户参数(≤40 字)，LLM 只生成 desc → 直接落库+绑定\n",
    "8": " *   │            LLM 失败 → 降级：desc 留空，task 照样落库\n",
    "10": " *   ├ session 已绑真实 task：\n",
    "11": " *   │    生成新 draft → 写入 pending-store（TTL 5 分钟）→ 返\"预览 + 三选一引导\"：\n",
    "12": " *   │      1) mem:create-task confirm — 覆盖绑定，新建\n",
    "13": " *   │      2) mem:update-task [新描述] — 继续复用当前 Task 只更新描述\n",
    "14": " *   │      3) mem:create-task cancel — 撤销\n",
    "17": " *   │    从 pending 取出 draft → 创建新 task → 覆盖 session 绑定 → 清 pending\n",
    "20": " *        清 pending，不落库\n",
    "22": " * 参数解析：\n",
    "23": " *   - `confirm` / `cancel`（大小写不敏感）单独出现视为子命令\n",
    "24": " *   - 其它任意 args 一律作为 lockedTitle（首次调用的 title 提示）\n",
    "40": "  if (text.length === 0) return \"(空)\";\n",
    "49": "/** 判定 args 是否是 confirm / cancel 子命令。仅当整体 trim 后严格等于时命中。 */\n",
    "78": "  // ── confirm 分支 ────────────────────────────────────────────────────────\n",
    "89": "        `⚠️ 没有待确认的 Task 操作（可能已超时或被取消）。请重新执行 \\`mem:create-task [标题]\\`。`,\n",
    "96": "        `❌ Task 创建失败：${result.error ?? \"unknown error\"}`,\n",
    "103": "      ? `\\n\\n（已解除原绑定 Task \\`${result.previousTaskId}\\`）`\n",
    "106": "      `✅ 已创建并绑定新 Task。\\n\\n` +\n",
    "107": "        `- **标题**：${result.title}\\n` +\n",
    "108": "        `- **状态**：${result.status ?? \"running\"}\\n` +\n",
    "109": "        `- **描述**：${trimDesc(result.description)}\\n` +\n",
    "123": "  // ── cancel 分支 ─────────────────────────────────────────────────────────\n",
    "133": "        `⚠️ 取消失败：${result.error ?? \"unknown\"}`,\n",
    "139": "      ? `✅ 已取消待确认的 Task 操作。`\n",
    "140": "      : `ℹ️ 当前没有待确认的 Task 操作。`;\n",
    "144": "  // ── 首次调用分支 ────────────────────────────────────────────────────────\n",
    "150": "      `⚠️ 当前请求未携带对话消息，\\`mem:create-task\\` 需要最近对话作为上下文才能生成 Task。` +\n",
    "151": "        `\\n\\n提示：请先与 AI 进行几轮对话后再执行此命令。`,\n",
    "166": "  // 落库失败\n",
    "171": "      : \"\\n\\n你可以：\\n1. 稍后重试\\n2. `mem:create-task <你的标题>` 手动指定标题（LLM 只生成描述，即便失败也会用空描述兜底）\";\n",
    "173": "      `❌ Task 创建失败：${detail}${hintLine}`,\n",
    "179": "  // 已绑分支：pending 预览\n",
    "186": "      `⚠️ 当前会话已绑定 Task ${currentLine}。\\n\\n` +\n",
    "187": "        `**新 Task 预览**\\n` +\n",
    "188": "        `- 标题：${p.draftTitle}\\n` +\n",
    "189": "        `- 描述：${trimDesc(p.draftDescription)}\\n\\n` +\n",
    "190": "        `请选择下一步：\\n` +\n",
    "191": "        `1. \\`mem:create-task confirm\\` — 覆盖当前绑定，创建新 Task\\n` +\n",
    "192": "        `2. \\`mem:update-task\\` 或 \\`mem:update-task <新描述>\\` — 继续复用当前 Task，只更新描述（不新建）\\n` +\n",
    "193": "        `3. \\`mem:create-task cancel\\` — 取消，不做任何改动`,\n",
    "208": "  // 未绑分支：直接落库成功\n",
    "210": "    `✅ Task 已创建并绑定到本会话。\\n\\n` +\n",
    "211": "      `- **标题**：${result.title}\\n` +\n",
    "212": "      `- **状态**：${result.status ?? \"running\"}\\n` +\n",
    "213": "      `- **描述**：${trimDesc(result.description)}\\n` +\n",
    "215": "      `后续可用 \\`mem:update-task\\` 追加进度。`,\n"
  },
  "MemoryProxy/src/mem-command/commands/help.ts": {
    "1": " * mem:help — 返回支持的命令列表及示例\n",
    "7": "const HELP_TEXT = `## 支持的 mem: 命令\n",
    "9": "| 命令 | 说明 |\n",
    "11": "| \\`mem:session-reset\\` | 重置本次会话的团队/Agent/任务绑定，立即弹出重新选择 |\n",
    "12": "| \\`mem:sync\\` | 刷新本次会话的全部资产注入（Skill / 记忆 / Knowledge / Task & Agent 描述） |\n",
    "13": "| \\`mem:create-skill [提示词]\\` | 把本次对话归档为 Skill，后台异步提取 |\n",
    "14": "| \\`mem:create-task [标题]\\` | 从当前会话上下文创建 Task 并绑定到本 session |\n",
    "15": "| \\`mem:update-task [新描述]\\` | 更新已绑定 Task 的描述 |\n",
    "16": "| \\`mem:help\\` | 显示本帮助 |\n",
    "20": "### 🆕 \\`mem:create-task\\` — 创建并绑定 Task\n",
    "22": "**用法**\n",
    "23": "- **无参数**：LLM 从最近对话中推断 title + description\n",
    "24": "- **有参数**：参数作为 title（40 字截断），LLM 只生成 description\n",
    "26": "**若本 session 已绑真实 Task**，返回新 Task 预览，回复以下之一：\n",
    "28": "| 回复 | 效果 |\n",
    "30": "| \\`mem:create-task confirm\\` | ✅ 覆盖绑定，创建新 Task |\n",
    "31": "| \\`mem:update-task\\` 或 \\`mem:update-task <新描述>\\` | ⭐ **推荐** — 继续复用当前 Task，只更新描述 |\n",
    "32": "| \\`mem:create-task cancel\\` | 🚫 取消，不做任何改动 |\n",
    "36": "### ✏️ \\`mem:update-task\\` — 更新当前 Task 描述\n",
    "38": "**用法**\n",
    "39": "- **无参数**：LLM 对比 \"当前 description + 最近对话\" 生成新 description\n",
    "40": "  - 判无实质改动 → 返回 ℹ️ 无需更新（幂等，可安全重试）\n",
    "41": "  - 判有改动 → 返回预览\n",
    "42": "- **有参数**：参数直接作为新 description（不调 LLM），返回预览\n",
    "44": "**确认预览**：\n",
    "45": "- ✅ \\`mem:update-task confirm\\` — 确认\n",
    "46": "- 🚫 \\`mem:update-task cancel\\` — 取消\n",
    "48": "**保护规则**：\n",
    "49": "- 若本 session 未绑 Task → 拦截并提示先执行 \\`mem:create-task\\`\n",
    "50": "- 若绑定的 Task 不是你创建的 → 拒绝更新（不支持跨用户改），建议 \\`mem:create-task\\` 新建一个属于你的\n",
    "54": "### 示例\n",
    "58": "mem:create-skill 重点总结数据库迁移步骤和踩坑\n",
    "59": "mem:create-task 重构 SessionRegistrar\n",
    "62": "mem:update-task 补充今天完成的进度与遗留风险\n",
    "69": "> 标准格式为 \\`mem:<command>\\`，冒号后不加空格。命令名大小写不敏感。`;\n"
  },
  "MemoryProxy/src/mem-command/commands/session-reset.ts": {
    "1": " * mem:session-reset — 重置当前 session 的绑定, 在会话中间重新走 session-init 表单.\n",
    "3": " * 语义:不管当前 state 是 uninitialized / pending_* / initialized / bypassed,\n",
    "4": " * 命令执行后一律进入 `pending_asset_confirm`,并携带 `resetEpoch = Date.now()` +\n",
    "5": " * `resetFlow = true`。下一次请求进来时,`handleSessionInit` 会因 state !==\n",
    "6": " * initialized 而弹 asset_confirm 表单,用户重新选择 Team/Agent/Task。\n",
    "8": " * 跨节点一致性:\n",
    "9": " *   - 写入 store 走 `store.set` write-through,L1 + L2a(SessionRepo) 同步落盘\n",
    "10": " *   - 别的 pod 的 L1 里可能还有旧 initialized —— `store.getOrRecover` step 1\n",
    "11": " *     用 resetEpoch 对齐 L2a,发现 L2a.resetEpoch 更大就打破 L1 短路,拿新\n",
    "12": " *     pending 状态弹表单\n",
    "13": " *   - L2b binding (initialized 时的\"小纸条\") 显式删除,避免下一轮 `rebuildFromBinding`\n",
    "14": " *     把旧 initialized 直接灌回来\n",
    "16": " * 文案:参照 create-skill 的口味,只暴露\"重置/关联/团队资产\"等用户能理解的\n",
    "17": " * 词,不出现 status / resetEpoch / binding / pending_asset_confirm 等内部术语。\n",
    "30": "  // 记录 old 状态用于观测(埋点在 Commit 4 追加,现在只在返回值 data 里带上)\n",
    "36": "  // 写 uninitialized 而非 pending_asset_confirm:\n",
    "38": "  // pending_asset_confirm 意味着\"proxy 已经弹了 form,等用户答复\"—— 但 reset\n",
    "39": "  // 后的下一轮 body 里不会有 form tool_use/tool_result(因为 form 还没弹过)。\n",
    "40": "  // 如果写 pending_asset_confirm,下一轮 handleSessionInit 进入 \"pending_asset_confirm\n",
    "41": "  // 分支\" 试图从 user 消息解析 form 答案 → 解不出 → unrecognized → bypass →\n",
    "42": "  // 命令白跑了。\n",
    "44": "  // 写 uninitialized 让下一轮走 Case 1 \"第一次进来\" 分支:重拉 teams → 弹\n",
    "45": "  // asset_confirm form。因为 Commit 2 已经删掉 isFreshCCConversation gate,\n",
    "46": "  // 即使 messages 很多也不会被 safety-net 拦截。\n",
    "57": "  // 兜底 bind identity:handler 路径正常会先调 store.getOrRecover(compositeKey, identity, ...)\n",
    "58": "  // 完成 bind,但 pre-hook 前置拦截时 store 里未必已经 bind 过 —— 显式补一次\n",
    "59": "  // 保证 store.set 的 L2a write-through 能命中正确 namespace。\n",
    "69": "  // 显式删除 L2b binding —— 否则下一次请求走 getOrRecover 时 step 3 rebuildFromBinding\n",
    "70": "  // 会用旧 initialized binding 直接灌回来,resetFlow 空跑。\n",
    "71": "  // store.set 内部已经 `deleteBinding` 了 pending 状态(只在 initialized 才 put),\n",
    "72": "  // 但为了防御性:这里显式 delete,不依赖 store.set 内部行为的实现细节。\n",
    "86": "  // 结构化 audit log — 让运维在 dashboard / grep 时能精确抓到 session-reset\n",
    "87": "  // 事件。字段与后续 session_init_logs 的 completion 埋点对齐(same session_key /\n",
    "88": "  // agent_source),PM/运维可以 join 看\"reset 触发 → 完成 init 的转化率\"。\n",
    "89": "  // reset_epoch 用于跨节点一致性调试 —— pod A 写、pod B 读,若两侧看到不同的\n",
    "90": "  // reset_epoch 就能立刻定位 L2a probeL2a stale 的问题。\n",
    "109": "    // 结构化数据只在日志/面板可见,不进用户可读文案\n",
    "121": " * 根据老状态构造用户可读文案。\n",
    "123": " * 老状态       文案强调\n",
    "124": " * uninitialized 「继续对话时会弹出选择」\n",
    "125": " * initialized   「已解除绑定,请重新选择」\n",
    "126": " * bypassed      「已恢复选择入口」\n",
    "127": " * pending_*     「已重新开始选择」\n",
    "131": "    return \"✅ 已重置,继续对话时会弹出团队资产选择\";\n",
    "134": "    return \"✅ 已恢复团队资产选择入口,继续对话时会弹出重新选择\";\n",
    "137": "    return \"✅ 已解除本次会话的团队资产绑定,继续对话时会弹出重新选择\";\n",
    "140": "  return \"✅ 已重新开始团队资产选择,继续对话时会弹出选择\";\n"
  },
  "MemoryProxy/src/mem-command/commands/sync.ts": {
    "1": " * mem:sync — 刷新当前 session 的全部注入缓存\n",
    "3": " * 底层动作覆盖：\n",
    "4": " *   - 重新拉 Agent / Task detail 覆写到 SessionStore（描述、prompt、goal 都跟着更新）\n",
    "5": " *   - 重跑所有声明了 session_init / hybrid 缓存策略的 hook（Skill / 记忆 /\n",
    "6": " *     Knowledge / 固定资产 等），把新块 putMany 到 HookCacheRepo (COS)\n",
    "8": " * 文案侧只讲\"资产 / 描述\"这些用户能理解的概念，不暴露 injector id、hook 名字、\n",
    "9": " * archive_key 等内部术语。\n",
    "18": "  parts.push(\"Skill / 记忆 / Knowledge 资产\");\n",
    "20": "    parts.push(\"Task & Agent 描述\");\n",
    "23": "  return `✅ 所有资产注入已刷新（${scope}），耗时 ${result.tookMs}ms`;\n",
    "29": "  // 未绑定时不走 refreshSessionCache(它会暴露 session key 给用户)\n",
    "31": "    const messageText = \"⚠️ 当前会话未绑定团队资产，无法同步。请先用 `mem:session-reset` 选择 Team/Agent 后再试。\";\n",
    "49": "    : `❌ 资产刷新失败：${result.error ?? \"未知错误\"}`;\n",
    "61": "    // 详细的 hookId 列表 / 时长 保留在结构化数据里给面板 / 日志用。\n"
  },
  "MemoryProxy/src/mem-command/commands/update-task.ts": {
    "1": " * mem:update-task — 更新已绑定 Task。\n",
    "3": " * 交互（每次都需要用户确认）：\n",
    "5": " *   ┌ session 未绑 → 拦截，引导 mem:create-task\n",
    "6": " *   ├ 首次调用（无 confirm/cancel 子命令）：\n",
    "7": " *   │    有参数 → description 直接替换（跳 LLM）→ 写 pending → 预览\n",
    "8": " *   │    无参数 → LLM diff 生成新 desc + status 建议 → 写 pending → 预览\n",
    "9": " *   │            LLM 判 changed=false → 返\"无需更新\"，不写 pending\n",
    "10": " *   ├ `mem:update-task confirm` → 从 pending 取 → updateTask（含 status 透传） → 清 pending\n",
    "11": " *   └ `mem:update-task cancel`  → 清 pending\n",
    "26": "  if (text.length === 0) return \"(空)\";\n",
    "69": "        `⚠️ 没有待确认的 Task 更新（可能已超时或被取消）。请重新执行 \\`mem:update-task [补充]\\`。`,\n",
    "76": "        `❌ Task 更新失败：${result.error ?? \"unknown error\"}`,\n",
    "83": "      `✅ Task 已更新。\\n\\n` +\n",
    "84": "        `- **标题**：${result.title}（不可变）\\n` +\n",
    "85": "        `- **状态**：${result.status ?? \"running\"}\\n` +\n",
    "86": "        `- **新描述**：${trimDesc(result.description)}\\n` +\n",
    "108": "        `⚠️ 取消失败：${result.error ?? \"unknown\"}`,\n",
    "114": "      ? `✅ 已取消待确认的 Task 更新。`\n",
    "115": "      : `ℹ️ 当前没有待确认的 Task 更新。`;\n",
    "119": "  // ── 首次调用 ────────────────────────────────────────────────────────────\n",
    "125": "      `⚠️ 当前请求未携带对话消息，无参数版 \\`mem:update-task\\` 需要最近对话作为上下文。` +\n",
    "126": "        `\\n\\n你可以直接：\\`mem:update-task <你的补充描述>\\` 手动指定新描述。`,\n",
    "141": "  // 未绑\n",
    "144": "      `⚠️ 当前 session 尚未绑定 Task。请先执行 \\`mem:create-task\\` 创建一个 Task 再来更新。`,\n",
    "150": "  // 跨用户更新：kernel 不支持，proxy 侧提前拒绝并建议新建\n",
    "153": "      `❌ 无法更新：该 Task 不是你创建的，当前不支持跨用户修改。\\n\\n` +\n",
    "154": "        `如果你需要基于当前会话新建一个属于你的 Task，请使用 \\`mem:create-task\\`。`,\n",
    "160": "  // 其它错误\n",
    "165": "      : \"\\n\\n你可以：\\n1. 稍后重试\\n2. `mem:update-task <你的补充>` 手动指定新描述\";\n",
    "167": "      `❌ Task 更新失败：${detail}${hintLine}`,\n",
    "173": "  // LLM 判无需更新\n",
    "176": "      `ℹ️ Task 无需更新 —— 最近对话未产生新的进展或范围变化。\\n\\n` +\n",
    "178": "        `可稍后再次执行 \\`mem:update-task [补充]\\` 触发判断，或直接带参数强制更新。`,\n",
    "184": "  // 首次调用：pending 预览\n",
    "188": "      ? `\\n- 状态建议：${p.statusSuggestion}`\n",
    "191": "      `📝 Task「${p.currentTitle ?? p.taskId}」更新预览：\\n\\n` +\n",
    "192": "        `- 新描述：${trimDesc(p.draftDescription)}${statusLine}\\n\\n` +\n",
    "193": "        `回复 \\`mem:update-task confirm\\` 确认，或 \\`mem:update-task cancel\\` 取消。`,\n",
    "209": "  // 兜底（理论走不到）：返回当前状态\n",
    "211": "    `ℹ️ Task 无变更。\\n\\nTask ID: \\`${result.taskId}\\``,\n"
  },
  "MemoryProxy/src/mem-command/index.ts": {
    "1": " * mem-command 模块统一入口\n",
    "3": " * 提供：\n",
    "4": " *  - parseMemCommand()  — 检测是否为 mem: 命令\n",
    "5": " *  - executeMemCommand() — 执行命令并返回结果\n",
    "6": " *  - isMemCommandEnabled() — 检查配置开关\n",
    "26": "/** 已知命令列表 */\n",
    "37": " * 检查 memCommand 功能是否启用，且命令是否在白名单中。\n",
    "41": "  // session-reset 豁免白名单(session 管理命令)\n",
    "48": " * 执行已解析的 mem: 命令。\n",
    "49": " * 调用方已确认 isMemCommandAllowed 通过。\n",
    "57": "  // 未知命令\n",
    "59": "    const text = `❌ 未知命令：\\`mem:${cmd.command}\\`。输入 \\`mem:help\\` 查看可用命令。`;\n",
    "81": "      const text = `❌ 未知命令：\\`mem:${cmd.command}\\``;\n"
  },
  "MemoryProxy/src/mem-command/parser.ts": {
    "3": " * 从 request body.messages 中检测最后一条 user message 是否为 mem: 命令。\n",
    "5": " * 判定规则：\n",
    "6": " * 1. 取 messages 数组最后一条 role=\"user\" 的消息\n",
    "7": " * 2. 通过 agentAdapter.extractUserText 按客户端规则提取用户真实输入：\n",
    "8": " *    - claude-code: 取最后一个 text block（跳过 <system-reminder> 前缀元数据）\n",
    "9": " *    - codebuddy / unknown: 走保守的\"拼接所有 text\"（待抓包适配）\n",
    "10": " * 3. trim 后以 \"mem:\" 开头（大小写不敏感）\n",
    "11": " * 4. 整条消息就是命令（不是嵌在其他文字中间）\n",
    "17": "  /** 命令名（小写），如 \"sync\"、\"create-skill\"、\"help\" */\n",
    "19": "  /** 命令后的参数文本（如 create-skill 的提示词） */\n",
    "21": "  /** 原始 user 消息（审计 / L0 写入用） */\n",
    "26": " * 已知 mem 命令的 args 约束表。\n",
    "27": " * - `false`：命令**严格匹配** —— 命令后不能跟任何非空白内容（如 `mem:help 你好` 视为\n",
    "28": " *   普通对话，透传上游 LLM 而非拦截）。\n",
    "29": " * - `true` ：命令**接受可选 args** —— `mem:create-skill 数据库迁移总结` 命中且携带\n",
    "30": " *   args；`mem:create-skill`（无 args）也命中。\n",
    "32": " * 未列表的命令（用户 typo 如 `mem:helpp` / `mem:foo`）不受此校验影响 —— parser\n",
    "33": " * 仍返回 ParsedMemCommand，交给 `executeMemCommand` 走\"未知命令\"分支反馈\n",
    "34": " * `❌ 未知命令 mem:xxx，输入 mem:help 查看`，给用户 typo 兜底。\n",
    "40": "  // task 命令族：args 语义与 create-skill 对齐 —— 作为 LLM 生成 title/description\n",
    "41": "  // 的额外提示（reason）；空 args 表示纯从近 30 条上下文自动生成。\n",
    "48": " * 从 request body 中检测是否为 mem: 命令。\n",
    "49": " * 返回 null 表示非 mem: 命令，继续走正常链路。\n",
    "51": " * ⚠️ 只支持 body.messages[] 形态 (CC/CB)。Codex 走 body.input[]，\n",
    "52": " * 调用方（codexHandler）应先用 `codexAdapter.extractUserText(input)` 拿\n",
    "53": " * 到 text，再直接调 `parseCommandFromText(text)`（跳过 body 解析这一步）。\n",
    "55": " * @param body - 请求 body（含 messages 数组）\n",
    "56": " * @param agentSource - 客户端类型（URL 前缀），用于选择 agentAdapter\n",
    "57": " * @param options.checkFirst - 为 true 时检查第一条 user message 而非最后一条。\n",
    "58": " *   用于 session init 刚完成的场景：最后一条是 init 交互回答，首条才是用户原始命令。\n",
    "68": "  // 取目标消息：默认最后一条，checkFirst 时取第一条 user message\n",
    "78": "  // 通过 adapter 按客户端规则提取纯文本\n",
    "87": " * 从已提取的用户文本判定是否为 mem: 命令。\n",
    "89": " * 抽出这一层是为了让 codex handler 能复用同一套 mem 命令语义：\n",
    "90": " * codex body 用 `input[]` 而非 `messages[]`，parseMemCommand 从 body\n",
    "91": " * 起步的路径认不出 codex，早期直接返 null，导致所有 mem:xxx 静默透传给\n",
    "92": " * LLM，模型编造\"Memory synced\"之类假回复（P0-1 QA 报告）。codex handler\n",
    "93": " * 现在拿 `codexAdapter.extractUserText(input)` 得到 text 后直接调本函数。\n",
    "95": " * CC/CB 的 `parseMemCommand(body, agentSource)` 内部也走这条路径，行为\n",
    "96": " * 完全不变。\n",
    "99": "  // trim 后判断\n",
    "102": "  // 必须以 mem: 开头（大小写不敏感）\n",
    "105": "  // 提取命令部分（mem: 之后的内容）\n",
    "106": "  const afterPrefix = trimmed.slice(4); // 去掉 \"mem:\"\n",
    "108": "  // 兼容冒号后的可选空格\n",
    "111": "  // 拆分命令名和参数（第一个空格分割）\n",
    "126": "  // 命令名不能为空\n",
    "129": "  // 已知命令的 args 严格校验：命令不接受 args 时，args 非空即视为普通对话。\n",
    "130": "  // 例：`mem:help 你好` → 用户\"输入了 mem:help 并同时问了个问题\"，应该走上游\n",
    "131": "  // LLM 正常回答，而非返回 help 帮助文本。未知命令不受此约束（见 MEM_COMMANDS_ARGS 说明）。\n"
  },
  "MemoryProxy/src/mem-command/pending-store.ts": {
    "3": " * 存放 mem:create-task / mem:update-task 首次调用生成的\"待确认\"动作草稿。\n",
    "4": " * 用户下一轮回复 `... confirm` 时从这里取出落库；回复 `... cancel` 或超时则丢弃。\n",
    "6": " * 设计取舍：\n",
    "7": " *   - 内存 Map，重启丢失（用户重发一次即可，符合\"过渡态\"定位）。\n",
    "8": " *   - TTL 5 分钟，getPending 时 lazy 清理。\n",
    "9": " *   - key = team_id:agent_id:session_id（对齐 kernel 侧唯一键含义）；缺 agent_id\n",
    "10": " *     则用 \"-\" 占位，兼容未来 agent_id 缺失的路径。\n",
    "22": "  /** 目标 task id（一定是\"当前 session 绑定的真实 task\"）*/\n",
    "24": "  /** LLM 生成或用户直传的新描述 */\n",
    "26": "  /** LLM 建议的状态（如 \"running\" / \"completed\" / 其它自由文本），可为空 */\n",
    "28": "  /** 原任务当前 title（只读展示用） */\n",
    "30": "  /** 原任务当前 status（只读展示用） */\n",
    "32": "  /** 输入提示（args 或 \"session context\"） */\n",
    "39": "  /** 当前会话已绑定的旧 task，用于提示\"将替换绑定\" */\n",
    "91": "/** 测试用：全清 */\n"
  },
  "MemoryProxy/src/mem-command/pre-intercept.ts": {
    "1": " * mem:session-reset 的前置拦截判定.\n",
    "3": " * 背景:所有 mem 命令原本在各 handler 里 session-init 段**之后**才被识别\n",
    "5": " * `codexHandler.ts:609-680` / `workbuddyHandler.ts:1111-1180`)。这套顺序\n",
    "6": " * 对 sync / create-skill 是合理的 —— 它们依赖 sessionInfo。但 session-reset\n",
    "7": " * 需要在 **uninitialized / pending_* / initialized / bypassed** 四种起始状态\n",
    "8": " * 都能生效:\n",
    "10": " *   - uninitialized:state machine 会把 \"mem:session-reset\" 当成\"用户第一句\"\n",
    "11": " *     用作 form 反射数据的 asset_confirm 分支不适用,但至少会先弹一次 form,\n",
    "12": " *     用户看到 form 反而困惑\n",
    "13": " *   - pending_*:state machine 会把它当成\"用户对 form 的答复\",走 parseFormAnswer\n",
    "14": " *     → unrecognized → session bypass,reset 命令永远拦不到\n",
    "15": " *   - initialized / bypassed:老 mem-command 拦截段能识别,但拿掉 \"会话未初始化\n",
    "16": " *     命令不可用\" gate 后行为一致。这两种也走前置拦截更简洁\n",
    "18": " * 折中方案:只对 session-reset 加前置拦截 —— 其他 mem 命令不动。这个函数\n",
    "19": " * 就是判\"这个请求是不是 session-reset\",handler 里前置一句 if 决定要不要\n",
    "20": " * 短路。\n",
    "27": " * 判断请求 body 里最后一条 user 消息是不是 `mem:session-reset`.\n",
    "29": " * 兼容 body.messages[] (CC/CB/dsh) 和 body.input[] (Codex/WB) 两种形态。\n",
    "30": " * 内部用对应 adapter 的 `extractUserText` 提取纯文本,与既有 mem-command\n",
    "31": " * 拦截段保持一致的文本提取语义。\n",
    "33": " * 不抛错:任何异常 / 缺字段一律 false,让原链路继续跑。\n",
    "45": "    // Codex / WorkBuddy 用 body.input[] (Responses API)\n",
    "49": "      // 只识别\"最新一条 input item 是 role=user message\"的情况;\n",
    "50": "      // 若最新一条是 function_call_output 说明当前是 form 交互中,\n",
    "51": "      // codex 客户端 replay 整个历史 input 包括最早的 mem:session-reset —\n",
    "52": "      // 此时不应重复触发 pre-hook, 否则 state 会被无限打回 uninitialized 死循环。\n",
    "56": "      // 直接从最后一条 message 抽 text, 不复用 extractUserText (它会向前扫)\n",
    "68": "      // CC/CB/dsh 用 body.messages[]\n",
    "71": "      // 最后一条 user\n"
  },
  "MemoryProxy/src/mem-command/response-builder.ts": {
    "1": " * 按 Anthropic / OpenAI 协议构造伪造的 LLM 响应。\n",
    "3": " * 返回的 Response 对 IDE 来说就是一条正常的 assistant 回复。\n",
    "10": "  // 当请求开启了 extended thinking 时，必须包含 thinking block，\n",
    "11": "  // 否则 Claude Code 会认为响应不完整并发起额外请求。\n",
    "44": "  // 当请求开启了 extended thinking 时，先发一个完整的 thinking block，\n",
    "45": "  // 否则 Claude Code 会认为响应不完整并发起额外请求。\n",
    "182": "  // 关键：Responses API 里每个 event 的 data JSON **必须**带 `type` 字段\n",
    "183": "  //（跟 SSE `event:` 头同名）。codex 客户端解析器优先读 data.type，缺字段\n",
    "184": "  // 会丢弃事件，表现为伪造文本客户端不显示。对齐真实上游帧姿势。\n",
    "263": "  /** 请求是否开启了 extended thinking（Anthropic 专用） */\n"
  },
  "MemoryProxy/src/mem-command/task-draft-generator.ts": {
    "1": " * task-draft-generator · mem:create-task / mem:update-task 的 LLM 草稿生成器。\n",
    "3": " * proxy 首个\"主动\"向 LLM 发起请求的模块（其它 LLM 调用都是 passthrough 反向代理）。\n",
    "4": " * 骨架仿 packages/cost-guard/src/compressor/cfq/llm-infer.ts —— 直接 fetch OpenAI\n",
    "5": " * chat/completions + AbortSignal.timeout，不引第三方 SDK。\n",
    "7": " * 与 CFQ LLMInfer 的关键差异：\n",
    "8": " * - CFQ 失败 = 返回 null 数组（silent fallback），因为 CFQ 是可选增强；\n",
    "9": " * - 本模块失败 = 返回 { ok: false, error }（**显式错误**），因为 Task 是持久化实体，\n",
    "10": " *   坏草稿会污染库；上层 command 会把 error 拼进\"❌ Task 生成失败：...\"文案。\n",
    "12": " * 使用方：mem-command/commands/create-task.ts / update-task.ts（阶段 3.2 / 3.3）\n",
    "14": " * 参考：docs/design/... TODO(阶段5) 补设计文档\n",
    "17": "/** LLM 端点配置。字段与 LLMInferConfig 保持形状一致，便于将来抽公共。 */\n",
    "19": "  /** 总开关。默认 false —— 未启用时命令层直接返\"未配置\"错误。 */\n",
    "21": "  /** 模型名，如 \"deepseek-v3-0324\"。 */\n",
    "23": "  /** API 端点（不含 /chat/completions）。 */\n",
    "25": "  /** API Key，以 Bearer 头传递。 */\n",
    "27": "  /** 单次调用超时（毫秒）。建议 15000-30000（草稿要写完整）。 */\n",
    "31": "/** 最近对话消息片段（供 LLM 理解上下文）。 */\n",
    "37": "/** 现有 Task（update 模式必填）。 */\n",
    "44": "/** 生成器输入。 */\n",
    "46": "  /** create：新建一个 task；update：基于现有 task 判定变更 + 改写。 */\n",
    "48": "  /** 用户 mem:create-task/update-task 后的额外提示（reason），可空。 */\n",
    "50": "  /** 最近对话消息（proxy 从 sessionMessages 剪出来，通常近 30 条）。 */\n",
    "52": "  /** update 模式必填，其它模式忽略。 */\n",
    "55": "   * 仅 create 模式生效。用户已在 mem:create-task 后写明 title，proxy 强制锁定：\n",
    "56": "   * LLM 只负责根据对话生成 description，返回的 title 字段会被忽略。\n",
    "57": "   * 上层调用方需自行把 lockedTitle 传下来（generator 不做 40 字截断，调用方保证）。\n",
    "62": "/** 生成器输出：成功携带结构化草稿；失败带 error 文案。 */\n",
    "68": "      /** 建议状态，允许模型给出（可选）。 */\n",
    "71": "       * 仅 update 模式有效。false 表示\"最近对话未产生新进展，Task 无需更新\"—— 上层\n",
    "72": "       * 应直接返\"Task 无需更新\"给用户，不再进弹窗流程。create 模式恒为 true。\n",
    "79": " * Status 校验（放宽版）：\n",
    "80": " *   按 TAPD 需求 & 用户决策，proxy 不做 status 枚举校验，LLM 出啥透传啥；\n",
    "81": " *   仅做 trim + 空/非字符串过滤，最终由内核决定是否接受。\n",
    "84": "/** 输出 schema 上限。 */\n",
    "87": "/** 最近对话截断（防 prompt 过长）。 */\n",
    "91": " * LLM 单次生成的 token 上限。\n",
    "93": " * 从 800 提到 2000（2026-08-18 修复）：实际观察到 desc + title + status + JSON\n",
    "94": " * 结构字符总量在中英混杂场景经常 >800 而被截断，导致 JSON 中途结束 parse 失败。\n",
    "95": " * 参考：wiki-ingest 用 8192，L1 extractor 用 4096；本处 draft 输出限制在 title≤40\n",
    "96": " * + desc≤300 + status，理论最大 ~800 字符 → tokens 上限给 2000 有充分余量。\n",
    "101": " * LLM 调用重试策略（2026-08-18 加入）。\n",
    "103": " * 触发场景：LLM 偶发抖动（空对象 / 截断 / 超时），单次成功率不到 100%，\n",
    "104": " * 但连续 2 次全部空对象/截断的概率很低。用户视角：无感，一次点击 = 一次成功。\n",
    "106": " * 参数：\n",
    "107": " *   - LLM_RETRY_MAX_ATTEMPTS=3：总共 3 次机会（1 次首发 + 2 次重试）\n",
    "108": " *   - LLM_RETRY_BASE_DELAY_MS=200：指数退避基数，第 2 次等 200ms，第 3 次等 400ms\n",
    "110": " * 什么时候重试：只要 attemptDraftOnce 返回 ok=false 就重试（不区分具体错误类型，\n",
    "111": " * 因为空对象 / 截断 / schema 违规 / 上游 5xx 都是\"再来一次可能就好了\"的情况）。\n",
    "112": " * 什么时候不重试：cfg.enabled=false / 参数校验不通过 / 3 次都失败。\n",
    "118": " * 首次尝试的超时上限（2026-08-20）：现网观察到 gateway 偶发挂 20-30s，\n",
    "119": " * 首次就用 cfg.timeoutMs（默认 30s）会让用户干等。首次改成 min(cfg.timeoutMs,\n",
    "120": " * FIRST_TIMEOUT_MS=10s)，超时后立刻退避 200ms + retry，retry 用完整 timeoutMs\n",
    "121": " * 兜底\"真的慢但会成功\"的场景。env TDAI_TASK_DRAFT_FIRST_TIMEOUT_MS 可覆盖。\n",
    "132": "  // 不能超过配置本身（不然改动无效）\n",
    "137": " * 指令关键词剥离表：LLM 有时会把用户输入的原始指令字面搬进 title（如\n",
    "138": " * \"Fix mem:create-task LLM ...\"），这些前缀既冗余又占字符预算。\n",
    "139": " * 匹配到就截掉，让真正的语义部分能落到 40 字以内。\n",
    "147": " * create 模式 system prompt。\n",
    "149": " * 2026-08-18 强化（针对生产失败根因）：\n",
    "150": " *   - 用 STRICT / MUST NOT / NEVER 等硬约束词，代替之前的 ≤ 40（LLM 会越界）\n",
    "151": " *   - 加正确示例 + 反例，让 LLM 明确\"什么是超长\"、\"什么是空对象\"\n",
    "152": " *   - 明确禁止空对象：如果信息不够也要给合理默认，避免 `{}`\n",
    "161": "    背景 (background) → 目标 (goal) → 已知约束 (known constraints)\n",
    "167": "  {\"title\":\"Refactor auth module\",\"description\":\"背景：现有 auth 逻辑分散在 3 个文件。目标：合并到 auth-service。约束：不改动对外 API。\",\"suggestedStatus\":\"running\"}\n",
    "176": "/** create 模式（title 已锁定）system prompt —— 只让 LLM 出 description。 */\n",
    "184": "    背景 → 目标 → 已知约束\n",
    "191": "/** update 模式 system prompt。 */\n",
    "199": "  - 目标调整 (goal changes)\n",
    "200": "  - 约束 (constraint changes)\n",
    "201": "  - 进展 (progress)\n",
    "202": "  - 关联链接 (linked resources / references)\n",
    "203": "  - 参与 Agent (collaborating agents)\n",
    "216": " * 构造发给 LLM 的 user message。\n",
    "256": " * 从 LLM 原始输出里提取 JSON 对象。\n",
    "258": " * 三级降级：\n",
    "259": " *   1) 直接 JSON.parse —— 覆盖标准场景\n",
    "260": " *   2) 剥离 markdown fence（```json ... ``` 或 ``` ... ```）后再 parse\n",
    "261": " *   3) 括号平衡扫描：找到第一个 `{`，向后配对到匹配的 `}` 截取子串再 parse\n",
    "262": " *      —— 覆盖 LLM 前后加了自然语言的场景，如：\n",
    "263": " *      \"好的，这是 JSON：{...}\"、\"Here you go:\\n{...}\\n希望有帮助\"\n",
    "265": " * 扫描时会正确处理字符串字面量内的 `{` `}` `\"`（不参与平衡计数）。\n",
    "267": " * 若三级都失败返回 null（调用方产出 \"LLM output is not valid JSON\" 错误）。\n",
    "270": "  // 1) 直接 parse\n",
    "277": "  // 2) 剥离 markdown fence\n",
    "287": "  // 3) 括号平衡扫描\n",
    "301": " * 括号平衡扫描：从 raw 里找第一个能自洽闭合的 `{...}` 子串。\n",
    "302": " * 状态机处理字符串字面量与转义，防止字符串里的 `{}` 干扰计数。\n",
    "343": " * 归一化字符串字段：trim + 长度校验。\n",
    "346": " *   - \"strict\"（默认）：超长返 null（老行为，用于必须严格的场景）\n",
    "347": " *   - \"clip\"：超长自动截到 maxLen（保留有效内容，用于 title/description\n",
    "348": " *     这类\"哪怕不完美也比失败强\"的字段）\n",
    "350": " * 空字符串一律返 null（没有任何截断能救空串）。\n",
    "367": " * 从 title 里剥离 mem:xxx 之类的指令关键词前缀。\n",
    "368": " * LLM 常把用户当前指令写进 title（如 \"Fix mem:create-task JSON parse\"），\n",
    "369": " * 剥离后能让真正的语义部分不超字数上限。\n",
    "370": " * 只剥前缀、不剥中间；空匹配就原样返回。\n",
    "380": "/** 归一化 status 字段：可选，允许缺失；不做枚举校验，只做基本清洗。 */\n",
    "389": " * 生成 Task 草稿（带自动 retry）。\n",
    "391": " * 外层职责：\n",
    "392": " *   - 前置参数校验（不重试）\n",
    "393": " *   - 循环调 attemptDraftOnce，最多 LLM_RETRY_MAX_ATTEMPTS 次\n",
    "394": " *   - 每次失败记录日志（attempt=N/M reason=xxx），指数退避后再试\n",
    "396": " * @returns { ok:true, ... } 或 { ok:false, error }\n",
    "423": "        // 打点：让运维知道触发过 retry，但最终成功了\n",
    "431": "    // 最后一次失败，不再等待\n",
    "448": " * 单次 LLM 调用 + 结果解析。**内部函数，外层不要直接调**。\n",
    "450": " * 与老的 generateTaskDraft 主体逻辑一致，只是：\n",
    "451": " *   - 参数校验移到了外层\n",
    "452": " *   - 加了 finish_reason / usage / http_status 观测日志（第一次成功也打，方便看抖动率）\n",
    "454": " * attempt 参数只用于日志。\n",
    "464": "  // 首次用短 timeout（默认 10s），后续用完整 cfg.timeoutMs\n",
    "505": "  // 观测：finish_reason + usage.completion_tokens。三种典型值：\n",
    "506": "  //   - \"stop\"          正常完成\n",
    "507": "  //   - \"length\"        触到 max_tokens 被截断 → 需要扩大 max_tokens\n",
    "508": "  //   - \"content_filter\" 触发安全策略 → prompt/context 有问题\n",
    "509": "  //   - null            上游没吐（多半是空对象场景）\n",
    "526": "  // 观测公共字段（每条 FAIL 日志都会带这三个，方便一眼看出根因）\n",
    "529": "  // 三级兜底：直接 parse → markdown fence → 括号平衡扫描\n",
    "532": "    // 关键排查日志：把 LLM 原始 content 前 500 字打出来，方便定位是哪种失败模式\n",
    "533": "    // （标准场景 / markdown fence / 前后带自然语言 / 完全无 JSON）\n",
    "534": "    // finish_reason=length 就说明是 max_tokens 截断，需要扩大上限\n",
    "549": "  // update 模式：先看 changed 字段\n",
    "551": "    // 保留 currentTask 值原样返回，changed=false 上层直接跳过弹窗\n",
    "561": "  // create 模式 + lockedTitle：title 强制使用 lockedTitle，只解析 description\n",
    "563": "  // 宽容策略（2026-08-18）：description 只在**完全缺失/空串/非字符串**时才失败；\n",
    "564": "  // 超长自动截断到 MAX_DESC_LEN——LLM 少写几个字比返错给用户强得多。\n",
    "583": "  // title 归一化：先剥指令关键词前缀，再走 clip 模式。\n",
    "584": "  //   典型场景：\"Fix mem:create-task JSON parse failure\"（42 字，会因超 40 拒绝）\n",
    "585": "  //   → 剥离 \"Fix mem:create-task \" → \"JSON parse failure\"（更精炼且不超字）\n",
    "613": " * 日志辅助：截断长字符串，防止 LLM 洗版把日志撑爆。\n",
    "614": " * 500 字够看清 JSON 结构与主要内容，多余的用 `...[truncated N]` 标记。\n",
    "622": " * 日志辅助：把已解析的 obj 序列化为紧凑字符串再截断（保留结构）。\n",
    "623": " * 用于失败分支——让排查时能一眼看到 LLM 到底填了什么字段、什么值。\n"
  },
  "MemoryProxy/src/mem-command/types.ts": {
    "1": " * mem-command 模块类型定义\n",
    "7": " * 最近对话消息片段。task-draft-generator 消费。\n",
    "8": " * 目前只需 role + content，不带 tool_calls / attachment，保持极简。\n",
    "25": "  /** 命令参数（如 create-skill / create-task 的提示词） */\n",
    "27": "  /** 请求是否开启了 extended thinking（Anthropic 专用） */\n",
    "30": "   * 当前请求的最近对话消息（用于 task-draft-generator 生成草稿）。\n",
    "32": "   * - CC/CB 走 chat/completions：直接是 body.messages[]\n",
    "33": "   * - Codex/WorkBuddy 走 Responses API：目前传空数组（阶段 5 联调时再补 body.input 解析）\n",
    "35": "   * 未提供时视为空数组，task 命令族会返 \"no recent messages\" 错误。\n",
    "41": " * 已支持的 mem: 命令名 —— 强类型联合，供 index.ts 分派 / commands/* 收窄使用。\n",
    "42": " * 未列入的字符串会被 executeMemCommand 走\"未知命令\"兜底。\n",
    "53": "  /** 用户可读的结果文本（写入 L0 / 展示给用户） */\n",
    "55": "  /** 结构化数据（可选） */\n",
    "57": "  /** 构造好的 HTTP Response（已按协议格式伪造） */\n"
  },
  "MemoryProxy/src/mem-command/utils.ts": {
    "1": " * mem-command 内部工具函数。\n",
    "3": " * 目前只有 extractSimpleMessages —— 把 handler 层的 body.messages（协议原生形态）\n",
    "4": " * 归一到 task-draft-generator 消费的 { role, content } 极简格式。\n",
    "6": " * 已支持三种协议形态（对同一个函数入参兼容）：\n",
    "8": " *   - Anthropic (CC 原生):    body.messages = [{ role, content: string | Array<{type:\"text\",text}|...> }]\n",
    "12": " * Responses API 与前两者主要差别：\n",
    "13": " *   1. content block 用 `type:\"input_text\"` / `type:\"output_text\"` 而非 `type:\"text\"`\n",
    "14": " *   2. 每条 message 外层多一个 `type:\"message\"` 包裹\n",
    "15": " *   3. input[] 里还夹杂 function_call / function_call_output 等非 message 项 —— 只取 message\n",
    "17": " * 该函数对三种形态都自动识别，调用侧不用区分。\n",
    "23": " * 从 content 数组抽取纯文本。\n",
    "24": " * 支持三种 block 类型：\n",
    "26": " *   - {type:\"input_text\", text}   (Responses API 用户/系统消息)\n",
    "27": " *   - {type:\"output_text\", text}  (Responses API 助手消息)\n",
    "28": " * 未知类型的 block 忽略（e.g. tool_use / function_call / image ...）。\n",
    "44": " * 从任意 messages 数组抽取 { role, content } 极简形态。\n",
    "46": " * 容错策略：\n",
    "47": " * - 非数组 / 空 → 返 []\n",
    "48": " * - role 不在 [\"user\",\"assistant\",\"system\"] → 忽略该条\n",
    "49": " * - Responses API 项若含 type 字段且非 \"message\"（如 \"function_call\" / \"function_call_output\"）→ 忽略\n",
    "50": " * - content 是数组 → 合并所有 text/input_text/output_text 段（其余类型忽略）\n",
    "51": " * - content 是空字符串 → 忽略该条\n",
    "54": " * 把 mem 命令的 args 压成一行简短日志片段，便于 [mem-command] 打点排查。\n",
    "56": " * - 换行/连续空白 → 单空格\n",
    "57": " * - 超过 max（默认 40）→ 尾部截断加 \"...\"\n",
    "58": " * - 空/纯空白 → 返回空串（调用侧再决定是否拼进日志）\n",
    "60": " * 注意：这个只给日志用，绝对不能反向解析回原文；args 里可能带敏感/多行内容，\n",
    "61": " * 展示前必须过它。\n",
    "79": "    // Responses API: 只保留 type=message，跳过 function_call / function_call_output 等\n",
    "80": "    // （OpenAI / Anthropic messages 里没有 type 字段，此判定不生效，兼容）\n"
  },
  "MemoryProxy/src/memory/bridge-telemetry.ts": {
    "1": " * Bridge-side tool-call 埋点 helper（memory-bridge + skill-bridge 共用）。\n",
    "3": " * 设计：每次 upstream fetch 完成（成功或失败）都发一条 kind='bridge_call'。\n",
    "4": " *      调用方负责把 body 脱敏到 <= 512 字节（本函数不再清洗），只做透传。\n",
    "6": " * 硬约束（§7.-1）：\n",
    "7": " *   - 同步返回 void\n",
    "8": " *   - sink 异常静默吞掉\n",
    "9": " *   - body/sub 已由调用方准备好，绝不额外读 session store\n",
    "20": "  /** \"claude-code\" | \"codebuddy\" | \"unknown\" — 从 sessionKey 前缀反解 */\n",
    "24": "  /** 具体 sub 字符串（\"atomic/search\" / \"skill/get\" 等） */\n",
    "26": "  /** 已脱敏、已截断的 outbound body（<= 512 字节） */\n",
    "28": "  /** upstream HTTP status（网络失败可传 0 或 502） */\n",
    "30": "  /** upstream 耗时毫秒 */\n",
    "33": "   * 前置校验失败原因。空/未传 = 请求打到了上游（成功或 4xx/5xx）。\n",
    "34": "   * 非空 = proxy 前置早退，没到 fetcher。见 clickhouse.ts ToolCallLogInput 注释。\n",
    "40": " * 发一条 bridge_call 埋点。sink 默认走 clickhouse.writeToolCallRow。\n",
    "41": " * 内部自吞异常，绝不 throw。\n",
    "69": "      // sink 抛 → 埋点绝不阻塞业务\n",
    "72": "    // input 构造异常也吞掉\n",
    "77": " * 从 proxy session-key 反解 agentSource。\n",
    "80": " *   \"conv-abc\"（无前缀）    → \"unknown\"\n",
    "89": " * 前置校验失败埋点 helper —— proxy 层拒绝了请求, 没能打到上游 fetcher。\n",
    "91": " * 前置早退大部分连 session ids 都还没解析出来 (missing header / bad content-type /\n",
    "92": " * invalid json ...), 能拿到的稳定字段有限。剩下的字段按可选透传。\n",
    "94": " * 内部套 emitBridgeToolCallTelemetry, kind 仍是 'bridge_call', 通过 rejectReason\n",
    "95": " * 非空来区分。老 dashboard 全部 kind='bridge_call' 的 SQL 一字不动仍能跑,\n",
    "96": " * 新增维度靠 `WHERE reject_reason != ''` 反查。\n",
    "98": " * 硬约束: 同步返回 void, 埋点绝不阻塞业务; sessionKey 允许空串。\n",
    "101": "  /** derive 出来就填, 前置阶段 (missing header) 派生不了填 \"\" */\n",
    "104": "  /** 稳定枚举值, 供 GROUP BY —— 详见 tool_call_logs.reject_reason 注释 */\n",
    "106": "  /** proxy 返给客户端的 HTTP status (401/415/400/...); 存到 upstream_status 列 */\n",
    "108": "  /** subpath 若已能算出就填, 否则 \"\" */\n",
    "110": "  /** body 若已解析可填 (调用方负责 <=512 截断), 否则 \"\" */\n"
  },
  "MemoryProxy/src/memory/memory-bridge.ts": {
    "3": " * 设计思路与 src/skill/skill-bridge.ts 同形：\n",
    "4": " *   - 不在 body.tools 里塞 native tool 定义（agent host 不识别）\n",
    "5": " *   - 注入文本 `<tdai_memory_tools>` 引导 LLM 用 Bash curl 这个 bridge\n",
    "6": " *   - bridge 强制注入 session IdFields + serviceToken 鉴权后转发到 tdai\n",
    "8": " * 行为：\n",
    "9": " *   1. 路径必须是 /memory-bridge/v3/{sub} ；sub 在 ALLOWED_SUBPATHS 内\n",
    "10": " *   2. 强制 POST + Content-Type application/json\n",
    "11": " *   3. 必须能识别 session（x-conversation-id / x-session-id ...），否则 401\n",
    "12": " *   4. body 里 team_id/user_id/agent_id/session_id 一律被 session 值覆盖（防伪造）\n",
    "13": " *   5. 转发到 ${coreSkill.endpoint}/v3/{sub}，添加 Bearer + service-id 头\n",
    "14": " *   6. 透传 status 和 JSON body\n",
    "16": " * 安全：\n",
    "17": " *   - allowlist 限定只有 search / read 类只读 subpath；mutation 走主链路\n",
    "18": " *   - 不接受 atomic/update / scenario/write / core/write 等写操作\n",
    "19": " *   - v3 strict isolation: 强制注入 session_id，满足 L0/L1 必填要求\n",
    "35": " * 允许通过 bridge 转发的 tdai 子路径（**只读**，LLM 通过 Bash 工具按需调用）。\n",
    "37": " * 设计取舍：\n",
    "38": " *   - L0/L1 不再每轮自动召回，改为静态工具按需检索（cache 友好），因此放行\n",
    "39": " *     atomic/* 与 conversation/* 的 search/query。\n",
    "40": " *   - L2：system 给索引 `<l2_scene_index>`，正文按需读 → 放行 scenario/ls + scenario/read。\n",
    "41": " *   - L3（persona）：直接注入 system，无需工具 → **不放行** core/read。\n",
    "43": " * 写操作（write / rm / add / update / delete）一律不在 allowlist 里；写入走主链路。\n",
    "46": "  \"atomic/search\",        // L1 原子记忆 hybrid search\n",
    "47": "  \"atomic/query\",         // L1 按 type/时间/分页\n",
    "48": "  \"conversation/search\",  // L0 对话 hybrid search\n",
    "49": "  \"conversation/query\",   // L0 按 session 取历史\n",
    "50": "  \"scenario/ls\",          // L2 场景列表（path 索引）\n",
    "51": "  \"scenario/read\",        // L2 按 path 读全文\n",
    "63": "   * `SessionInfo.space_id`（原本来自请求路径 `/{agent}/{spaceId}/...`）。\n",
    "64": "   * 用它做 tenant 路由是正确形态；`config.tdai.serviceId` /\n",
    "65": "   * `config.coreSkill.serviceId` 只作为老 session（迁移前缓存）的兜底。\n",
    "70": "   * (`${agentSource}:${sessionId}`). 用于埋点侧对齐 session_init_logs 的\n",
    "71": "   * session_key —— 埋点不能猜前缀，必须用真实命中的 key。\n",
    "77": " * curl 模板固定 2 header:\n",
    "81": " * 不再吃 Authorization。见 docs/design/2026-08-03-binding-flatten.md。\n",
    "138": "  // handler 层存的 L1 key 形如 `${agentSource}:${sessionId}`; curl 拿到的\n",
    "139": "  // 通常是 bare sessionId。按候选前缀顺序探,命中即返回。\n",
    "154": " * L2 fallthrough —— 拍平后只吃 (spaceId, sessionId)。见\n",
    "157": " * 不再走 verifyUserKey + getOrRecover 4 段路径:\n",
    "158": " *   1) bridge curl 模板没塞 bearer,verify 拿不到 userId\n",
    "159": " *   2) 拍平后 binding.json 里已经存了 user_id/team_id/agent_id/agent_source/user_key,\n",
    "160": " *      单次 GET 直接凑齐 IdFields\n",
    "260": "    // 前置校验早退埋点: 每个 return 前落一条 reject_reason 非空的 bridge_call, 与 skill-bridge 对称。\n",
    "353": "    // 强制注入 session IdFields — LLM 不能伪造身份。\n",
    "354": "    // search 类默认同时查 self + 借入 chat_memory；非 search 类默认 self，可通过 body.agent_id\n",
    "355": "    // 选择 <tdai_profile_memory> 里暴露的 imported agent_id。\n",
    "377": "    // task_id 优先级：caller 显式传 > session 注入。session_id 保持\"仅 caller 显式传\"，\n",
    "378": "    // 因为 search 类希望默认跨 session（agent 维度）；task_id 属于身份维度，仍应强制。\n",
    "407": "        // 埋点：每次实际调用 upstream 都记一条（成功/失败都算，方案 §5.1）\n",
    "408": "        // 优先用 loadSessionIdsL1 里真实命中的 compositeKey，跟 session_init_logs 对齐；\n",
    "409": "        // 拿不到才回落 raw sessionKey（L1/L2 miss 路径不该走到这里，但保底）。\n",
    "429": "      // 两类 search 的响应 shape 不同：\n",
    "432": "      // 早期代码固定读 data.items，导致 conversation/search 在 multi 分支\n",
    "433": "      // 永远返回空（历史 bug）。这里按 sub 分派读写字段，保持透传语义。\n",
    "469": "      // 保持透传语义：返回字段名与上游一致（items vs messages）。\n"
  },
  "MemoryProxy/src/meta/client.ts": {
    "84": " * /v3/meta/task/create 请求入参。\n",
    "86": " * 权限约束（内核）：caller 必须是 team 活跃成员，且 creator_user_id 强制 == caller。\n",
    "87": " * mem:create-task 命令层已经保证 creator_user_id = sessionInfo.user_id，不允许伪造。\n",
    "96": "  /** 可选：由调用方指定 task_id；缺省时内核自动生成。mem 命令族不传，交给内核生成。 */\n",
    "99": "   * 可选：关联的 Agent。TAPD 需求：mem:create-task 需要\"关联当前 Agent\"。\n",
    "100": "   * 内核会把它写进 task 的 agent_ids 或元数据字段（具体行为由内核决定，proxy 只透传）。\n",
    "106": " * /v3/meta/task/update 请求入参。\n",
    "108": " * 权限约束（内核）：caller 必须 == task.creator_user_id。\n",
    "109": " * mem:update-task 命令层在调用本 API 前会先做 owner-check，非 creator 时直接返错，\n",
    "110": " * 不会走到这里（见需求 Q4 决策 B —— 不越权改核心权限模型）。\n",
    "113": "  /** 标题按需求约定不允许更新，此处仅描述 / 状态可改。 */\n",
    "138": "// One append per session-init 完成 —— 记录 (team, task, agent, user) 四元组的\n",
    "139": "// 参与事件。语义与 `task-agent/link`（声明关系）不同：这里是\"实际发生了一次\n",
    "140": "// session\"的追加日志，前端看板据此展示\"参与 User / Agent\"。见 v3.2 §34/35。\n",
    "158": "  /** 来源标识，例：`context_proxy:claude-code`。省略则内核默认 `unknown`。 */\n",
    "161": "  /** ISO8601 UTC；省略则用服务端当前时间。 */\n",
    "172": "  /** 是否按 user_id 去重（内核默认 false）。仅 user 维度生效，agent 需前端自行 dedupe。 */\n",
    "305": "   * mem:create-task 命令族入口。约束见 `CreateTaskInput` 注释：\n",
    "306": "   *   - creator_user_id 必须 == 当前 caller\n",
    "307": "   *   - team_id 必须是 caller 的活跃 team\n",
    "308": "   * 内核 envelope 直接返回落盘后的 TaskEntity（含生成的 task_id）。\n",
    "325": "   * Patch an existing task's description / status. Title is immutable per 产品需求。\n",
    "327": "   * 调用方需先做 owner-check（caller == creator）；本方法不做二次校验，\n",
    "328": "   * 内核会以 401/403 拒绝非 creator。\n",
    "372": "   * Called fire-and-forget from session-init completion —— 失败不阻断注入路径。\n",
    "373": "   * 内核为 append-only，同一四元组重复调用会累积多条；前端展示时按 user_id\n",
    "374": "   * 走 `listParticipationLogs({dedupe:true})`，agent 维度靠客户端 dedupe。\n",
    "396": "   * de-duplicates by `user_id` (kernel doc §35) —— 需要按 agent_id 去重时，\n",
    "397": "   * 请在调用侧再走一遍 client-side dedupe。\n",
    "419": "   * `applyVisibilityFilter=true`（默认）：让内核基于 canBindAsset 过滤掉\n",
    "420": "   * agent 无法访问的绑定 —— 典型场景：其它成员把资产切成 private 后，\n",
    "421": "   * 老的 fixed-asset 表里还残留绑定行，但 caller 已经不能使用；带 filter\n",
    "422": "   * 后 injection / memory-bridge 只会拿到当前仍然可用的绑定。\n",
    "424": "   * 传 false 只用于需要看到\"物理绑定\"的场景（e.g. 管理面板 debug）。\n",
    "450": "      // 停止条件（任一命中即停）：\n",
    "451": "      //   1. 累计 items 数 ≥ total（正常场景）\n",
    "452": "      //   2. 本页返 0 条（apply_visibility_filter 过滤后 items 可能全空，\n",
    "453": "      //      但内核 total 仍是过滤前的原始行数 —— 不 break 会永远翻页；\n",
    "454": "      //      2026-07-11 修：新加此条防死循环）\n",
    "455": "      //   3. 本页返 < FA_PAGE_SIZE（小于一页 → 到底）\n"
  },
  "MemoryProxy/src/opik.ts": {
    "255": "    input: span.inputMessages,    // 直接传 messages 数组\n",
    "256": "    output: outputMessages,       // 直接传 messages 数组\n"
  },
  "MemoryProxy/src/pricing.ts": {
    "3": " * TDAI 下发的计费规则以 Credit / 1K Token 为单位，按不同 token 类型\n",
    "4": " * (input / output / cacheRead / cacheWrite5m / cacheWrite1h) 分别定价。\n",
    "5": " * 定价表放在 config.yaml 的 `creditPricing.models`，支持热更新。\n",
    "7": " * 匹配规则（`getModelPricing`）：\n",
    "8": " *   大小写不敏感的全词匹配 —— modelId 与 config.name 忽略大小写后必须完全相等。\n",
    "9": " *   未匹配时返回 null，调用方降级为 raw token count。\n",
    "32": " * 解析 model 的展示名（用于 UI/报表）。\n",
    "34": " * 匹配逻辑：\n",
    "35": " * 1. `modelId` 空/null/undefined → 返回 `\"\"`\n",
    "36": " * 2. 定价表命中且 entry.modelName 非空 → 返回 entry.modelName（如 \"Claude Sonnet 4\"）\n",
    "37": " * 3. 定价表未命中 或 命中但 modelName 未配置/为空 → **回落 modelId 本身**\n",
    "38": " *    （前端始终有非空展示；unknown model 至少能看到内部 ID）\n",
    "40": " * 与 `getModelPricing` 共用一份匹配逻辑（大小写不敏感全词匹配）。\n",
    "44": " * @returns 展示名字符串（永远非 null，可能为 \"\"）。\n",
    "56": " * 反向解析：把客户端侧的展示名（`modelName`）翻译回真实 `model_id`（`entry.name`）。\n",
    "58": " * 用于请求拦截阶段——客户端可以在 `model` 字段填易辨认的 `modelName`\n",
    "59": " * （如 `claude-opus-4.7`），代理转发上游前将其换成对应的 model_id\n",
    "60": " * （如 `ep-pksklwtb`）。是 `resolveModelName` 的逆操作，复用同一份\n",
    "61": " * `creditPricing.models` 映射，避免双份维护。\n",
    "63": " * 匹配逻辑（与 `getModelPricing` 保持大小写不敏感）：\n",
    "64": " * 1. `requested` 空/null/undefined → 原样返回（空串）\n",
    "65": " * 2. 命中某条 entry 的 `modelName`（忽略大小写、非空）→ 返回该 entry 的 `name`\n",
    "66": " * 3. 未命中（含 requested 本身已是真实 model_id、或未知模型）→ **原样返回**\n",
    "67": " *    （保证向后兼容：直接传真实 model_id 的客户端不受影响）\n",
    "69": " * 同一 `modelName` 若对应多条 entry，取第一条命中的（`Array.find` 语义）。\n",
    "72": " * @param requested - 客户端请求中的 `model` 字段值。\n",
    "73": " * @returns 真实 model_id；无匹配时回落 `requested` 本身。\n",
    "90": " * 校验客户端请求的 `model` 是否已在价目表的 **`modelName`（展示名）** 中登记。\n",
    "92": " * 用于请求入口的门禁：价目表配置存在时，客户端只能用展示名（`modelName`）\n",
    "93": " * 请求，真实 `model_id`（`entry.name`）视为内部细节，不再作为公开入口。\n",
    "94": " * 未匹配的 model 一律拒绝，避免\"转发成功但无法计费\"的静默漏计费问题。\n",
    "96": " * 规则：\n",
    "97": " * 1. `config` / `config.models` 为空 → **返回 true**（价目表未配置时跳过校验，\n",
    "98": " *    向后兼容旧部署；由 `computeCreditDelta` 走 raw 追溯路径处理）。\n",
    "99": " * 2. `requested` 空/null/undefined → **返回 false**（必须显式提供 model 才允许放行）。\n",
    "100": " * 3. 命中任意 entry 的**非空** `modelName`（大小写不敏感全词匹配）→ true。\n",
    "101": " * 4. 否则 → false。\n",
    "103": " * 注：未配置 `modelName` 的 entry 不可被客户端请求命中（此时该模型属\"内部专用\"，\n",
    "104": " * 仅供内部转发使用，不对客户端暴露）。\n",
    "107": " * @param requested - 客户端请求中的 `model` 字段值。\n",
    "108": " * @returns 是否允许放行。\n",
    "114": "  // 价目表未配置：跳过校验（向后兼容）\n",
    "116": "  // 显式要求非空 model\n"
  },
  "MemoryProxy/src/rate-limit/guard.ts": {
    "96": "    ? \"该模型请求频率已达上限，请稍后重试\"\n",
    "97": "    : \"该模型输入 Token 用量已达上限，请稍后重试\";\n"
  },
  "MemoryProxy/src/report/log.ts": {
    "149": "          // 复杂类型（对象/数组）→ 优先 JSON 序列化保留结构，失败时（如循环引用、\n",
    "150": "          // BigInt、Symbol）再退回 String() 兜底，避免出现 \"[object Object]\" 丢失字段。\n",
    "151": "          // 上限 2000 字符防止单条日志膨胀。\n"
  },
  "MemoryProxy/src/routes/instance-destroy.ts": {
    "1": " * `POST /v3/instance/proxy-destroy` — 运维口：清理 proxy 侧为某个 instance\n",
    "2": " * （= spaceId）缓存的 ProxyStorage 数据 + kernel-sts pool 里的 STS backend。\n",
    "4": " * 契约字段名对齐 core 的 `/v3/instance/destroy`\n",
    "5": " * （`tdai-memory-openclaw-plugin/src/gateway/server.ts:1202-1221`），路径用\n",
    "6": " * `proxy-destroy` 动作与 core `destroy` 区分，抓包/日志一眼可辨。\n",
    "8": " * 请求：\n",
    "10": " * Header 可选：Authorization: Bearer <admin.apiKey>\n",
    "12": " * 响应 200：\n",
    "19": " *         \"storage_ttl_deleted\":   <number>,   // 缺省 0；出错时 storage_ttl_error 会同时出现\n",
    "27": " * 失败：\n",
    "28": " *   400 参数缺失 / 参数非法（含 `/` 或 `..`）\n",
    "29": " *   401 auth 开启且 Bearer 缺失/不匹配\n",
    "30": " *   200 局部失败：cleaned 内含 <step>_error 字段（对齐 core 局部成功不阻断策略）\n",
    "32": " * 说明：Redis session store（`cg:sess:*`）不清理。路由模块的 `sessionKey` 来\n",
    "33": " * 自 `x-conversation-id` / `x-claude-code-session-id`（`profiles/*.ts`），不\n",
    "34": " * 含 spaceId，无法按 space 精确 SCAN；默认 TTL 1800s 自然过期。cleaned 里\n",
    "35": " * `redis_skipped: \"per-session-ttl-only\"` 显式声明这一点。\n",
    "45": "/** 统一的运维口响应外壳。 */\n",
    "61": " * 构建 handler；`config` 只用来读 `admin.apiKey`，storage 单例通过\n",
    "62": " * `getProxyStorage(config.storage)` 拿（`initProxyStorage()` 已在 index.ts 里\n",
    "63": " * await 过，这里是幂等 get）。\n",
    "73": "    // ── 2. 参数解析 & 校验 ─────────────────────────────────────\n",
    "88": "      // 复用 sessionDirOf 校验规则：非空 + 不含 `/` + 不含 `..`\n",
    "102": "    // ── 3. 清理动作 ────────────────────────────────────────────\n",
    "108": "    // 3a. ttl/<spaceId>/ ——   session-init / hook 预热等热缓存\n",
    "118": "    // 3b. nottl/<spaceId>/ —— binding / skill 抽取 / kv version pin 等业务态\n",
    "128": "    // 3c. kernel-sts pool evict —— 回收该 space 的 STS backend\n",
    "138": "    // 3d. Redis 不清 —— 声明式，方便调用方知道我们没漏\n"
  },
  "MemoryProxy/src/routes/session-force-archive.ts": {
    "3": " * 手动强制归档当前 session 的 skill buffer（第三个触发条件）。\n",
    "5": " * 两种使用方式：\n",
    "6": " *   1. 函数调用（mem:create-skill 内部用）— import forceArchiveSkill()\n",
    "7": " *   2. HTTP 接口（面板前端用）— createSessionForceArchiveHandler() 注册路由\n",
    "35": "/** Core 接口返回类型 */\n",
    "47": " * 手动强制归档当前 session 的 skill buffer。\n",
    "49": " * 从 SessionStore 取 sessionInfo → 调 CoreSkillClient.forceArchive() → 返回结果\n",
    "54": "  // 参数校验\n",
    "59": "  // 从 SessionStore 取 session 状态\n",
    "70": "  // 调用 Core 接口\n",
    "110": " * 创建 HTTP handler，注册到 server.ts。\n"
  },
  "MemoryProxy/src/routes/session-refresh.ts": {
    "3": " * 核心逻辑：基于 session init 信息重新执行 prewarmFromConfig，刷新 COS 上的注入缓存。\n",
    "4": " * 除了 hook 侧的缓存，同时也会重新拉一次 Agent/Task 的 detail 覆写到 SessionStore，\n",
    "5": " * 让\"任务描述 / Agent 描述\"这类 session-init 时快照下来的字段也跟着刷新。\n",
    "7": " * 两种使用方式：\n",
    "8": " *   1. 函数调用（mem:sync 内部用）— import refreshSessionCache()\n",
    "9": " *   2. HTTP 接口（面板前端用）— createSessionRefreshHandler() 注册路由\n",
    "31": "  /** Hook 缓存刷新的 hookId 列表（skill/memory/knowledge/... injector 各自的 id）。 */\n",
    "34": "  /** 是否成功重拉了 agent detail（覆写到 SessionStore）。 */\n",
    "36": "  /** 是否成功重拉了 task detail（覆写到 SessionStore）。 */\n",
    "45": " * 重新拉一次 Agent + Task detail 并覆写到 SessionStore。\n",
    "47": " * 失败仅 warn 不抛：agent/task detail 拉不到属于\"降级\"场景，hook 缓存刷新仍然\n",
    "48": " * 应当继续。返回值告诉 caller 哪一项成功了以便文案里说明。\n",
    "60": "  // ServiceId (space) 取自 sessionInfo；调用方传入的 spaceId 作为兜底。\n",
    "62": "  // MetadataClient 需要 x-tdai-user-key，优先 sessionInfo，再退到调用方。\n",
    "66": "    // 缺少 kernel 调用要素 → 直接跳过 detail 刷新，不视为错误。\n",
    "139": " * 刷新当前 session 的全部注入缓存。\n",
    "141": " * 从 SessionStore 取 sessionInfo → 重拉 Agent/Task detail 覆写 store →\n",
    "142": " * 构建 PrewarmInput → 调用 prewarmFromConfig()\n",
    "147": "  // 参数校验\n",
    "156": "  // 从 SessionStore 取 session 状态\n",
    "179": "  // Step 1: 尝试重拉 agent/task detail 并覆写 SessionStore。\n",
    "180": "  //         失败不阻断 hook 缓存刷新，只是最终返回值里 agentRefreshed/taskRefreshed 为 false。\n",
    "185": "  // Step 2: 用最新 state 里的 agent/task detail 构造 PrewarmInput。\n",
    "204": "      // 刷新场景必须 clearBefore=true —— 首次 session_init 复用同一入口时不带\n",
    "205": "      // 这个选项,保留 \"cache miss 由 pipeline 走 execute() self-heal\" 的语义;\n",
    "206": "      // 刷新时开启后,任何 hook 拿到 []/超时/异常都会连带旧缓存一起清掉,防止\n",
    "207": "      // \"资产已解绑但注入还带着老快照\" 的场景(尤其 knowledge 的 wiki/code-graph\n",
    "208": "      // 全解绑 → prewarm 返回空 → 默认逻辑不写 → COS 上老 <knowledge_tools>\n",
    "209": "      // 无限续命)。详见 `injection/prewarm.ts` 的 PrewarmOptions.clearBefore 注释。\n",
    "235": " * 创建 HTTP handler，注册到 server.ts。\n",
    "236": " * 走 admin auth 鉴权（复用 admin-auth.ts 的模式）。\n"
  },
  "MemoryProxy/src/routes/session-task.ts": {
    "1": " * mem:create-task / mem:update-task 的核心业务：\n",
    "2": " * 从当前 session 上下文出发，生成 Task 并写入 metadata，绑定到 session。\n",
    "4": " * 交互模型（\"用户确认\"闸门 —— 破坏性操作前必须先 draft、后 confirm）：\n",
    "6": " *       未绑真实 task → LLM 生成 title+description → 直接落库+绑定（一步到位）\n",
    "7": " *       已绑真实 task → 生成新 draft → 写 pending → 返 pending，等用户 confirm/cancel\n",
    "9": " *       未绑 task_id → 拦截返错，引导用户先 mem:create-task\n",
    "10": " *       已绑：拉当前 task → 生成 draft（含 status 建议）→ 写 pending → 返 pending\n",
    "11": " *              非 creator 不再拒绝，改为把 warning 挂到 pending 上,confirm 时告知用户\n",
    "13": " *       从 pending-store 取出 payload,执行真正的创建 / 更新 / 覆盖绑定 / 清 pending\n",
    "15": " * 关键约束（kernel 对齐）：\n",
    "16": " *   - create 时传 agent_id,让 kernel 关联当前 Agent (TAPD 需求原文)\n",
    "17": " *   - 落库时**不改 title**（对齐既有产品约定,kernel 允许改但产品不改）\n",
    "18": " *   - update 时 status 由 LLM 提议,proxy 直接透传给 kernel,不做枚举校验\n",
    "19": " *   - creator_user_id / team_id 严格取 sessionInfo,防止越权\n",
    "20": " *   - taskDraft 未配置 = 直接返 \"config missing\"（不做本地兜底）\n",
    "40": "// ── 输入 / 输出类型 ────────────────────────────────────────────────────────\n",
    "47": "  /** 用户提示（mem:create-task 后的自由文本），可空 */\n",
    "49": "  /** 最近对话（用于生成草稿） */\n",
    "52": "   * 用户在 mem:create-task 后写明的 title（原文，40 字截断由调用方负责）。\n",
    "53": "   *   - 有值：title 锁定，LLM 只负责生成 description；LLM 失败降级为 desc 留空、task 照样落库。\n",
    "54": "   *   - 无值：title + description 都由 LLM 从对话推断；LLM 失败则直接返错。\n",
    "67": "   * 用户在 mem:update-task 后写明的 description（原文）。\n",
    "68": "   *   - 有值：description 直接替换为该文本，不调 LLM。\n",
    "69": "   *   - 无值：调 LLM diff 出新 description（changed=false 时不落库）。\n",
    "74": "/** confirm/cancel 入参（不带 recentMessages，因为 draft 已在 pending 里） */\n",
    "82": "/** create 命令首次调用时的 pending 摘要（给命令层拼预览文案用） */\n",
    "91": "/** update 命令首次调用时的 pending 摘要 */\n",
    "106": "  /** 成功时的 taskId */\n",
    "108": "  /** 成功时的最终 title */\n",
    "110": "  /** 成功时的最终 description */\n",
    "112": "  /** 成功时的最终 status */\n",
    "114": "  /** update 模式：LLM 判定为 \"无需更新\" 时 true —— 未落库 */\n",
    "117": "   * create 模式：session 已绑定 task_id，此前的兼容位；新交互下会同时给 pending。\n",
    "118": "   * 保留字段名以兼容 HTTP handler 的响应外壳（respond() 里读它决定 409）。\n",
    "122": "   * 首次调用生成了 pending 草稿等待用户确认。未落库；命令层需拼预览文案，\n",
    "123": "   * 引导下一轮回复 `mem:create-task confirm` / `mem:update-task confirm` 或 `... cancel`。\n",
    "126": "  /** confirm 分支：session 上没有对应 pending（可能超时/被取消/未生成过） */\n",
    "128": "  /** cancel 分支：是否命中并清理了 pending */\n",
    "130": "  /** create 覆盖分支：原来绑定的旧 task_id（用于返回文案） */\n",
    "132": "  /** 失败原因，供调用方拼用户可见文案 */\n",
    "136": "// ── 内部：取 session state 的通用检查 ─────────────────────────────────────\n",
    "168": "  // \"本次不关联任务\" 走 config.sessionInit.defaultTaskId（虚拟值，kernel 不存在），\n",
    "169": "  // 视为**未绑真实 task** —— 允许 create-task，禁止 update-task（后者会用 no-task 分支返错）。\n",
    "178": "// ── 内部：检查并取 taskDraft 配置 ─────────────────────────────────────────\n",
    "200": "/** 由 resolved session 生成 pending key（含 team/agent/session 三元组）。 */\n",
    "204": "    // pending-store 的 agent 用 sessionInfo.agent_id (kernel 侧概念),缺失就用 agentSource 兜底\n",
    "210": "// ── 内部：真正落库的两个原子操作（首次直落 / confirm 时也走这里） ────────\n",
    "213": " * 执行 create：调 kernel createTask，然后绑定 session。\n",
    "214": " * agent_id 会尝试从 sessionInfo 取；缺就不传（由 kernel 决定）。\n",
    "247": "// ── 核心：create-task ─────────────────────────────────────────────────────\n",
    "249": "// 交互分层：\n",
    "250": "//   未绑 + 任意参数 → LLM 生成 draft → 立即落库（不需要 confirm）\n",
    "251": "//   已绑真实 task → LLM 生成 draft → 写 pending，等 confirm/cancel\n",
    "262": "  // Step 1: LLM 生成草稿（lockedTitle 情况下只出 description）\n",
    "270": "  // Step 1a: 结果归一化\n",
    "272": "  // 三挡降级（2026-08-18 修复失败率过高问题）：\n",
    "273": "  //   1) draft.ok=true          → 用 LLM 结果\n",
    "275": "  //   3) draft.ok=false + 无参数 → \"未命名任务_yyMMdd_HHmm\" 兜底，desc=\"\"，让用户后续 mem:update-task 补\n",
    "277": "  // 之前第 3 挡是\"严格返错\"，导致 LLM 一波动就要用户重试 3 次——非常糟糕的 UX。\n",
    "278": "  // 现在给个默认名并绑上，用户拿到 taskId 后可以随时补描述；\n",
    "279": "  // 兜底错误信息通过 bindWarning 透传给上层，UI 层可以选择性提示\"AI 命名失败，已使用默认名\"。\n",
    "287": "    // 有参数 + LLM 失败 → 降级：title 用参数、desc 留空\n",
    "292": "    // 无参数 + LLM 失败 → 兜底：\"未命名任务_yyMMdd_HHmm\"\n",
    "298": "  // Step 2a：已绑真实 task → 写 pending，等 confirm 覆盖绑定\n",
    "306": "      // 拉不到旧 task title 也不阻塞（只影响预览显示）\n",
    "336": "  // Step 2b：未绑 → 直接落库\n",
    "340": "  // 合并两类 warning：AI 兜底 + 绑定失败，都通过 error 字段透传给上层\n",
    "341": "  // （字段名 error 是历史遗留；语义是\"success=true 但有 warning\"）\n",
    "356": " * 生成\"未命名任务_yyyyMMdd_HHmm\"作为 LLM 兜底 title。\n",
    "358": " * 时间戳精确到分钟，可读性 & 可搜性平衡：\n",
    "359": " *   - 用户看到\"未命名任务_260818_1710\"能马上定位是啥时候创的\n",
    "360": " *   - 不用秒，避免连续创建重名 —— 而且分钟内连开两个 task 是异常操作\n",
    "361": " * 保证 ≤ MAX_TITLE_LEN(40) 字。\n",
    "370": "  return `未命名任务_${yy}${MM}${dd}_${HH}${mm}`;\n",
    "373": "// ── 核心：update-task ─────────────────────────────────────────────────────\n",
    "375": "// 交互分层（每次都需要 confirm）：\n",
    "376": "//   未绑 → 拦截返错，让用户先 create-task\n",
    "377": "//   已绑 → 拉当前 task → 生成 draft（含 status 建议）→ 写 pending → 返 pending\n",
    "378": "//          非 creator 直接拒（kernel 也不支持跨用户 update，proxy 侧提前给出建议 create-task 的指引）\n",
    "390": "        \"no task bound to this session (session is in \\\"本次不关联任务\\\" mode or task_id missing); \" +\n",
    "395": "  // 无参数分支才需要 LLM 配置\n",
    "401": "  // Step 1: 拉当前 task\n",
    "413": "  // 非 creator 直接拒（kernel 侧仍以 owner-check 拒绝跨用户 update，proxy 侧提前阻断以给出清晰指引）\n",
    "421": "  // Step 2: 决定新 description + statusSuggestion\n",
    "425": "    // 有参数：直接替换，不调 LLM，也不出 status 建议\n",
    "443": "      // LLM 判无改动 → 不写 pending，直接返 noUpdateNeeded\n",
    "457": "  // Step 3: 写 pending，等 confirm\n",
    "490": "// ── 核心：confirm / cancel ────────────────────────────────────────────────\n",
    "493": " * 用户回复 `mem:create-task confirm` 或 `mem:update-task confirm` 时调用。\n",
    "494": " * 根据 pending 里的 kind 分派：\n",
    "495": " *   - create → doCreateAndBind（覆盖原绑定）\n",
    "496": " *   - update → kernel updateTask（description + status 透传）\n",
    "497": " * 落库成功后清 pending；失败时保留 pending（用户可重试）。\n",
    "520": "      // 落库失败保留 pending 让用户重试\n",
    "536": "  // update 分支\n",
    "561": " * 用户回复 `mem:create-task cancel` 或 `mem:update-task cancel` 时调用：\n",
    "562": " * 清 pending，不落库。返回 cancelled=true 让命令层拼提示文案。\n",
    "575": "// ── 内部：把 taskId 写回 sessionInfo ──────────────────────────────────────\n",
    "598": " * 面板前端 / e2e 用。body 形如：\n",
    "600": " * 若不带 recent_messages，会返 \"no recent messages\" —— 面板要主动传（从\n",
    "601": " * conversation/query 拉最近 N 条）。\n",
    "623": " * POST /v3/session/update-task —— 同上，但走 update 分支。\n",
    "644": "// ── HTTP body 解析 ────────────────────────────────────────────────────────\n"
  },
  "MemoryProxy/src/routes/whitelist.ts": {
    "1": " * 白名单端点表：集中管理 context-proxy 支持转发的 Anthropic/OpenAI 端点。\n",
    "3": " * 该表是路由、URL 拼接、handler 分派三处逻辑的**单一数据源**：\n",
    "4": " * - `server.ts` 依据表注册 Hono 路由（精确匹配放在 catch-all 之前）\n",
    "5": " * - `guard-adapter.ts:joinUrl` 依据表决定上游 endpoint suffix，而非硬编码二分支\n",
    "6": " * - `auxiliaryHandler.ts` 依据表决定是否透传、是否需要走 stream 分支\n",
    "8": " * 新增端点时，只需在 `WHITELIST_ENDPOINTS` 增加一条记录即可，无需散点修改。\n",
    "11": "/** 白名单端点元数据。 */\n",
    "14": "   * 用户请求 path 的后缀（剥离 `/proxy/{spaceId}` 前缀后精确匹配）。\n",
    "15": "   * 例：`/v1/messages/count_tokens`\n",
    "19": "   * 转发到 upstream 时拼接到 `upstream.url` 之后的 endpoint 部分。\n",
    "20": "   * 例：`/messages/count_tokens`（拼在 `https://tokenhub.../v1` 之后）\n",
    "24": "   * 协议类型：决定鉴权头格式（anthropic → `x-api-key`，openai → `Authorization: Bearer`）\n",
    "25": "   * 与 credit-reporter 的 usage 解析分支一致。\n",
    "28": "  /** 端点是否支持流式响应（SSE）。 */\n",
    "31": "   * 是否为主端点：主端点由现有的 `handleAnthropicMessages` / `handleChatCompletions`\n",
    "32": "   * 处理（含路由决策）；非主端点走轻量的 `handleAuxiliaryEndpoint`\n",
    "33": "   * （跳过路由，仅做鉴权 + 转发 + credit）。\n",
    "39": " * 当前支持的白名单端点列表。\n",
    "41": " * 顺序不重要——`matchWhitelistEndpoint` 内部会按 `pathSuffix` 长度**从长到短**排序，\n",
    "42": " * 以保证 `/v1/messages/count_tokens` 优先于 `/v1/messages` 命中。\n",
    "45": "  // ── 主端点（由现有 handler 处理，含路由）────────────────────────\n",
    "60": "  // ── 辅助端点（由 handleAuxiliaryEndpoint 处理，不走路由）─────────\n",
    "89": "  // ── Codex Responses API 端点（由 codexHandler 处理）──────────────\n",
    "90": "  // 主端点：见 codexHandler.ts；上游拼接靠这里防止 joinUrl 走 fallback\n",
    "91": "  // 兜底到 /chat/completions（错误协议）。\n",
    "93": "  // 两组条目对应 base_url 带 /v1 与不带 /v1 两种客户端写法，见 server.ts 的\n",
    "94": "  // codex 路由注释。matchWhitelistEndpoint 按 pathSuffix 长度降序匹配，\n",
    "95": "  // /v1/responses 会优先于 /responses 命中，保证语义清晰。\n",
    "154": "/** 按长度降序排列的缓存，避免每次匹配都重新排序。 */\n",
    "159": "/** `/proxy/{spaceId}` 前缀正则：仅剥离一层，避免误伤路径中的 \"proxy\" 字面量。 */\n",
    "162": " * Agent 前缀正则：匹配 `/{agent}[/{spaceId}]/{v1|responses|...}` 形态。\n",
    "163": " *   - `/claude-code/v1/messages`              → 剥 `/claude-code`\n",
    "164": " *   - `/claude-code/{spaceId}/v1/messages`    → 剥 `/claude-code/{spaceId}`\n",
    "165": " *   - `/codex/{spaceId}/responses`            → 剥 `/codex/{spaceId}`（codex 客户端\n",
    "166": " *     不像 CC/CB 那样自拼 /v1/，源码 endpoint 常量就是 /responses，因此 base_url\n",
    "167": " *     不带 /v1 时前缀后紧接的就是 /responses 或 /memories 等）\n",
    "168": " * lookahead 允许 `/v1/`、`/responses`、`/responses/`、`/memories/`、`/realtime/`\n",
    "169": " * 后紧邻，其中 `/v1/` 必须带尾斜杠避免误伤未来出现的 `/v1foo` 之类；responses\n",
    "170": " * 等 codex 端点允许尾斜杠可选（如 `/responses` 是完整路径）。\n",
    "171": " * 白名单入口 `/v1/messages`、`/responses` 自身不会被误剥（因为它们不匹配 agent\n",
    "172": " * 段——agent 段限定为已知名字）。\n",
    "177": " * `/cost-guard` marker 正则：位于 `/{agent}/{spaceId}` 之后的独立 segment。\n",
    "179": " * 语义（相对早期的 `/direct` marker 已反转）：**默认 passthrough**，\n",
    "180": " * 仅在请求路径显式带上 `/cost-guard` 段时才让 primary handler 走 cost-guard 路由。\n",
    "182": " * 命中条件（两者同时满足）：\n",
    "183": " *   1. lookahead `(?=/)`——marker 是一个独立 segment（后面还有内容），\n",
    "184": " *      不限定紧接 `/v1/` 还是裸尾巴。这样 marker 与客户端拼的尾巴解耦：\n",
    "185": " *        - `/codebuddy/{spaceId}/cost-guard/chat/completions`（CB 裸尾）\n",
    "186": " *        - `/claude-code/{spaceId}/cost-guard/v1/messages`（CC 带 /v1）\n",
    "187": " *      两种都识别为 marker。词干 `/cost-guarded/` `/cost-guarding/`\n",
    "188": " *      `/pre-cost-guard/` 因 lookahead 要求 `/cost-guard` 后紧邻 `/` 而被隔断。\n",
    "189": " *   2. lookbehind `(?<=(?:/[^/]+){2,})`——marker 前必须有 ≥ 2 段非空 segment，\n",
    "190": " *      使 marker 只在 `/{agent}/{spaceId}/cost-guard/...` 或\n",
    "191": " *      `/proxy/{spaceId}/cost-guard/...` 的结构下命中；spaceId 恰好\n",
    "192": " *      叫 \"cost-guard\"（三段结构 `/agent/cost-guard/...`）不会误触发。\n",
    "194": " * 见 `hasCostGuardMarker` 让 primary handler 判定是否**启用** router；\n",
    "195": " * `normalizeWhitelistRequestPath` 同步剥离它以保证白名单匹配继续工作。\n",
    "200": " * `/analyse` marker：结构完全对齐 `/cost-guard`——位于 `/{agent}/{spaceId}` 之后\n",
    "201": " * 的独立 segment，命中即表示\"本次请求要走内部资产反思模式\"。\n",
    "203": " * 由 `injection.assetReflection.markerOptIn` 门控，\n",
    "204": " * 见 `AssetReflectionInjector`。跟 cost-guard 不同，`/analyse` **不注册**\n",
    "205": " * 专门的 hono 路由——它是完全透明的标记：正常业务路径继续处理，\n",
    "206": " * 只是 injector 检测到 marker 后往 system prompt 末尾多贴一段反思提示。\n",
    "208": " * `normalizeWhitelistRequestPath` 剥掉这个 marker，保证白名单后缀匹配继续工作。\n",
    "213": " * 请求路径是否携带 `/cost-guard` marker（位于 `/v1/` 之前的独立 segment）。\n",
    "214": " * 携带时 primary handler 走完整的 cost-guard 路由；不带时（默认）直接透传到默认上游。\n",
    "223": " * 请求路径是否携带 `/analyse` marker（结构同 `/cost-guard`）。\n",
    "224": " * 命中时 `AssetReflectionInjector` 会在系统提示词末尾追加 `<asset_reflection>` 块。\n",
    "233": " * 规范化请求路径以便白名单匹配。\n",
    "235": " * 1. 剥离 query string\n",
    "236": " * 2. 剥离 `/cost-guard` marker（如有，见 `hasCostGuardMarker`）\n",
    "237": " * 3. 剥离 `/analyse` marker（如有，见 `hasAnalyseMarker`）\n",
    "238": " * 4. 剥离 `/proxy/{spaceId}` 前缀（如有）\n",
    "239": " * 5. 剥离 `/{agent}/{spaceId}` 前缀（如 `/claude-code/{spaceId}/v1/messages`）\n",
    "255": " * 从请求路径匹配白名单条目。\n",
    "257": " * 匹配规则：\n",
    "258": " * 1. `normalizeWhitelistRequestPath` 规范化路径（剥离 query / proxy 前缀 / agent+spaceId 前缀）\n",
    "259": " * 2. 按 `pathSuffix` 长度**从长到短**尝试精确后缀匹配\n",
    "261": " * @returns 命中的白名单条目，未命中返回 `null`\n"
  },
  "MemoryProxy/src/server.ts": {
    "29": "  // `/cost-guard` marker 门控 (P0 前置)：\n",
    "30": "  // markerOptIn=false 时 marker 完全作废——任何路径里带 `/cost-guard/` 段的请求\n",
    "31": "  // 都直接 404，避免 catch-all `POST /*` 把它兜住走成默认路由（会产生迷惑：\n",
    "32": "  // 客户端以为自己\"启用了 marker\"，proxy 却按 default_passthrough 处理）。\n",
    "33": "  // 放在最前面，早于所有业务路由。\n",
    "51": "  // `/analyse` marker 门控（结构完全对齐 cost-guard）：\n",
    "52": "  // assetReflection.markerOptIn=false 时任何带 `/analyse/` 段的请求都 404，\n",
    "53": "  // 避免 catch-all 静默兜住把 marker 请求当默认路径处理。此前只有 primary\n",
    "54": "  // 路由的注册开关，缺这个顶部拒绝——CC/CB 侧的 `/analyse` marker 请求\n",
    "55": "  // 在 markerOptIn=false 时会 fall through 到 catch-all `POST /*` 让上游\n",
    "56": "  // 收到裸 body（marker 客户端以为\"启用\"了实际没走 injector）；codex 侧\n",
    "57": "  // 同样症状（P1-6 报告里除 `/cost-guard` 之外的 `/analyse` 变体）。一并\n",
    "58": "  // 修掉，保持两 marker 门控行为对称。\n",
    "79": "  // 多节点场景：storage 请求 cos 但降级到进程内 (fs / memory / sqlite) 时\n",
    "80": "  // 返回 503 + degraded=true，让 k8s LB 把该 pod 摘掉，避免\"两个节点各写各\n",
    "81": "  // 的内存\"这种数据一致性事故。sqlite 也算 process-local——多节点各自本地\n",
    "82": "  // 文件也是不共享的。见 docs/design/2026-07-13-proxy-multinode-state-audit.md P0-2。\n",
    "128": "  // Memory bridge: 同样模式但反代 tdai L0/L1/L2/L3 只读接口。\n",
    "129": "  // 让 LLM 用 Bash 调 <proxy>/memory-bridge/v3/atomic/search 等，proxy 注入身份。\n",
    "133": "  // ── Ops endpoint（在 catch-all `POST /*` 之前注册） ───────────────────────\n",
    "134": "  // /v3/instance/proxy-destroy — shark 销毁实例时清理 proxy 侧 COS 缓存 +\n",
    "135": "  // kernel-sts pool。契约字段跟 core `/v3/instance/destroy` 对齐，路径用\n",
    "136": "  // `proxy-destroy` 动作与 core 区分。鉴权走 config.admin.apiKey（空则公开）。\n",
    "145": "  // ── Session management endpoints (mem: command 底层接口, 面板前端可复用) ──\n",
    "162": "  // 这些端点走轻量透传 handler（不进入路由模块，不构成对话回合）。\n",
    "163": "  // 详见 docs/design/2026-07-02-arbitrary-path-passthrough-design.md\n",
    "169": "  // Agent-prefixed routes with spaceId — 客户端标准配置格式：\n",
    "172": "  // 路径示例: /claude-code/mem-example001/v1/messages\n",
    "174": "  // `/cost-guard` marker: primary handler 检测到该段后启用 cost-guard 路由；\n",
    "175": "  // 默认路径（不带 marker）则跳过 router 直接透传上游。\n",
    "177": "  // marker 机制受 `config.costGuard.markerOptIn` 门控：\n",
    "178": "  //   - false（默认/线上）: 不注册这两条路由——所有请求走 `/:agent/:spaceId/v1/...`，\n",
    "179": "  //     handler 内 useGuard 恒 true，行为等同于历史\"默认走 router\"。此时任何\n",
    "180": "  //     `/cost-guard/...` 请求都命中不到路由，走到最终 catch-all 或返 404\n",
    "181": "  //     （catch-all `/*` 存在，会 fallthrough 到 handleChatCompletions；下面在\n",
    "182": "  //     顶部加了 marker→404 拒绝，见 handler / anthropicHandler）。\n",
    "183": "  //   - true（测试环境）: 注册以下两条 marker 路由；handler 内根据 marker 决定\n",
    "184": "  //     是否走 router。\n",
    "185": "  // 详见 `hasCostGuardMarker`。\n",
    "186": "  // Hono 优先匹配更精确的路径，需注册在通用 `/:agent/:spaceId/v1/...` 之前。\n",
    "192": "  // `/analyse` marker (asset-reflection 内部效果评估) —— 跟 cost-guard 完全对称：\n",
    "193": "  // 由 `injection.assetReflection.markerOptIn` 门控。marker 只是一个透明标记，\n",
    "194": "  // handler 内 (AssetReflectionInjector) 检测到即在 system prompt 末尾追加\n",
    "195": "  // <asset_reflection>；不带 marker 的请求走原有正常路由。\n",
    "197": "  // ⚠️ 关键：不注册这两条路由时，`/{agent}/{spaceId}/analyse/v1/messages`\n",
    "198": "  // (5 段) 会 fall through 到最下面的 catch-all `POST /*` → handleChatCompletions\n",
    "199": "  // (OpenAI handler)，把 Anthropic body 打到 OpenAI 端点 → 上游 400。所以只要\n",
    "200": "  // markerOptIn=true 就必须显式注册这两条 anthropic/openai 5 段路由。\n",
    "207": "  // Codex CLI 客户端走 OpenAI Responses API，第三条独立协议路径。\n",
    "209": "  // 客户端行为差异：codex-rs core/src/client.rs 里 endpoint 常量是 `/responses`\n",
    "210": "  // （不带 /v1），而 CC/CB 客户端的常量是 `/v1/messages` `/v1/chat/completions`\n",
    "211": "  // （带 /v1）。用户 base_url 惯例：\n",
    "212": "  //   - CC/CB：base 不带 /v1，客户端自动拼 /v1/messages 等\n",
    "213": "  //   - Codex：base 需要自带 /v1，客户端拼 /responses\n",
    "215": "  // 为了让三个 agent 的接入方式对用户统一（base 都填 `/<agent>/<spaceId>`\n",
    "216": "  // 不带 /v1），proxy 同时接受**带 v1** 和**不带 v1** 两种 path：\n",
    "217": "  //   /codex/<spaceId>/v1/responses  ← codex 客户端 base 自带 v1 时命中\n",
    "218": "  //   /codex/<spaceId>/responses     ← codex 客户端 base 不带 v1 时命中（对齐 CC/CB 用法）\n",
    "219": "  // 两条路径全部映射到 handleCodexEndpoint，行为完全一致。\n",
    "224": "  // 兼容 base_url 不带 /v1 的写法（对齐 CC/CB 的用户体验）\n",
    "231": "  // WorkBuddy CLI/Desktop 客户端走 OpenAI Responses API（与 Codex 同协议），\n",
    "232": "  // 但客户端行为与 codex-cli 有差异（sub-path 更多：compact / trace_summarize\n",
    "233": "  // / realtime / memories属于 aux；主 endpoint 是 /v1/responses）。\n",
    "235": "  // 与 CC/CB/Codex 一样支持 base_url 带/不带 /v1 两种写法。\n",
    "240": "  // 兼容 base_url 不带 /v1 的写法\n",
    "246": "  // Codex 侧的 `/cost-guard` / `/analyse` marker 路由 —— 完全对齐 CC/CB：\n",
    "247": "  // marker 是 URL 上的独立段（位于 `/{agent}/{spaceId}` 之后），门控开关相同。\n",
    "248": "  // 不注册这些路由时，`/codex/{spaceId}/cost-guard/responses` 5 段路径既不\n",
    "249": "  // 匹配 codex 上面 8 条精确路由，也不匹配任何 agent-prefixed 路由，会 fall\n",
    "250": "  // through 到 catch-all `POST /*` → handleChatCompletions，把 Responses API\n",
    "251": "  // body 打到 OpenAI /chat/completions 上游返 200 chatcmpl-* 让客户端崩。\n",
    "253": "  // `/cost-guard` 语义：primary handler 检测到该段后启用 cost-guard 路由；\n",
    "254": "  //   codex 侧的 handler 需要在 forwardToUpstream 时读 hasCostGuardMarker\n",
    "255": "  //   决定是否走 router。（当前 codexHandler.ts 尚未接 resolveForwardTarget，\n",
    "256": "  //   见 P1-6 报告；本 commit 只解决路由注册与 fall-through 静默 200 的\n",
    "257": "  //   问题，让请求先到达 codexHandler。cost-guard router 分流的实际支持\n",
    "258": "  //   独立 commit 处理。）\n",
    "260": "  // `/analyse` 语义：AssetReflectionInjector 检测到该段后往 system prompt\n",
    "261": "  //   末尾追加 `<asset_reflection>` 反思块；codexHandler.ts 已将 requestPath\n",
    "262": "  //   传给注入 pipeline（见 codexHandler.ts injection 段落中 `requestPath:\n",
    "263": "  //   c.req.path`），路由一注册 `/analyse` marker 立即对 codex 生效。\n",
    "274": "  // dsh 是 DeepSeek 官方 agent harness,协议 = 标准 OpenAI Chat Completions\n",
    "275": "  // (POST /chat/completions + SSE),body/messages shape 100% 兼容 CB 现有 handler。\n",
    "277": "  // 客户端行为差异(抓包 fixtures/*.req.json 实证,\n",
    "278": "  // 见 docs/dsh-recon/2026-08-14-dsh-capture-analysis.md):\n",
    "279": "  //   - dsh 源码 endpoint 常量: `${baseURL}/chat/completions`(**不带 /v1**,\n",
    "280": "  //     见 packages/llm/llm-deepseek/src/adapter.ts:301)\n",
    "281": "  //   - 建议用户配置: OPENAI_BASE_URL=http://<proxy>:8096/dsh/<spaceId>\n",
    "282": "  //   - 与 CC/CB/Codex/Workbuddy 惯例对齐,同时接受**带 v1** 和**不带 v1**两种路径,\n",
    "283": "  //     用户可选把 baseURL 配成 `.../dsh/<spaceId>` 或 `.../dsh/<spaceId>/v1`\n",
    "285": "  // main / title / compaction 三类请求识别不在路由层做,由 agent-adapters/dsh.ts\n",
    "286": "  // 的 classifyRequest 按 header + body 特征判(见其 doc)。\n",
    "289": "  // dsh 目前抓包未见 embeddings/moderations/completions,预留 aux 端点(与 CC/CB 对称)\n",
    "294": "  // dsh cost-guard / analyse marker 路由 —— 与 CC/CB/Codex 完全对称。\n",
    "295": "  // 参照 codex 注释:必须显式注册这四条 5 段路径,否则会 fall through 到 catch-all\n",
    "296": "  // POST /*(默认路径),marker 静默失效。\n",
    "306": "  // opencode cost-guard / analyse marker 路由 —— 与 CC/CB/Codex/dsh 完全对称。\n",
    "307": "  // opencode 客户端走标准 OpenAI Chat Completions（POST /v1/chat/completions），\n",
    "308": "  // 路径形态与 CB/dsh 同族；因此 marker 段的路由形态与 dsh 一致：\n",
    "310": "  //   /opencode/{spaceId}/cost-guard/chat/completions（客户端 base 不带 /v1 时）\n",
    "311": "  // 未显式注册这些 5 段路径时，会 fall through 到 catch-all POST /*，marker 静默失效。\n",
    "312": "  // Router 分流已经在 handler.ts 里通过 agentName=agentFromPath(\"opencode\") 传给\n",
    "313": "  // resolveForwardTarget，只要路由能命中，Router 就能按 agentSource=opencode 分支决策。\n",
    "335": "  // 保留以兼容不带 agent 前缀的客户端。\n"
  },
  "MemoryProxy/src/session/claude-code/cleaner.ts": {
    "3": " * 曾经这个文件里有 `stripInitArtifacts` 用来在 session_init 完成后剥离\n",
    "4": " * 假表单对话（避免 LLM 看到 form 交互）。**该功能已删除**——现在永远保留\n",
    "5": " * 用户的所有真实对话（含 session_init form 交互），不做任何删除。\n",
    "7": " * 目前只剩一个 export: `getLastUserMessageText`，用于在 session_init\n",
    "8": " * state machine 里读最后一条 user / tool 消息的文本以解析用户选择。\n",
    "30": " * 优先扫最近一条 role=tool 且含 form answer 关键词 (AskUserQuestion /\n",
    "31": " * multi_question_result / 表单标题) 的消息 —— 这是 Claude Code 上报用户\n",
    "32": " * 选择结果时的 tool_result 载体; fallback 到最近一条 user 消息。\n"
  },
  "MemoryProxy/src/session/claude-code/extractor.ts": {
    "3": " * 解析用户从 `AskUserQuestion` form 的回复。\n",
    "4": " * Claude Code 用户选择在 `role: \"tool\"` 消息中，格式为 JSON tool_result。\n",
    "6": " * 支持的回复格式：\n",
    "9": " *   3. 纯文本 label（用户选择了某个选项后 CLI 返回的字符串）\n",
    "11": " * 不含任何 CodeBuddy XML 解析逻辑。\n",
    "19": "const SKIP_RE = /跳过|不关联|skip|do not associate/i;\n",
    "24": " * 从用户答复中提取 asset_confirm 选择。\n",
    "25": " * 返回 true=是（关联资产），false=否（bypass），null=未识别。\n",
    "27": " * 格式兼容：\n",
    "28": " *   1. 精准选项: \"Yes, associate team assets\" / \"No, do not associate this time\" / \"是，关联团队资产\" / \"否，本次不关联\"\n",
    "29": " *   2. Q&A 格式: \"Your questions have been answered: \\\"Q?\\\"=\\\"A\\\".\"\n",
    "30": " *   3. \"Chat about this\" / 自由文本 → 降级返回 null（bypass）\n",
    "36": "  // 先检查是否是拒绝/跳过/自由文本（Chat about this / declined / rejected）\n",
    "37": "  // 增加中文\"非回答\"模式：用户可能直接输入了与问句无关的内容\n",
    "42": "  // Claude Code 返回格式可能包含完整问答上下文，如:\n",
    "43": "  // \"Your questions have been answered: \\\"问题？\\\"=\\\"答案\\\".\"\n",
    "44": "  // 提取最后一个 = 后面引号中的内容作为实际答案\n",
    "51": "  // 安全阀：如果提取后超过 80 字符，大概率不是纯用户答案而是包含 Q&A 全文\n",
    "52": "  // 此时拒绝走 loose regex，避免把问题中的\"是否要关联\"误判为\"是\"\n",
    "53": "  // （只有精准匹配 ASSET_CONFIRM_YES / ASSET_CONFIRM_NO 可以通过）\n",
    "56": "  // 精准匹配：完整选项文本（英文或中文）\n",
    "57": "  if (answerOnly.includes(ASSET_CONFIRM_YES) || answerOnly.includes(\"是，关联团队资产\")) {\n",
    "60": "  if (answerOnly.includes(ASSET_CONFIRM_NO) || answerOnly.includes(\"否，本次不关联\")) {\n",
    "65": "    // 宽松\"是\"匹配：必须以\"是\"、\"确认\"、\"yes\"或\"y\"开头\n",
    "66": "    if (/^(?:是|确认|yes|y)(?:[，,\\s]|$)/i.test(answerOnly.trim())) {\n",
    "69": "    // 宽松\"否\"匹配\n",
    "70": "    if (/^(?:否|不[，,\\s]|跳过|skip|no|n)(?:[，,\\s]|$)/i.test(answerOnly.trim())) {\n",
    "78": "// ── JSON 解析 helpers ──────────────────────────────────────────────────────────\n",
    "81": " * 从 Claude Code tool_result JSON 中提取答案文本。\n",
    "82": " * 支持格式：\n",
    "83": " *   - `{ answers: { \"q\": \"label\" } }` (AskUserQuestion 标准)\n",
    "85": " *   - 纯文本字符串（用户自由输入）\n",
    "120": "    // Not JSON — Claude Code tool_result 常常是拼串格式：\n",
    "122": "    // 只取 =\"...\" 里的 answer；这样问题文案里出现的 \"跳过 / 可跳过\"\n",
    "123": "    // 等词不会污染下游 SKIP_RE 匹配（回归 session1.json Bug）。\n",
    "131": " * 从 JSON 中提取 agent 和 task 答案（轮2 多 question 场景）。\n",
    "150": "          // CC form 轮2 只有 1 个 question (agent)，第一个非空答案就是 agent\n",
    "179": "    // Not JSON — 兼容 Claude Code 拼串格式 `…\"question\"=\"answer\".`\n",
    "188": "// ── Team 匹配 ──────────────────────────────────────────────────────────────────\n",
    "191": " * 轮1 提取：从用户答复中识别选定的 team_id。\n",
    "192": " * Claude Code: 用户选择在 `role: \"tool\"` 消息中，走 JSON 解析。\n",
    "200": "  // 先检查是否是拒绝/跳过（Chat about this / declined）\n",
    "207": "  // 检测\"本次不关联\"→ bypass\n",
    "212": "  // 匹配策略\n",
    "240": "// ── Agent / Task 匹配 ─────────────────────────────────────────────────────────\n",
    "296": " * task_select 阶段提取：只解析 answers 里的 label 而非 tool_result 全文。\n",
    "297": " * 这里不再复用 extractFromOptionText 的旧路径 —— 那条路径把 tool_result 原文\n",
    "298": " * 当 fallback 传给 matchTaskInTeam，会因问题文案（\"…（可跳过）：\"）里包含\n",
    "299": " * \"跳过\"而把用户明确选中的 task 误判成 bypass（历史 Bug）。\n",
    "301": " * 返回值：\n",
    "302": " *   - task_id string：命中；\n",
    "303": " *   - MORE_MARKER：用户点了 \"更多 →\"，调用方翻页；\n",
    "304": " *   - BYPASS_MARKER：declined / 空答复 / 兼容旧表单的显式跳过；\n",
    "305": " *   - null：identify 得到答案但匹配不到 team.tasks（调用方按未识别 → bypass 处理）。\n",
    "307": " * defaultTaskId 兜底关联通过 fetchTeamsAndAgents 头部注入实现 —— 用户选\"本次\n",
    "308": " * 不关联任务\" label 时走 matchTaskInTeam 命中虚拟条目、返回 defaultTaskId，无\n",
    "309": " * 需在此单独分支。\n",
    "315": "  // declined / rejected → bypass（与其它阶段一致）\n",
    "324": "  // 翻页\n",
    "327": "  // 兼容旧表单：用户手打 \"跳过 / skip / 不关联\" → 显式 bypass。注意：defaultTaskId\n",
    "328": "  // 虚拟条目的 label 是\"本次不关联任务\"，SKIP_RE 会命中，所以先尝试正常匹配\n",
    "329": "  // 再走 bypass。\n",
    "341": " * 轮2 提取：从用户答复中识别 agent + task。\n",
    "342": " * Claude Code: 走 JSON 解析。仅在已选定的 team 内匹配。\n",
    "349": "  // 先检查是否是拒绝/跳过（Chat about this / declined）\n",
    "363": "  // 检测 \"更多 →\" → 翻页\n",
    "368": "  // 检测 \"本次不关联\" → bypass\n"
  },
  "MemoryProxy/src/session/claude-code/form.ts": {
    "3": " * Claude Code 原生交互 form：\n",
    "5": " *   - Options: `{ label, description }` 结构体，2-4 个硬限制\n",
    "6": " *   - Protocol: 仅 Anthropic SSE\n",
    "8": " *   - 分页: 每页 3 个 agent + 1 个\"更多→\"/SKIP 槽位\n",
    "10": " * 不含任何 CodeBuddy 逻辑。\n",
    "33": " * 附在每步 question 文末的通用备注：告诉用户\"选择跳过 = 本次 session init 跳过、不注入任何团队资产\"。\n",
    "34": " * Claude Code 的 AskUserQuestion 会给用户一个 \"Other\" 输入框，回复\"跳过 / skip /\n",
    "35": " * 不关联\" 就走 SKIP_RE bypass；没识别到的自由文本会 unrecognized → 同样 bypass。\n",
    "36": " * 文案与 workbuddy/codex/codebuddy/dsh 五端统一，避免多客户端表述漂移。\n",
    "40": "// 分页布局统一走 pagination.ts；此处仅用其常量。\n",
    "66": "  /** Claude Code 分页：当前 agent 页码 (0-based) */\n",
    "101": "    // Team options: 只列真实 team。主动\"跳过\"入口只在 asset_confirm 阶段，后续\n",
    "102": "    // 阶段\"异常/未识别\"由 init.ts 兜底 bypass。\n",
    "104": "    // 调用方（init.ts）保证 teams.length ≥ 2 — 单 team 会被 auto-select 跳过，\n",
    "105": "    // 根本不会走到 team form。form builder 不再兜底占位。\n",
    "106": "    // description 留空 —— label 已含 team 名 + id 后缀，重复一遍 \"Team: name\"\n",
    "107": "    // 只是噪音。\n",
    "108": "    // Team 阶段目前不分页 —— 最多渲染 CC_MAX_OPTIONS 个 team（超过的静默截断，\n",
    "109": "    // 属于 pre-existing 限制，本次未处理）。\n",
    "121": "      question: titlePrefix + \"请选择本次会话所属的 Team：\" + SKIP_HINT,\n",
    "138": "    // 只保留 agent 自身描述（有信息量），删掉 \"(选完 agent 后可选 N 个任务)\" /\n",
    "139": "    // \"(无任务)\" 的尾巴 —— 用户此时正在选 agent，任务数量提示既不影响决策\n",
    "140": "    // 也占屏。agent 若无自定义描述则 description 留空，不再回退到 \"Agent: 名\"\n",
    "141": "    // （label 已经有名字）。\n",
    "149": "      combinedOptions.push({ label: MORE_LABEL, description: `查看下一批（还剩 ${remaining} 个 Agent）` });\n",
    "151": "    // 末页不再追加 SKIP：主动跳过只在 asset_confirm 提供；后续阶段\"异常/未识别\"\n",
    "152": "    // 由 init.ts 兜底 bypass。\n",
    "154": "    // pagination.ts 保证每页真实项数 ≥ 2（total > 4 时；total ≤ 4 时单页含全部\n",
    "155": "    // 4 个 slot 铺满，无 MORE）；此处不该再收到 <2 的 combinedOptions。\n",
    "163": "    const pageSuffix = page.totalPages > 1 ? `（第 ${pageIndex + 1}/${page.totalPages} 页）` : \"\";\n",
    "165": "      question: titlePrefix + `请选择「${team.team_name}」下要使用的 Agent${pageSuffix}：` + SKIP_HINT,\n",
    "181": "    // description 留空 —— label 已含 task 名 + id 后缀，\"Task: name\" 只是噪音。\n",
    "182": "    // 虚拟兜底条目（isDefault）不拼 id 后缀，反正只有一个不会重名歧义。\n",
    "194": "        description: `查看下一批（还剩 ${remaining} 个任务）`,\n",
    "198": "    // 同 agent 阶段：pagination.ts 保证 count ≥ 2，此处 <2 说明分页器有 bug。\n",
    "206": "    const taskPageSuffix = page.totalPages > 1 ? `（第 ${taskPageIndex + 1}/${page.totalPages} 页）` : \"\";\n",
    "208": "      question: titlePrefix + `请选择「${team.team_name}」下要关联的任务${taskPageSuffix}：` + SKIP_HINT,\n"
  },
  "MemoryProxy/src/session/claude-code/index.ts": {
    "3": " * 独立的 Claude Code session-init 实现：\n",
    "4": " *   - Form: `AskUserQuestion` tool_use (仅 Anthropic SSE)\n",
    "5": " *   - Extractor: JSON tool_result 解析\n",
    "6": " *   - Cleaner: tool_use id 匹配\n",
    "7": " *   - 分页模式: 每页 3 个 agent + \"更多→\" 按钮\n"
  },
  "MemoryProxy/src/session/claude-code/init.ts": {
    "1": " * Claude Code Session Initialization — 状态机入口.\n",
    "4": " *   1. uninitialized → 内核拉 teams[], 发 `AskUserQuestion` form\n",
    "5": " *   2. pending_team_select → 解析 JSON tool_result team 选择, 发 agent_select form\n",
    "6": " *   3. pending_agent_select → 解析 agent (支持分页), 发 task_select form\n",
    "7": " *   4. pending_task_select → 解析 task (支持分页), fetch 详情, register, inject\n",
    "8": " *   5. initialized → 每次请求 strip + inject\n",
    "72": "  /** 本次注册是 session-reset 触发的（pre-hook 设 resetFlow=true → 保留到 completeRegistration）。 */\n",
    "129": "      // 源头注入：defaultTaskId 配置了就作为\"本次不关联任务\"虚拟条目排在真\n",
    "130": "      // task 之前。下游 form/extractor/init 一个字节都不用改 —— 分页 total\n",
    "131": "      // 和 auto-select 级联唯一真相都是 tasks.length。用户选中虚拟条目 →\n",
    "132": "      // completeRegistration 用 defaultTaskId 上报 → getTask 会 404 但\n",
    "133": "      // Promise.allSettled 兜掉，taskDetail=null → 不注入 [Task]，正好是\n",
    "134": "      // \"跳过 task 但保留 agent 关联\"的语义。\n",
    "169": " * 历史背景：旧分页策略（`3 每页 + MORE`）在 total mod 3 == 1（4、7、10…）时\n",
    "170": " * 末页会剩 1 个，用户点 MORE 后被这个函数直接 auto-select，体验很怪。现在\n",
    "171": " * pagination.ts 已经把 total ≤ 4 改为单页展示、total > 4 末页 solo 时把倒\n",
    "172": " * 数第二页匀 1 个过来，正常路径下不会再触发本函数的 solo 分支。\n",
    "174": " * 保留原因：防御性兜底 —— pagination.ts 若将来改回旧策略或有 bug，本函数\n",
    "175": " * 仍能防止渲染 1-option form；另外 total === 1 的首末页依然会命中（那个场\n",
    "176": " * 景 init.ts 上游其实已经 advanceFromAgentPicked auto-select 掉了，但双保\n",
    "177": " * 险无害）。\n",
    "294": "  // 0 tasks → bypass (统一契约：team+agent+task 缺一不注入)。\n",
    "295": "  //   历史行为是\"注册但 task_id=undefined，只注入 [Agent] 段\"，现改为完全 bypass。\n",
    "296": "  // 1 task → auto-select，直接推进到 completeRegistration。\n",
    "392": "  // 曾经这里会按 config.keepInitArtifacts 决定要不要 stripInitArtifacts,\n",
    "393": "  // 现在**永远保留** session_init form 交互, 不做任何删除。\n",
    "449": "  // narrowing to a task. The interactive \"本次不关联任务\" / defaultTaskId path\n",
    "465": "    // 当 task_id 是 defaultTaskId（虚拟值）时，跳过 getTask 调用——\n",
    "466": "    // 内核不存在该 task，调用只会 404 产生无意义的 warn。\n",
    "495": "  // Fire-and-forget: 记录一条 (team, task, agent, user) 参与日志，供看板\"实际参与\"\n",
    "496": "  // 分区展示。bypass 路径已在上方 return，走不到这里；debug forceIdentity 路径也\n",
    "497": "  // 走 append —— 用于本地 / e2e 联调验证。失败仅 warn，不阻断 session 注入路径。\n",
    "529": "    // 保留 resetFlow/resetEpoch 以供 handler 侧 prewarm 判断是否 clearBefore\n",
    "551": " * 顶层入口 wrapper：装饰 handleSessionInitInner，在完成后发一条埋点\n",
    "552": " * （仅当 prev !== initialized && after === initialized 时）。\n",
    "554": " * 埋点装饰绝不改动状态机；失败/异常静默，业务链路零感知。\n",
    "555": " * 详见 docs/design/2026-08-03-internal-usage-telemetry-plan.md §7.2。\n",
    "577": "    // 无论正常/异常返回都尝试发一次埋点；装饰器内部自吞异常。\n",
    "603": "  // 曾经这里会按 config.keepInitArtifacts 决定要不要 stripInitArtifacts,\n",
    "604": "  // 现在**永远保留** session_init form 交互, 不做任何删除。变量名 stripped\n",
    "605": "  // 保留只为下游调用点不用大改, 语义上就是 messages 本身。\n",
    "667": "  // ── Case 1: Uninitialized → 先弹 asset_confirm 对话框 ───────────────────\n",
    "830": "      // 保留 resetFlow/resetEpoch: pre-hook 写入的标记必须贯穿整个 form 流程,\n",
    "831": "      // 让 completeRegistration 最终返回 resetFlow=true → handler 触发确认响应。\n",
    "854": "      // bypass: 用户明确选择\"不关联\" —— 保留 form 对话原样，不删。\n",
    "877": "      // Auto-select cascade: 只有 1 个 team 时不弹 team form，直接推进到\n",
    "878": "      // agent 阶段（agent 内部再判断是否 auto-select）。这样\"1 team + 1 agent\n",
    "879": "      // + 1 task\"的最小配置在 asset_confirm=是 之后不再弹任何额外表单。\n",
    "891": "      // ≥2 teams → 弹 team_select 表单。\n",
    "914": "    // 未识别 → bypass (保留 form 对话原样)\n",
    "941": "    // bypass: 保留 form 对话原样, 不删。\n",
    "962": "      // 从 pagination.ts 拿 totalPages 判越界，与 form.ts 的切片同一算法。\n",
    "966": "      // 防御性：pagination.ts 已保证正常路径不会出现 solo 末页，但双保险\n",
    "967": "      // 兜住 —— 万一分页器改动出 bug，用户仍不会被 auto-select 惊到（会走\n",
    "968": "      // 到这里 auto-select，从旧行为角度也是可接受的降级）。\n",
    "1029": "    // bypass: 保留 form 对话原样, 不删。\n",
    "1041": "    // 新版 UI 没有 \"跳过\" 按钮：extractor 从 answers JSON 里取 label，\n",
    "1042": "    // 不再把 tool_result 全文当 answer（旧路径会因问题文案里的 \"（可跳过）\"\n",
    "1043": "    // 触发 answer.includes(\"跳过\") 假阳性 → 用户明确选的 task 被吞掉）。\n",
    "1052": "      // 防御性：见 agent MORE 分支的同名注释。正常路径 pagination.ts 已避免\n",
    "1053": "      // solo 末页；此处保留双保险。\n",
    "1081": "    // BYPASS_MARKER = declined / 兼容旧表单显式跳过；null = 有答复但匹配不到任何 task\n",
    "1082": "    // 两者都视作 \"未识别\"，与其它阶段的行为对齐 → bypass 整个 session-init。\n",
    "1085": "      // bypass: 保留 form 对话原样, 不删。\n",
    "1094": "    // 命中 task_id\n"
  },
  "MemoryProxy/src/session/claude-code/pagination.ts": {
    "1": " * Claude Code AskUserQuestion 分页布局器。\n",
    "3": " * 背景：CC AskUserQuestion 单 question 最多 4 个 option。中间页需要留 1 个\n",
    "4": " * slot 给\"更多 →\"，所以最多塞 3 个真实项；末页无 MORE，可塞满 4 个。\n",
    "6": " * 策略：\n",
    "7": " *   - total ≤ 4：单页展示所有（无 MORE）\n",
    "8": " *   - total > 4：前 N-1 页每页 3 个 + MORE，末页塞剩下的（∈ [2, 4]）\n",
    "10": " * 页数：`totalPages = ceil((total - 4) / 3) + 1`\n",
    "11": " *   - 前 N-1 页各 3 项 = 3(N-1) 项\n",
    "12": " *   - 末页 total - 3(N-1) 项\n",
    "13": " *   - 反过来推 N：total - 3(N-1) ∈ [2, 4] ⇔ N - 1 = ceil((total - 4) / 3)\n",
    "15": " * 效果：\n",
    "16": " *   total=4  → [4]                   （单页）\n",
    "19": " *   total=7  → [3+MORE, 4]           ← 末页塞满 4，比\"3+2+2\"少 1 页\n",
    "22": " *   total=10 → [3+MORE, 3+MORE, 4]   ← 末页塞满 4\n",
    "26": " * 每页 count 恒定 ≥ 2 ≤ 4，永不 solo末页 —— init.ts 的 autoSelectSolo* 兜底\n",
    "27": " * 分支从此进不到 MORE 翻页路径（total===1 的场景由 advanceFromAgentPicked 上\n",
    "28": " * 游 auto-select，也走不到 form）。\n",
    "30": " * agents 和 tasks 共用同一分页，行为一致。\n",
    "33": "/** CC AskUserQuestion 单 question 硬上限（含 MORE 槽位）。 */\n",
    "36": "/** 非末页真实选项数（保留 1 slot 给 MORE→）。 */\n",
    "39": "/** 单页展示阈值：total ≤ 此值时不分页、不放 MORE，一页展示全部。 */\n",
    "43": "  /** 该 page 覆盖的元素区间 [start, end)，start 含 end 不含 —— 直接 slice 用。 */\n",
    "46": "  /** 该 page 是否是最后一页（末页不追加 MORE 选项）。 */\n",
    "48": "  /** 该 page 展示的真实选项数（= end - start）。 */\n",
    "50": "  /** 分页后的总页数（total ≤ 4 时为 1）。 */\n",
    "52": "  /** 全体元素数量，方便调用方拼提示文案。 */\n",
    "57": " * 计算给定 `total` 项、目标 `pageIndex`（0-based）的切片区间。\n",
    "59": " * 保证：任何合法的 pageIndex 返回的 `count >= 2`（除非 total < 2，此时是\n",
    "60": " * form builder 上游的边界问题，非本函数职责）。\n",
    "62": " * pageIndex 超出 totalPages-1 时，钳制到最后一页（防御性；正常调用方会先\n",
    "63": " * 用 totalPages-1 计算 safeNextPage，见 init.ts MORE 分支）。\n",
    "68": "  // 单页阈值：≤ CC_SINGLE_PAGE_LIMIT 时不分页。\n",
    "80": "  // total > 4 时：前 N-1 页各 3 real + MORE，末页塞 total - 3(N-1) 项（∈ [2, 4]）。\n",
    "81": "  // N - 1 页承载 3 每页需 ≥ (total - 4) 项（末页最多 4），即 N-1 = ceil((total-4) / 3)。\n",
    "84": "  // 钳制 pageIndex 到合法范围（防御性）。\n"
  },
  "MemoryProxy/src/session/codebuddy/cleaner.ts": {
    "3": " * 曾经这个文件里有 `stripInitArtifacts` 用来在 session_init 完成后剥离\n",
    "4": " * 假表单对话（避免 LLM 看到 form 交互）。**该功能已删除**——现在永远保留\n",
    "5": " * 用户的所有真实对话（含 session_init form 交互），不做任何删除。\n",
    "7": " * 目前只剩一个 export: `getLastUserMessageText`，用于在 session_init\n",
    "8": " * state machine 里读最后一条 user / tool 消息的文本以解析用户选择。\n",
    "10": " * ── CodeBuddy ask_followup_question 回写格式 ──\n",
    "12": " * 用户点击表单后，CodeBuddy 下一条请求中问答所在的消息结构：\n",
    "16": " *   [N]   role=user       content=<additional_data> 或其他普通 user 消息\n",
    "18": " * multi_question_result JSON（实际抓包格式）：\n",
    "19": " *   空壳中间态（表单刚展示，用户还没点）：\n",
    "25": " *   真实答案（用户点击后）：\n",
    "27": " *      \"questions\":[{\"id\":\"team\",\"answer\":\"Team名 (id尾8位)\",...}],\n",
    "28": " *      \"answers\":{\"team\":\"Team名 (id尾8位)\"}}}\n",
    "30": " * getLastUserMessageText 当前只扫描 user 消息，不处理 tool 消息。\n",
    "31": " * team 提取依赖 extractor 的 substring 兜底匹配在无关 user 文本中碰巧蹭到 team 名，\n",
    "32": " * 不是精确解析。如需可靠提取，需增加 tool 消息解析路径。\n",
    "63": "  // CB writes \"是，关联团队资产\" etc. into tool_result content.\n",
    "73": "    // 兼容四种前缀：CB 的 `call_session_init_`、WB 的 `call_wb_session_init_`\n",
    "74": "    // （workbuddy/form.ts 里 TOOLCALL_PREFIX = \"call_wb_session_init_\"）、\n",
    "75": "    // dsh 的 `call_dsh_session_init_`（dsh/form.ts TOOLCALL_PREFIX）、\n",
    "76": "    // opencode 的 `call_oc_session_init_`（opencode/form.ts TOOLCALL_PREFIX）。\n"
  },
  "MemoryProxy/src/session/codebuddy/extractor.ts": {
    "3": " * 解析用户从 `ask_followup_question` form 的回复。\n",
    "5": " * ── 为什么看上去像在解析 XML，但跨 team 多轮 form 也能用 ──\n",
    "7": " * 当前只解析 `<question_answer>` XML（CodeBuddy 旧格式）。\n",
    "8": " * 实测中 CodeBuddy 实际回写格式是 `role: \"tool\"` 消息中的 `multi_question_result` JSON\n",
    "9": " * （详见 cleaner.ts 头部注释中的抓包格式），但 extractor 的 substring 兜底匹配\n",
    "10": " * 能在无关 user 消息文本中\"碰巧\"匹配到 team/agent/task 名，使得 session init 侥幸成功。\n",
    "11": " * 这是 fragile 依赖，不是精确解析。如需可靠提取，需增加 JSON 解析路径。\n",
    "13": " * 不含任何 Claude Code 逻辑（不解析 JSON tool_result）。\n",
    "21": "const SKIP_RE = /跳过|不关联|skip|do not associate/i;\n",
    "24": "// ── opencode tool-result 剥壳 ───────────────────────────────────────────────\n",
    "26": "// opencode CLI 的原生 `question` tool 收到用户选择后，把答案以纯文本形式回给\n",
    "27": "// model 作为 tool-result，格式为：\n",
    "28": "//   User has answered your questions: \"问题描述...\"=\"用户答案\"[, \"问题2\"=\"答案2\"]\n",
    "30": "// 这里的**问题描述里往往包含\"跳过\"、\"不关联\"等字样**（因为我们在 form 里让\n",
    "31": "// 用户看到\"跳过\"选项）。如果直接把整段 content 喂给 extractAgentOnly /\n",
    "32": "// extractTaskOnly，第一行 SKIP_RE.test 就会命中问题描述里的\"跳过\" →\n",
    "33": "// 误判为 BYPASS_MARKER，导致 agent_select / task_select 阶段永远走不通。\n",
    "35": "// 修复：在这些 extractor 入口先剥壳——只提取所有 `=\"...\"` 右边的 answer 段\n",
    "36": "// 拼接后再做后续判断。非 opencode 场景（CB XML / codex 裸文本 / wb 直接\n",
    "37": "// answer）不含此包裹层，helper 返回 null，走原始 content 老路径。\n",
    "39": "// asset_confirm 场景不用这个 helper，因为 extractAssetConfirm 是\"先找肯定\n",
    "40": "// 标记再找否定标记\"的白名单式匹配，问题描述里的\"跳过\"字样天然无害。\n",
    "42": "// 匹配 `=\"value\"` 中的 value——注意 value 里可能出现被 opencode 转义过的\n",
    "43": "// 内层引号，实测（2026-08-19）opencode 客户端遇到内层引号会输出转义后的\n",
    "44": "// `\\\"` 或直接透传全角引号 `\"`，为最大兼容用 `[^\"]*` 简易匹配即可（覆盖\n",
    "45": "// 我们 form.ts 里所有 label——纯 label 文本没有裸英文引号）。\n",
    "58": " * 从用户答复中提取 asset_confirm 选择。\n",
    "59": " * 返回 true=是（关联资产），false=否（bypass），null=未识别。\n",
    "68": "    answer.includes(\"是，关联团队资产\") ||\n",
    "69": "    /^(?:yes|y|是|确认)/i.test(answer.trim()) ||\n",
    "70": "    /是.*关联|关联.*是|确认.*关联/i.test(answer)\n",
    "76": "    answer.includes(\"否，本次不关联\") ||\n",
    "77": "    /^(?:no|n|否|不|跳过|skip)/i.test(answer.trim()) ||\n",
    "78": "    /否.*不关联|不关联.*否|本次不关联/i.test(answer)\n",
    "85": "// ── XML 解析 ───────────────────────────────────────────────────────────────────\n",
    "105": "  // 先扫描所有 question_item 判断总数（轮1: 1个, 轮2: 2个）\n",
    "144": "// ── Team 匹配 ──────────────────────────────────────────────────────────────────\n",
    "147": " * 轮1 提取：从用户答复中识别选定的 team_id。\n",
    "148": " * CodeBuddy: 用户选择在 `role: \"user\"` 消息中，走 `<question_answer>` XML 解析。\n",
    "156": "  // opencode: 剥壳 tool-result 包裹，避免问题描述里的\"跳过\"字样触发误 SKIP。\n",
    "157": "  // 见 extractOpencodeAnswers 头部注释。\n",
    "169": "  // 匹配策略（team 选项 label 格式: \"team名 (id尾8位)\"）\n",
    "173": "  // 检测\"本次不关联\" / SKIP_RE → bypass。\n",
    "174": "  // (P1-4) 早期只对 XML 解析出的 teamText 判 SKIP_RE, 非 XML content 走不到;\n",
    "175": "  // 真 codex CLI 里 codexFormAnswersAsMessages 把 JSON 答案抽成裸 content\n",
    "176": "  // (如 \"跳过\"), 结果 SKIP_RE 永远不触发 → 走到普通 team 名匹配 → 未命中 →\n",
    "177": "  // 被上层 (init.ts pending_team_select) 当\"未识别\"计 attemptCount, 3 次才\n",
    "178": "  // maxRetries 强制 bypass。对齐 extractAgentOnly (line 293) /\n",
    "179": "  // extractTaskOnly (line 324) 的姿势: 在正式匹配前无条件测 SKIP_RE。\n",
    "208": "// ── Agent / Task 匹配 ─────────────────────────────────────────────────────────\n",
    "264": " * 轮2 提取：从用户答复中识别 agent + task，**强制限定在已选定的 team 内**。\n",
    "265": " * CodeBuddy: 走 `<question_answer>` XML 解析。\n",
    "279": "  // opencode: 剥壳 tool-result 包裹（见 extractOpencodeAnswers 头部）。\n",
    "293": "  // 检测 Agent 选了\"本次不关联\"→ bypass\n",
    "304": "  // Resolve task。defaultTaskId 兜底通过 fetchTeamsAndAgents 头部注入实现：\n",
    "305": "  // 用户选中\"本次不关联任务\"label 会 matchTaskInTeam 命中虚拟条目返回\n",
    "306": "  // defaultTaskId，无需在此单独处理。SKIP_RE 兜底放到 match 失败之后，避免\n",
    "307": "  // 虚拟条目的\"不关联\"文案误伤。\n",
    "312": "    taskId = undefined; // 显式手打\"跳过\"→ 保持 undefined（走 completeRegistration bypass）\n",
    "318": "// ── codex-only 单题提取器 ─────────────────────────────────────────────────────\n",
    "320": "// 2026-08-08 codex session-init 重构：拆分 pending_agent_task 为独立的\n",
    "321": "// pending_agent_select + pending_task_select 后，每一步都只提取一个字段。\n",
    "322": "// 复用 matchAgentInTeam / matchTaskInTeam 上面的模糊匹配（含 label/name/\n",
    "323": "// suffix/substring 兜底），保证 codex handler 传下来的 \"AgentName (xxxxxxxx)\"\n",
    "324": "// 之类原样 label 也能命中。\n",
    "326": "// CB 客户端老路径（一发同时问 agent+task）继续走 extractFromOptionText，不\n",
    "327": "// 触碰这两个新函数。\n",
    "330": " * 仅从纯文本 answer 里识别一个 agent_id（codex 单 agent_select stage）。\n",
    "332": " * 返回：\n",
    "333": " *   - BYPASS_MARKER：用户显式表达\"跳过/不关联\"\n",
    "334": " *   - agent_id：命中候选\n",
    "335": " *   - null：未识别\n",
    "348": "  // opencode: 剥壳 tool-result 包裹（见 extractOpencodeAnswers 头部）。\n",
    "358": " * 仅从纯文本 answer 里识别一个 task_id（codex 单 task_select stage）。\n",
    "360": " * 返回：\n",
    "361": " *   - BYPASS_MARKER：用户显式表达\"跳过/不关联\"（此处的\"不关联任务\"由 defaultTaskId\n",
    "362": " *     虚拟条目命中 matchTaskInTeam 走返回 task_id 分支，而不会跑到 BYPASS 分支）\n",
    "363": " *   - task_id：命中候选\n",
    "364": " *   - null：未识别\n",
    "377": "  // opencode: 剥壳 tool-result 包裹（见 extractOpencodeAnswers 头部）。\n",
    "382": "  // 先尝试匹配真实/虚拟 task 条目（虚拟条目由 fetchTeamsAndAgents 头部注入,\n",
    "383": "  // label=\"本次不关联任务\"命中后返回的是 defaultTaskId，符合\"跳过 task 但保\n",
    "384": "  // 留 agent\"契约，不当作 BYPASS）。\n"
  },
  "MemoryProxy/src/session/codebuddy/form.ts": {
    "3": " * CodeBuddy 渲染可点击按钮的 form：\n",
    "5": " *   - Options: 平铺字符串列表，无数量限制\n",
    "9": " * 不含任何 Claude Code 逻辑。\n",
    "22": "/** 兼容旧测试的总标题（cleaner.ts 检测用）。 */\n",
    "33": " * 附在每步 question 文末的通用备注：告诉用户\"选择跳过 = 本次 session init 跳过、不注入任何团队资产\"。\n",
    "34": " * CodeBuddy 是按钮式表单，唯一的跳过入口在最初的 asset_confirm 步骤选「否」；\n",
    "35": " * 进入 team / agent_task 后没有按钮内跳过，需要下一次会话重新选择。\n",
    "36": " * 文案与 claude-code/workbuddy/codex/dsh 五端统一；后续步骤额外提示回退路径。\n",
    "60": " * CB form 支持的 stage。\n",
    "62": " * CB 客户端只用 \"asset_confirm\" | \"team\" | \"agent_task\"（agent+task 一发同时问）。\n",
    "63": " * 2026-08-08 拆 stage 后 codex 客户端复用 CB 状态机时会额外走 \"agent_select\"\n",
    "64": " * 和 \"task_select\" 两个子 stage —— CB 出口 formData 的 stage 字段值会跟到\n",
    "65": " * codex handler，`buildCodexFormResponse` 拿 stage 判该 render 哪个 question。\n",
    "67": " * CB 自身的 `buildFollowupQuestionArgs` 遇到 agent_select/task_select 时按只问\n",
    "68": " * 一个的语义 render；CB 客户端在 codex-only 路径下走不到这里（真正 render\n",
    "69": " * 出口是 codex form.ts），保留分支只是防御性兜底 & 便于 CB 侧单测。\n",
    "83": "   * codex-only：agent_select 阶段选定的 agent_id，透传给 task_select stage\n",
    "84": "   * form。CB 客户端自身不使用（CB 一发同时问 agent+task）。\n",
    "92": "   * 仅 agentSource=\"codex\" 场景使用：CB 状态机透传给下游 codex form 重渲染的\n",
    "93": "   * 分页页码。CB 客户端自己不 render 分页（`ask_followup_question` 无 option\n",
    "94": "   * 数量限制），字段填了也不影响 CB 出口。\n",
    "100": "   * true = questions 传真 array（CB v1.106+）；false = 传 JSON string（老版本）。\n",
    "101": "   * 未设置时默认 true。\n",
    "109": " * CB v1.106+ 的 ask_followup_question schema 要求 `questions` 是真 array（不再接受\n",
    "110": " * JSON 字符串）。老版本（v1.105-）则期望 questions 为 JSON string。\n",
    "111": " * 通过 FormData.questionsAsArray 判断走哪条路径，默认 true（新版）。\n",
    "135": "      question: \"本次对话是否要关联团队资产？\" + SKIP_HINT_ASSET_CONFIRM,\n",
    "145": "      question: \"请选择本次会话所属的 Team：\" + SKIP_HINT_LATER_STAGE,\n",
    "155": "  // (codex-only split). CB 客户端不会走后两个 stage —— codex handler 会用\n",
    "156": "  // codex form.ts 重渲染，不会调 CB `buildFollowupQuestionArgs`。分支保留\n",
    "157": "  // 是防御性兜底，让 CB fallback render 也能出合法结构。\n",
    "170": "      question: `请选择「${team.team_name}」下要使用的 Agent：` + SKIP_HINT_LATER_STAGE,\n",
    "179": "      // 虚拟兜底条目（isDefault）不拼 id 后缀，反正只有一个不会重名歧义。\n",
    "189": "        question: `请选择「${team.team_name}」下关联的任务：` + SKIP_HINT_LATER_STAGE,\n",
    "201": " * CodeBuddy 支持双协议：\n",
    "202": " *   - protocol=\"openai\": tool_calls chunk stream 或 JSON\n",
    "203": " *   - protocol=\"anthropic\": tool_use SSE stream 或 JSON\n"
  },
  "MemoryProxy/src/session/codebuddy/index.ts": {
    "3": " * 独立的 CodeBuddy session-init 实现：\n",
    "6": " *   - Cleaner: XML envelope 检测\n",
    "7": " *   - 无分页、无选项数限制\n"
  },
  "MemoryProxy/src/session/codebuddy/init.ts": {
    "1": " * CodeBuddy Session Initialization — 状态机入口.\n",
    "4": " *   1. uninitialized → 内核拉 teams[], 发 `ask_followup_question` form\n",
    "5": " *   2. pending_team_select → 解析用户 team 选择, 发 agent_task form\n",
    "6": " *   3. pending_agent_task → 解析 agent+task, fetch 详情, register, inject\n",
    "7": " *   4. initialized → 每次请求 strip + inject\n",
    "47": "// WorkBuddy 复用 CC 的 AskUserQuestion form + CC 分页布局（workbuddy/form.ts 直接\n",
    "48": "// import computePagination + MORE_LABEL=\"更多 →\"）。WorkBuddy 走 CB 状态机时的\n",
    "49": "// MORE 翻页拦截需要用同一套 MORE_LABEL 判定 + computePagination 判越界，才能与\n",
    "50": "// form 侧切片对齐。CC 的 MORE_LABEL 值与 workbuddy/form.ts 完全一致。\n",
    "61": "   * codex 客户端专属：codex 的答复不走 CB 兼容的 messages[]，而是塞在\n",
    "62": "   * `body.input[]` 里的 `function_call_output` 项。codexHandler 已经用\n",
    "63": "   * `codexFormAnswersAsMessages` 转出一份 `messages` 交给 CB 状态机做正常的\n",
    "64": "   * 选项匹配，但状态机内部还需要**原始** input[] 才能：\n",
    "65": "   *   1. 检测 Default 模式 gate 字符串（客户端每次会回放历史 gate output）\n",
    "66": "   *   2. 按 question id 精确定位 MORE 命中的是 team/agent/task 哪个\n",
    "68": "   * CC/普通 CB 场景永远不传此字段。\n",
    "72": "   * CB v1.106+ 的 ask_followup_question schema 要求 questions 为真 array；\n",
    "73": "   * 老版本期望 JSON 字符串。handler 从 body.tools 中检测后填入此字段，\n",
    "74": "   * form builder 据此决定是否 JSON.stringify(questions)。\n",
    "75": "   * 未设置时默认 true（向新版对齐）。\n",
    "88": "  /** 用户选\"否\"不关联团队资产 → bypass 路径，所有注入钩子应跳过。 */\n",
    "91": "   * bypass 触发原因（仅 `bypassed === true` 时有意义）。codexHandler 用它决定\n",
    "92": "   * 首次 gate 命中是否要返 \"Plan 模式提示\" 而非直接透传。\n",
    "94": "   * - \"default-gate\"  → codex 客户端 Default 模式截断了我们的 request_user_input\n",
    "95": "   *                     并伪造出 \"request_user_input is unavailable in Default mode\"\n",
    "96": "   *                     字符串；codexHandler 应返一次 Plan 模式提示后再进入 bypass\n",
    "97": "   *                     稳态。\n",
    "98": "   * - undefined       → 其它 bypass 路径（用户显式选\"否\"、no-agents、preset\n",
    "99": "   *                     mismatch 等），走各自 handler 的默认 bypass 行为。\n",
    "101": "   * CC/CB 客户端本身触发不到 default-gate（客户端不会伪造 gate 字符串），保留\n",
    "102": "   * 常量供跨 handler 判定即可。\n",
    "113": "   * 原始 FormData —— 当 `intercepted === true` 时**总是**填充。\n",
    "115": "   * codex handler 拿到后用 `session/codex/form.ts::buildFormResponse` 重渲染成\n",
    "116": "   * OpenAI Responses API SSE 格式（`response.output_item.added` +\n",
    "117": "   * `function_call` item）。CB / CC 自身不会读这个字段——`response` 已经是它们\n",
    "118": "   * 各自协议下的完整响应。\n",
    "128": " * codex-only: 判定 `body.input[]` 里最后一个 function_call_output 是否为\n",
    "129": " * 客户端 Default 模式 gate 拦截。客户端每轮会带上历史 gate output，因此\n",
    "130": " * \"是否首次命中\"由调用方看 sessionStore.bypassed 判断，这里只负责结构识别。\n",
    "132": " * CB/CC 客户端从不会发这个字符串，所以放在 CB 状态机里 opt-in 检测 codex 专属\n",
    "133": " * 场景（agentSource===\"codex\" + reqCtx.codexAnswerInput 非空）依然安全。\n",
    "137": "  // 只识别\"input 的最后一个 item 就是 gate output\"的情况 —— 即这一轮 codex 客户端\n",
    "138": "  // 拦截了 tool_call 并直接 replay gate。\n",
    "140": "  // 为什么必须看**最末 item** 而不是\"最新的 function_call_output\"：\n",
    "141": "  // codex 客户端每一轮都 replay 整个历史 input,一旦历史里出现过 Default 模式的\n",
    "142": "  // gate output,它会永远沉淀在 input 里。用户从 Default 切到 Plan 后重发命令,\n",
    "143": "  // 客户端会在 input 尾部**追加一条新的 user message**（那条 mem:session-reset\n",
    "144": "  // 或用户对 form 的答复）,老 gate output 仍留在中间。若扫\"最新 function_call_output\"\n",
    "145": "  // 则永远命中老 gate,死循环卡住\"请切到 Plan 模式\"文案。\n",
    "147": "  // 只有当尾部就是 function_call_output(说明客户端刚 replay gate,还没让用户输入)\n",
    "148": "  // 才判 Default;若尾部是 user message / tool_use / 其他 → 说明当前是新一轮 turn,\n",
    "149": "  // 忽略历史残留 gate。\n",
    "158": " * codex-only: 根据 recovered.status + detectCodexMore().perQuestion 决定\n",
    "159": " * 应该 bump 的 pageIndex（team/agent/task）。返回 bump 结果与已经填好的\n",
    "160": " * 新 codexPageIndex object；调用方拿去写 state + 重发同 stage 的 form。\n",
    "162": " * partialMore 场景（agent 是真选、task=更多）：直接把 task 那侧 pageIndex+1\n",
    "163": " * 后交给 CB 状态机继续消费——CB 只会命中真选 agent 推进大状态；task 那侧靠\n",
    "164": " * 下一 stage form 的 taskPage 值重新出题。\n",
    "190": " * 生成带上 codex 分页页码的 FormData，供 codex handler 重渲染。CB 客户端\n",
    "191": " * 会忽略这些页码字段（CB `ask_followup_question` 不分页）。\n",
    "207": " * WorkBuddy-only: 检测某一 stage 的用户答复是否点了 \"更多 →\"，命中则算出翻页后\n",
    "208": " * 的 pageIndex（越界回绕到第 0 页）。\n",
    "210": " * 背景：WorkBuddy（agentSource=\"workbuddy\"）复用 CB 状态机跑\n",
    "212": " * 全套流程，但 form 渲染走 workbuddy/form.ts（AskUserQuestion + CC 分页，\n",
    "213": " * MORE_LABEL=\"更多 →\"）。CB 状态机原本只在 agentSource=\"codex\" 分支拦截 MORE\n",
    "214": " * （B 段的 isCodexSource 门控），WorkBuddy 落到 pending_*_select 分支后\n",
    "215": " * extractAgentOnly/extractTaskOnly 不识别 \"更多 →\" → 未识别 → 无限重发第 1 页。\n",
    "217": " * 修法：在每个 pending_*_select 分支入口、跑 extractor 之前，先用本函数判定\n",
    "218": " * MORE。命中就 bump 对应 stage 的页码并重发同 stage form（页码经\n",
    "219": " * session/index.ts 的 workbuddy 重渲染分支按 stage 从 codexPageIndex 挑出，\n",
    "220": " * 传给 workbuddy/form.ts 的 pageIndex）。\n",
    "222": " * @param answerText  getLastUserMessageText(messages) 提取的用户答复文本\n",
    "223": " * @param currentPage 当前 stage 的页码（0-based）\n",
    "224": " * @param total       当前 stage 候选总数（agents.length / tasks.length / teams.length）\n",
    "225": " * @returns null=非 MORE；number=翻页后的 pageIndex（越界回绕 0）\n",
    "234": "  // 用与 form 侧同款分页算法判越界，防止翻过末页后停在越界页导致 form 抛\n",
    "235": "  // solo-page 断言；对齐 claude-code/init.ts 的 safeNextPage 回绕逻辑。\n",
    "240": "/** 判断是否是「全新」CodeBuddy / dsh 对话（最多一条真用户输入、无 assistant/tool）。\n",
    "242": " * dsh (deepseek-harness) 首帧 body 里塞 3 条**非用户输入**的 role=user 元数据:\n",
    "243": " *   - <system-reminder> 工作区指令\n",
    "244": " *   - \"Current runtime context.\" 快照\n",
    "245": " *   - <system-reminder>\\nA skill is a reusable... 的 <available_skills> 列表\n",
    "246": " * 若原样计数会把 dsh 首帧误判为\"非全新\"→ 上层 safety-net 跳过 session-init。\n",
    "247": " * 这里在计数时跳过带 dsh 元数据签名的 user 消息(str content 且以已知锚点开头)。\n",
    "248": " * 见 MemoryProxy/docs/dsh-recon/2026-08-14-dsh-capture-analysis.md §2.3。\n",
    "256": "    // dsh 元数据 user 消息不算真用户输入\n",
    "293": "      // 见 claude-code/init.ts fetchTeamsAndAgents 的同款注释：defaultTaskId\n",
    "294": "      // 在源头 unshift 到 tasks 列表头部，下游 form/extractor 走既有路径。\n",
    "355": "  // 曾经这里会按 config.keepInitArtifacts 决定要不要 stripInitArtifacts,\n",
    "356": "  // 现在**永远保留** session_init form 交互, 不做任何删除。\n",
    "398": "  // narrowing to a task. The interactive \"本次不关联任务\" / defaultTaskId path\n",
    "414": "    // 当 task_id 是 defaultTaskId（虚拟值）时，跳过 getTask——内核不存在该 task。\n",
    "443": "  // Fire-and-forget: 记录参与日志（对齐 claude-code 分支，源标记为 codebuddy）。\n",
    "444": "  // bypass 场景已在上方 return，天然被过滤；失败仅 warn，不阻断注入。\n",
    "496": " * 顶层入口 wrapper：装饰 handleSessionInitInner，在完成后发一条埋点\n",
    "497": " * （仅当 prev !== initialized && after === initialized 时）。\n",
    "499": " * 埋点装饰绝不改动状态机；失败/异常静默，业务链路零感知。\n",
    "500": " * 详见 docs/design/2026-08-03-internal-usage-telemetry-plan.md §7.2。\n",
    "522": "    // codex-only 后置增强：把最新 state 里的 codexPageIndex 塞进 formData,\n",
    "523": "    // 让 codexHandler 的 buildCodexFormResponse 拿到正确的翻页页码。这样\n",
    "524": "    // handleSessionInitInner 内部所有 return { intercepted: true } 站点都\n",
    "525": "    // 不用逐个改。CB 客户端会忽略这些字段。\n",
    "561": "  // ── codex-only pre-checks: Default gate + MORE 分页 ───────────────────────\n",
    "563": "  // 两块识别 CB/CC 客户端永远命不中（DEFAULT_GATE_PREFIX / CODEX_MORE_LABEL\n",
    "564": "  // 是 codex 客户端专属字符串），因此 opt-in 到 codex source + 有原始 input\n",
    "565": "  // 才启用；老 CB 用户零回归。\n",
    "567": "  // isCodexClient (agentSource === \"codex\") 是\"stage 拆分\"总闸门：\n",
    "568": "  //   - 只要是 codex 客户端，session-init 就走两步式 pending_agent_select →\n",
    "569": "  //     pending_task_select，不再落地老 pending_agent_task。\n",
    "570": "  //   - CB 客户端一发同时问的 pending_agent_task 路径完全不动。\n",
    "571": "  // codexInput 检查放到 pre-checks 内，因为只有\"当前请求也带答案\"时才做\n",
    "572": "  // gate/MORE 识别；空 input 首帧不需要，但依然按 codex 语义走后续 stage 拆分。\n",
    "577": "  // A. Default 模式 gate —— codex 客户端拦截了 request_user_input 并回填了\n",
    "578": "  //    \"request_user_input is unavailable in Default mode\" 字符串。首次命中\n",
    "579": "  //    落 bypass state + 附带 bypassReason，让 codexHandler 返一次 Plan 模式\n",
    "580": "  //    提示；后续同 session 请求直接透传（state.bypassed=true 已在 Case 3\n",
    "581": "  //    分支覆盖）。\n",
    "605": "        // codex Default gate 触发时若来自 mem:session-reset,resetFlow 从旧 state 透传出\n",
    "606": "        // 供 codexHandler 换成\"reset 命令需要 Plan 模式\"的针对性文案。\n",
    "610": "    // 已 bypass: fall through, Case 3 会走透传分支\n",
    "613": "  // B. MORE 翻页 —— 用户点了 \"更多...\" 选项。CB 状态机自己识别并推进/翻页,\n",
    "614": "  //    而不是让 codexHandler 独立拦截。判定顺序：先看是否有真答案（partial vs\n",
    "615": "  //    full MORE），只在 fullMore 时拦截整个请求；partialMore 允许真答案推进\n",
    "616": "  //    大状态，MORE 那侧在推进后下一 stage 的 form 自动带新 pageIndex。\n",
    "625": "      // 用 codexFormAnswersAsMessages 转出的文本判断\"是否含非 MORE 真答案\"\n",
    "634": "      // fallbackStage: 纯字符串 MORE 时靠 state.status 反推\n",
    "655": "        // Full MORE (无真答案) → 拦截整个请求，返当前 stage 的新页 form。\n",
    "656": "        // Partial MORE (有真答案 + 有 MORE) → 落盘 pageIndex 但继续走 CB 状态机\n",
    "657": "        // 消费真答案，MORE 侧靠下一 stage form 的 taskPage/agentPage 值重出题。\n",
    "660": "          // Stage 依据当前 state.status 反推。拆分后每个 pending_* 都自映射到\n",
    "661": "          // 自己独立的 stage，回给 codex form 只出该 stage 的 question。\n",
    "687": "  // 当 sessionInit.debugForceIdentity 三元组齐全且 state 尚未 initialized 时，\n",
    "688": "  // 直接以强制身份完成注册，跳过 listTeams / 表单渲染。适用于本地/E2E 测试，\n",
    "689": "  // 无需依赖 kernel 侧的 team/agent 列表接口。\n",
    "690": "  // 对所有 agentSource（codebuddy/workbuddy/codex）统一生效。\n",
    "737": "  // ── Case 1: Uninitialized → 先弹 asset_confirm 对话框 ───────────────────\n",
    "846": "        // asset_confirm + team_select). codex/WB/dsh/opencode 走两步 stage 拆分，先 agent_select；\n",
    "847": "        // CB 客户端保持老 pending_agent_task 一发同时问的语义。\n",
    "849": "        // opencode 说明：opencode 客户端原生 `question` tool 每次只能弹一个题，\n",
    "850": "        // 无法承载\"同时问 agent+task\"的语义，必须拆 stage（同 codex/wb/dsh）。\n",
    "878": "    // 先弹 asset_confirm 对话框\n",
    "912": "      // bypass: 用户明确选择\"不关联\" —— 保留 form 对话原样，不删。\n",
    "934": "        // 递进 auto-select：team=1 时若 agent 也只有 1 个，直接跳过 agent form；\n",
    "935": "        // task 只有 1 个再进一步 auto-select，避免 form 出现 solo-page 断言。\n",
    "936": "        // 这个链路对 CB / WB / codex 都通用（CB 老路径原本会在 agent_task\n",
    "937": "        // 一发同时问，但 agent 只有 1 个时同样应跳过）。\n",
    "952": "          // 0 tasks → bypass；1 task → 直接 completeRegistration；≥2 → 出 task form。\n",
    "978": "          // ≥2 tasks：切到 task_select stage 出 task form。\n",
    "999": "        // ≥2 agents：CB 老路径 pending_agent_task 一发同时问；codex/WB/dsh/opencode 拆 stage\n",
    "1000": "        // 走 pending_agent_select。WB 的 form 本来就按 CC 风格拆开问，让它走\n",
    "1001": "        // codex 分支，避免落到 legacy agent_task stage 后 form 里只问 agent\n",
    "1002": "        // 却按老语义处理的语义歧义。opencode 原生 `question` tool 每次只能弹\n",
    "1003": "        // 一个题，也必须走 split stage。\n",
    "1077": "    // 用户在 team_select 阶段用 SKIP_RE (跳过/不关联/skip) 主动 bypass\n",
    "1078": "    // (P1-4 修复)。对齐 pending_agent_select (init.ts:922) / pending_task_select\n",
    "1079": "    // (init.ts:1037) 的 BYPASS_MARKER 分支姿势 —— 老代码这里漏写, 导致 SKIP\n",
    "1080": "    // 词被当\"未识别\"计入 attemptCount, 3 次才 maxRetries 强制 bypass。\n",
    "1099": "      // codex/WB/dsh/opencode 拆 stage：先 agent_select → task_select；CB 老路径继续 agent_task 一发同时问。\n",
    "1143": "  // codex 客户端专属分支。用户在 agent_select stage 选完 agent → 推进 task_select\n",
    "1144": "  // stage（若 team 只有 1 个 task 则 auto-select 直接 complete）。\n",
    "1145": "  // CB 客户端永远不进入此分支。\n",
    "1160": "    // ── WorkBuddy-only: MORE 翻页拦截 ──\n",
    "1161": "    // WorkBuddy 复用 CB 状态机 + workbuddy/form.ts 的分页 form（MORE_LABEL=\"更多 →\"）。\n",
    "1162": "    // extractAgentOnly 不识别 MORE，必须在其之前拦截，否则点\"更多 →\"会被当未识别 →\n",
    "1163": "    // 无限重发第 1 页。命中则 bump agentPage 并重发 agent_select form（页码经\n",
    "1164": "    // session/index.ts 的 workbuddy/opencode 重渲染分支按 stage 从 codexPageIndex.agentPage 挑出）。\n",
    "1165": "    // opencode 与 workbuddy 共用 `更多 →` MORE_LABEL 与分页语义，一并拦截。\n",
    "1214": "      // 0 tasks → bypass；1 task → auto-select 直接 complete。\n",
    "1246": "      // ≥2 tasks → 进 task_select stage 出下一 form。\n",
    "1252": "        // 进新 stage 时清掉 task 页码，从第 1 页开始翻。agentPage 已用完可保留可清；\n",
    "1253": "        // 保留无副作用，清了更干净——统一清成 0。\n",
    "1310": "    // ── WorkBuddy-only: MORE 翻页拦截 ──\n",
    "1311": "    // 同 pending_agent_select：extractTaskOnly 不识别 \"更多 →\"，必须先拦截。命中则\n",
    "1312": "    // bump taskPage 并重发 task_select form（页码经 session/index.ts 的 workbuddy/opencode\n",
    "1313": "    // 重渲染分支按 stage 从 codexPageIndex.taskPage 挑出）。\n",
    "1363": "      // 命中 task_id（含 defaultTaskId 虚拟条目）→ complete。\n",
    "1371": "    // 未识别 → retry / bypass。\n"
  },
  "MemoryProxy/src/session/codex/form.ts": {
    "3": " * Codex 客户端 form 协议：\n",
    "5": " *   - 参数: `{questions: [{prompt, options?}, ...]}`\n",
    "6": " *   - Protocol: OpenAI Responses API（SSE `response.*` 事件序列）\n",
    "8": " *   - Plan 模式门控: Default 模式下客户端 gate 拦截，返回\n",
    "11": " * 不含任何 Claude Code / CodeBuddy 逻辑。\n",
    "21": " * `function_call.call_id` 前缀 —— codex 客户端把 form 结果 replay 回 input[]\n",
    "22": " * 时用 `call_id` 关联到 `function_call_output`，此字段 OpenAI Responses 规范\n",
    "23": " * 允许自由格式，回读时用这个前缀识别是不是 session-init。\n",
    "27": " * `function_call.id` 前缀 —— OpenAI Responses 规范硬性要求 `id` 必须以 `fc`\n",
    "28": " * 开头（`fc_xxxxxxxxxxxx`）。之前 id 也用 TOOLCALL_PREFIX（call_ 起头），\n",
    "29": " * 严格执行该规范的 Responses API 上游会在客户端 replay 到 input[] 时返 400：\n",
    "32": " * 见 2026-08-13 用户抓包报错。call_id 保持 call_ 前缀，仅 id 用 fc_。\n",
    "45": " * codex 分页 \"更多...\" 选项的稳定标记（写在 option.label 里，用户点了会\n",
    "46": " * 原样回到 codex handler，我们据此拦截 → pageIndex++ 重发下一页 form）。\n",
    "48": " * MARKER 是内部识别用的稳定字符串（对齐 CC MORE_MARKER 命名），LABEL 是\n",
    "49": " * 面向用户的可读中文（\"更多...\"）。extract 时按 LABEL 子串匹配即可，\n",
    "50": " * MARKER 本身不进 label（避免\"__...\"字符污染 UI 显示）。\n",
    "56": " * 客户端 Default 模式下 gate 拦截的前缀字符串。\n",
    "57": " * `function_call_output.output` 以此开头时，判定为 gate 命中。\n",
    "62": " * 附在每步 question 文末的通用备注：告诉用户\"选择跳过 = 本次 session init 跳过、不注入任何团队资产\"。\n",
    "63": " * Codex 的 request_user_input 在 Plan 模式下展示 questions + options 给用户；\n",
    "64": " * 跳过入口为「否，本次不关联」按钮。文案与 claude-code/workbuddy/codebuddy/dsh 五端统一。\n",
    "66": "const SKIP_HINT = '（如选择\"跳过\"选项，本次 session init 将跳过，不注入任何团队资产）';\n",
    "84": " * 判定 `function_call_output.output` 是否为客户端 Default 模式 gate 拦截。\n",
    "85": " * gate 字符串 startsWith `\"request_user_input is unavailable in\"`。\n",
    "94": " * codex form 支持的 stage 枚举。\n",
    "96": " * 2026-08-08 重构：把老 \"agent_task\"（一发同时问 agent+task）在 codex 侧拆成\n",
    "97": " * 两个独立 stage：\"agent_select\" 只问 agent，\"task_select\" 只问 task。原因见\n",
    "98": " * docs & task-brief：老 stage 让 partialMore（agent 真选 + task=更多）无法回到\n",
    "99": " * 同一 stage 重问 task，用户翻页永远看第一页。\n",
    "101": " * 老 \"agent_task\" 值保留（用于 CB 客户端复用 CB 状态机时的 legacy 路径与旧\n",
    "102": " * 单元测试），codex 新会话不再使用它。\n",
    "111": "   * 已在 agent_select 阶段选定的 agent_id。task_select stage 渲染任务列表时\n",
    "112": "   * 该字段目前仅用于日志/回显——tasks 是 team-wide 的，不受 agent 影响。\n",
    "113": "   * 未来若引入 agent-scoped task 列表可直接派上用场。\n",
    "120": "   * codex 分页页码（0-based）。三个 stage 各自独立：\n",
    "122": "   *   - agentPage: agent_select stage（及 legacy agent_task 的 agent 问题）\n",
    "123": "   *   - taskPage:  task_select stage（及 legacy agent_task 的 task 问题）\n",
    "125": "   * 缺省全部按 0 处理。codex handler 从 sessionStore 的 codexPageIndex\n",
    "126": "   * 字段读出并传下来；用户答 \"更多...\" 时 handler 拦截并 +1 重发。\n",
    "136": " * 把 codex `body.input[]` 里 session-init form 的 `function_call_output` 提取\n",
    "137": " * 成 CB 兼容的 `messages[]`（一条 `role: \"user\"` 消息，content 是拼接后的答案\n",
    "138": " * 文本），以便复用 CB 的 `handleSessionInit` 状态机（不需要再造一套 codex 侧\n",
    "139": " * 的 extractor / state machine）。\n",
    "141": " * 只取**最后一个** call_id 以 `call_codex_session_init_` 起头的\n",
    "142": " * `function_call_output`，因为 codex 客户端会全量重放 input[]，累积多轮 form\n",
    "143": " * 答案；CB 状态机每次只关心最新一步。\n",
    "145": " * `output` 可能是：\n",
    "146": " *   - 纯字符串答案：`\"是，关联团队资产\"`\n",
    "147": " *   - JSON 字符串 `{\"answers\":{\"q1\":\"是\"}}`（不同版本 codex 客户端可能不同）\n",
    "148": " *   - JSON 字符串 `{\"question\":\"...\",\"answer\":\"...\"}` 或数组\n",
    "149": " * 兜底策略：能解析出结构化 answer 值就拼接返回，否则直接用原始字符串。\n",
    "151": " * 返回空数组 → CB 会走 `pending_*` 分支的重试/兜底逻辑。\n",
    "176": " * 尝试把 codex `function_call_output.output` 里的答案文本抽出来。\n",
    "178": " * codex `request_user_input` 的 tool_result 结构在不同客户端版本 / 语言下略有\n",
    "179": " * 差异——先尝试 JSON 解析常见几种 shape，都不匹配就退回原始字符串（CB 的\n",
    "180": " * 匹配器本来就是子串兜底，能命中 option label 就行）。\n",
    "186": "  // 非 JSON 前缀 → 直接原样返回。\n",
    "195": "    // JSON parse 失败 → 回退到原始字符串\n",
    "201": " * 扫描 codex `body.input[]` 里最后一次 session-init form 的\n",
    "202": " * `function_call_output`，按 question id 提取 MORE 标记命中情况。\n",
    "204": " * 用法：在 handleSessionInit 前拦截 —— 命中就 pageIndex+1，\n",
    "205": " * 直接重发 form，不推进 CB 状态机。\n",
    "207": " * 返回：\n",
    "208": " *   - hasMore: 至少一个 question 的答案含 CODEX_MORE_LABEL\n",
    "209": " *   - perQuestion: 各 question id 是否命中 MORE\n",
    "211": " * 对齐 `codexFormAnswersAsMessages`：只取最后一个 call_id 以\n",
    "212": " * `call_codex_session_init_` 起头的 `function_call_output`。\n",
    "214": " * 兼容多种 output 格式：\n",
    "215": " *   - 纯字符串（单题）\n",
    "216": " *   - JSON: `{\"answers\":{\"team_select\":\"更多...\"}}`\n",
    "217": " *   - JSON: `{\"answers\":{\"agent_select\":\"agent-1\",\"task_select\":\"更多...\"}}`\n",
    "220": " * 不解析的格式退化为\"只按纯字符串检查 CODEX_MORE_LABEL 子串\"——\n",
    "221": " * 此时 perQuestion 全 false（调用方按 state.status 自己决定该 bump 哪个）。\n",
    "254": "  // 尝试结构化解析\n",
    "264": "  // 兜底：整段文本子串匹配\n",
    "268": "  // 若结构化解析命中任何 perQuestion，也要把 hasMore 打上\n",
    "345": "// Codex `request_user_input` schema (从 codex 系统提示词里扒的官方定义)：\n",
    "348": "//     questions: [1..3 个],           顶层只能带 1~3 个 question\n",
    "349": "//     每个 question: {\n",
    "352": "//       question: string (required),  ← 注意字段名不是 prompt\n",
    "353": "//       options: [2..3 个],           每 question 只允许 2-3 个 option\n",
    "354": "//       每个 option: { label, description } (都 required)\n",
    "358": "// 客户端会自动追加\"其他\"选项作为自由输入兜底，我们不能自己塞。\n",
    "359": "// 选项超 3 个（比如 team/agent/task 列表长）→ 只留头 2 个 label，\n",
    "360": "// 用户想选剩下的就走\"其他\"手输 id/name。\n",
    "370": "  id: string;       // snake_case 稳定标识\n",
    "371": "  header: string;   // ≤ 12 chars UI 标签\n",
    "372": "  question: string; // 展示给用户的完整问题\n",
    "376": "/** 截断 header 到 ≤ 12 字符（中文按 1 字符算 —— codex 未明确，保守走字符数）。 */\n",
    "382": " * 分页构建 options：把 entries 按 codex 的 3-slot 上限切成多页，非末页尾部\n",
    "383": " * 追加一个 \"更多...\" 选项供用户翻页；末页无 MORE，直接列出剩余。\n",
    "385": " * - entries.length <= 3 && 单页 → 直接原样\n",
    "386": " * - 中间页：前 2 real + MORE（3 项，客户端\"其他\"槽独立）\n",
    "387": " * - 末页：剩余 2~3 real\n",
    "388": " * - entries.length === 0：兜底 2 项占位（codex 要求 ≥ 2）\n",
    "389": " * - entries.length === 1：真实项 + \"跳过\" 占位（codex 要求 ≥ 2）\n",
    "397": "      { label: \"跳过\", description: \"本次不关联，直接开始对话\" },\n",
    "398": "      { label: \"重试\", description: \"重新拉取列表再选\" },\n",
    "404": "      { label: \"跳过\", description: \"本次不关联，直接开始对话\" },\n",
    "420": "    description: `查看下一批候选（第 ${morePageNo}/${page.totalPages} 页，还剩 ${remaining} 个）`,\n",
    "426": "  return totalPages > 1 ? `（第 ${pageIndex + 1}/${totalPages} 页）` : \"\";\n",
    "431": "  const retryHint = retry ? \"（上次未识别，请重新选择）\" : \"\";\n",
    "439": "      header: clampHeader(\"是否关联资产\"),\n",
    "440": "      question: `本次对话是否要关联团队资产（Skill / Memory / Agent / Task / Knowledge）？${retryHint}`,\n",
    "444": "          description: \"接下来会请你选择 Team、Agent、Task，选完后每轮对话会自动注入相关资产。\",\n",
    "448": "          description: \"本次会话跳过团队资产，直接开始对话。\",\n",
    "457": "      description: `Team ID: ${t.team_id}${t.agents?.length ? `，含 ${t.agents.length} 个 Agent` : \"\"}`,\n",
    "462": "      header: clampHeader(\"选 Team\"),\n",
    "463": "      question: `请选择本次会话所属的 Team${pageSuffix(teamPage, pageInfo.totalPages)}${retryHint}：`,\n",
    "474": "  // Agent question — 新 stage \"agent_select\" 或 legacy \"agent_task\" 都渲染。\n",
    "483": "      header: clampHeader(\"选 Agent\"),\n",
    "484": "      question: `请选择「${team.team_name}」下要使用的 Agent${pageSuffix(agentPage, pageInfo.totalPages)}${retryHint}：`,\n",
    "489": "  // Task question — 新 stage \"task_select\" 或 legacy \"agent_task\" 都渲染。\n",
    "494": "        ? \"该 Agent 的默认任务（推荐）\"\n",
    "501": "      header: clampHeader(\"选 Task\"),\n",
    "502": "      question: `请选择「${team.team_name}」下关联的任务${pageSuffix(taskPage, pageInfo.totalPages)}${retryHint}：`,\n",
    "520": "  // `id` 与 `call_id` 是 OpenAI Responses 规范里两个独立字段：\n",
    "521": "  //   - id     ：function_call item 自身唯一标识，规范要求 `fc` 前缀\n",
    "522": "  //   - call_id：把 function_call 与后续 function_call_output 配对的关联键，\n",
    "523": "  //              规范允许自由格式。回读侧 (codexFormAnswersAsMessages /\n",
    "524": "  //              extractCodexMoreFlags) 用 call_id.startsWith(TOOLCALL_PREFIX)\n",
    "525": "  //              识别 session-init，所以 call_id 仍用 call_ 前缀。\n",
    "579": "  // 关键：Responses API 里每个 event 的 data JSON **必须**带 `type` 字段\n",
    "580": "  //（跟 SSE `event:` 头同名），否则 codex 客户端解析器读不到 event 类型，\n",
    "581": "  // 表现为 tool_call 没进客户端 UI（form 不弹）。对齐抓包底稿 §7.5.2/3 真实上游行为。\n",
    "613": "      // response.output_item.added (function_call) —— arguments 先空字符串,\n",
    "614": "      // 通过 arguments.delta 流式生成 (对齐真实上游帧序列)\n",
    "620": "      // response.function_call_arguments.delta —— 关键：真实上游 event 携带\n",
    "621": "      // item_id 用于跟 output_item.added 里的 item.id 对齐；缺 item_id 客户端\n",
    "622": "      // 无法把 delta 挂回对应 tool call。item.id 现在是 fcId (fc_ 前缀)，\n",
    "623": "      // item_id 必须同源。\n"
  },
  "MemoryProxy/src/session/codex/pagination.ts": {
    "1": " * Codex `request_user_input` 分页布局器 —— 结构照抄\n",
    "2": " * `session/claude-code/pagination.ts`，只是 codex 的 option 上限跟 CC 不同：\n",
    "4": " * - codex 单 question 最多 **3 个** option（客户端会自动追加\"其他\"占 1 位，\n",
    "5": " *   我们不能自己塞，所以实际能用 2 或 3 位）\n",
    "6": " * - 中间页需要留 1 slot 给\"MORE →\"翻页 → 每页最多 2 real\n",
    "7": " * - 末页无 MORE → 可塞满 3\n",
    "9": " * 策略：\n",
    "10": " *   - total ≤ 3：单页展示所有（无 MORE）\n",
    "11": " *   - total > 3：前 N-1 页每页 2 real + MORE，末页塞剩下的（∈ [2, 3]）\n",
    "13": " * 页数：`totalPages = ceil((total - 3) / 2) + 1`\n",
    "14": " *   - 前 N-1 页各 2 real = 2(N-1) 项\n",
    "15": " *   - 末页 total - 2(N-1) 项\n",
    "17": " * 效果：\n",
    "18": " *   total=3  → [3]                   （单页）\n",
    "20": " *   total=5  → [2+MORE, 3]           ← 末页塞满 3，比\"2+2+1\"避免 solo\n",
    "25": " * 每页 count 恒定 ≥ 2 ≤ 3，永不 solo 末页。\n",
    "27": " * 跟 CC 分页共用同一哲学，只是常量不同（3/2 vs 4/3）。agents 和 tasks\n",
    "28": " * 都用这套。\n",
    "31": "/** codex `request_user_input` 单 question option 硬上限（客户端\"其他\"槽独立）。 */\n",
    "34": "/** 非末页真实选项数（保留 1 slot 给 MORE→）。 */\n",
    "37": "/** 单页阈值：total ≤ 此值时不分页、不放 MORE。 */\n",
    "41": "  /** 该 page 覆盖的元素区间 [start, end)。 */\n",
    "44": "  /** 该 page 是否是最后一页（末页不追加 MORE 选项）。 */\n",
    "46": "  /** 该 page 展示的真实选项数（= end - start）。 */\n",
    "48": "  /** 分页后的总页数（total ≤ 3 时为 1）。 */\n",
    "50": "  /** 全体元素数量，方便调用方拼提示文案。 */\n",
    "55": " * 计算给定 `total` 项、目标 `pageIndex`（0-based）的切片区间。\n",
    "57": " * 保证：任何合法 pageIndex 返回 `count >= 2`（除非 total < 2 —— 边界由\n",
    "58": " * 上游 form builder 兜底）。\n",
    "60": " * pageIndex 超出 totalPages-1 时钳制到最后一页（防御性；正常调用方会先\n",
    "61": " * 用 totalPages-1 计算 safeNextPage）。\n",
    "80": "  // total > 3 时：前 N-1 页各 2 real + MORE，末页塞 total - 2(N-1) 项（∈ [2, 3]）。\n"
  },
  "MemoryProxy/src/session/dsh/form.ts": {
    "1": " * dsh Session Init Form — `ask_user_question` tool_call 载体。\n",
    "3": " * dsh(deepseek-harness)客户端在 preset 场景(web-app + standard/code/cordis\n",
    "4": " * 等 preset)会自动挂 `@deepseek-ai/dsh-tool-ask-user`,给主对话 tools 数组\n",
    "5": " * 加一个 `ask_user_question` 工具(见 dsh 源码\n",
    "8": " * proxy 侧的 session-init form 直接复用这个 dsh 原生 tool 名,fake 一个\n",
    "9": " * assistant tool_call SSE 让客户端 UI 渲染选项。\n",
    "11": " * # 与 workbuddy form 的差异(3 处 shape 差异 + tool name)\n",
    "14": " *   - 每题必填 `id`(dsh schema 硬约束,echoed in answer)\n",
    "15": " *   - 顶层 `questions[]` 一次可发多题(workbuddy/CC 通常单题;这里为对齐 dsh\n",
    "16": " *     schema 保留数组结构,单题就一元素数组)\n",
    "18": " * # 传输\n",
    "19": " *   - 协议 = **OpenAI /v1/chat/completions**(与 dsh 客户端 fetch 一致)\n",
    "20": " *   - SSE stream 或 non-stream(与请求 `body.stream` 保持一致)\n",
    "21": " *   - 骨架完全照抄 workbuddy(chunk 1 = role+tool_call decl / chunk 2 =\n",
    "24": " * # 状态机\n",
    "25": " *   - 完全复用 CB 状态机(session/codebuddy/init.ts),同 workbuddy 模式;\n",
    "26": " *     stage 值(asset_confirm / team / agent_select / task_select / agent_task)\n",
    "27": " *     与 CB 完全一致,直接透传\n",
    "29": " * # tool_call id 前缀\n",
    "30": " *   - `call_dsh_session_init_` —— 区分于 CB(`call_session_init_`)、\n",
    "33": " * # 抓包 schema 依据\n",
    "35": " *   - dsh 源码 `packages/interaction/tool-ask-user/src/index.ts`\n",
    "39": "// dsh (deepseek-harness) 的 ask_user_question UI 无 options 数量上限\n",
    "40": "// (源码 packages/interaction/tool-ask-user/src/index.ts + UI QuestionComposer.tsx\n",
    "41": "// 都直接 map 渲染,无截断)。因此 dsh form **不分页**,team/agent/task 全量塞。\n",
    "42": "// 对比:CC 硬要求 ≤4 options(AskUserQuestion 内部校验),必须分页;codex 有类似\n",
    "43": "// 限制;CB 无限制也不分页。dsh 属于\"UI 无限制\"类。\n",
    "44": "// 详见:MemoryProxy/docs/dsh-recon/2026-08-14-dsh-integration-notes.md 坑 #9。\n",
    "48": "/** dsh 原生 tool 名。**不要**改成 CC 的 `AskUserQuestion`——dsh preset 挂的是 snake_case。 */\n",
    "57": "// dsh 不分页,MORE_LABEL 保留仅作向后兼容(测试或未来切分页时用);当前不产出。\n",
    "61": " * fake tool_call assistant 消息的占位 reasoning_content。\n",
    "63": " * ## 为什么必须非空\n",
    "65": " * dsh `serialize.ts:99` 只在 `toolCalls.length > 0 && reasoning.length > 0` 时才\n",
    "66": " * 回传 reasoning_content 到上游 body。dsh `translate.ts:133` 入站解析也一样,\n",
    "67": " * `reasoning_content.length > 0` 才开一个 reasoning block —— **空串直接吃掉**。\n",
    "69": " * 空串 `\"\"` = 客户端解析出 `text: \"\"` 的 block → serialize join 后 length 0\n",
    "70": " * → 上游 body 缺 reasoning_content → deepseek thinking 模式硬校验 400\n",
    "73": " * 塞一个非空占位就能让客户端真开一个 reasoning block,下一轮 replay 时带上。\n",
    "74": " * 值本身对模型无影响(反正 fake session-init 也不真过模型)。\n",
    "76": " * 见 docs/dsh-recon/2026-08-14-dsh-integration-notes.md 坑 #7。\n",
    "85": " * 附在每步 question 文末的通用备注：告诉用户\"选择跳过 = 本次 session init 跳过、不注入任何团队资产\"。\n",
    "86": " * dsh 的 ask_user_question UI 也支持 \"Other\"/自由文本兜底（见 dsh 文档 §3.4\n",
    "87": " * `custom` 字段）；回复\"跳过 / skip / 不关联\"会走 SKIP_RE bypass。\n",
    "88": " * 文案与 claude-code/workbuddy/codex/codebuddy 五端统一（此处也一并从半角改为全角标点）。\n",
    "90": "const SKIP_HINT = '（如选择\"跳过\"选项，本次 session init 将跳过，不注入任何团队资产）';\n",
    "116": "  /** @deprecated dsh 不分页(见文件头);字段保留为兼容 session/index.ts dispatch 时透传,builder 忽略。 */\n",
    "123": "// ── ask_user_question input schema (dsh snake_case + 必填 id) ──────────────────\n",
    "131": "  /** dsh schema 硬要求,echoed in answer;proxy 生成稳定 id(题目名短标签)。 */\n",
    "136": "  /** dsh 是 snake_case,与 CC 的 multiSelect camelCase 不同。 */\n",
    "148": "      question: titlePrefix + \"本次对话是否要关联团队资产?\" + SKIP_HINT,\n",
    "149": "      header: \"关联资产\",\n",
    "151": "        { label: ASSET_CONFIRM_YES, description: \"选择 Team / Agent / Task,注入团队上下文\" },\n",
    "152": "        { label: ASSET_CONFIRM_NO, description: \"本次不注入任何内容,直接放行\" },\n",
    "160": "    // dsh 无 options 数量上限,全量渲染,不分页(见文件头注释)。\n",
    "173": "      question: titlePrefix + \"请选择本次会话所属的 Team:\" + SKIP_HINT,\n",
    "185": "    // dsh 无 options 数量上限,全量渲染,不分页。\n",
    "200": "      question: titlePrefix + `请选择「${team.team_name}」下要使用的 Agent:` + SKIP_HINT,\n",
    "209": "    // dsh 无 options 数量上限,全量渲染,不分页。\n",
    "210": "    // team.tasks[0] 是虚拟 default 任务(\"本次不关联任务\"),源头 unshift 一次,\n",
    "211": "    // 不分页就不会像旧版每页都出现在开头(踩坑文档 §6 坑 #9)。\n",
    "228": "      question: titlePrefix + `请选择「${team.team_name}」下要关联的任务:` + SKIP_HINT,\n",
    "244": " * 传输:**OpenAI chat/completions**(stream 或 non-stream)。\n",
    "245": " * arguments shape:dsh 原生 `{questions: [{id, question, header, options, multi_select}]}`。\n",
    "280": "        // deepseek thinking 模式强约束 —— 完整分析见 REASONING_PLACEHOLDER 定义。\n",
    "281": "        // 关键:必须**非空**,否则客户端 translate.ts:133 遇 `reasoning.length > 0`\n",
    "282": "        // 判据吃掉,serialize.ts:99 输出侧再判 length 0 → 上游 body 缺字段 → 400。\n",
    "313": "      // reasoning_content 必须**非空**(值 = REASONING_PLACEHOLDER)—— 空串会被\n",
    "314": "      // dsh translate.ts:133 吃掉,serialize 时不回传,上游 400。\n"
  },
  "MemoryProxy/src/session/extractor.ts": {
    "17": "const SKIP_RE = /跳过|不关联|skip/i;\n",
    "19": "/** Bypass 标记：用户选了\"本次不关联\"，整个 session-init 直接跳过。 */\n",
    "23": " * 分页\"更多\"标记：Claude Code 分页流里，用户点\"更多 →\"会触发 handler 把\n",
    "24": " * agentPageIndex+1 并重发下一页 form。状态保持 pending_agent_task，不算 retry。\n",
    "37": " *   <question>请选择本次会话使用的 Agent：</question>\n",
    "39": " *   Bug Fixer — 自动定位并修复代码缺陷\n",
    "66": "  // 先扫描所有 question_item，判断总共有几个 question。\n",
    "67": "  // 轮1 form 只有 1 个 question（team），轮2 form 有 2 个（agent + task）。\n",
    "83": "    // 已知 id：team / agent / task；未知 id 走索引兜底\n",
    "92": "        // 轮1 form 只有 1 个 question — q1 就是 team\n",
    "95": "        // 轮2 form 有 2 个 question — q1 是 agent\n",
    "101": "      // 兜底：第一个 question 且 id 未知，归到 team\n",
    "111": " * 轮1 提取：从用户答复中识别选定的 team_id。\n",
    "113": " * 解析策略按 agentSource 分叉：\n",
    "114": " * - **CodeBuddy**: 用户选择在 `role: \"user\"` 消息中（`<question_answer>` XML 或纯文本），\n",
    "115": " *   直接走 XML 解析 + substring fallback。不做 JSON 解析，避免误读 tool 空壳。\n",
    "116": " * - **Claude Code**: 用户选择在 `role: \"tool\"` 消息中（`multi_question_result` JSON\n",
    "117": " *   或 `AskUserQuestion` tool_result），走 JSON 解析 + substring fallback。\n",
    "120": " * @returns team_id，或 BYPASS_MARKER（用户选了\"本次不关联\"），或 null（未识别）\n",
    "177": "  // 检测\"本次不关联\"→ 直接 bypass（只在已提取到 teamText 时判断，\n",
    "178": "  // 避免 content 中表单选项文本里的 \"跳过/不关联\" 误触发 bypass）\n",
    "183": "  // 匹配策略（team 选项 label 格式: \"team名 (id尾8位)\"）：\n",
    "184": "  //   1. 精确匹配完整 label（含 id 后缀）\n",
    "185": "  //   2. 精确匹配纯 team_name\n",
    "186": "  //   3. 按 id 后缀匹配 \"(xxxxxxxx)\" 部分\n",
    "187": "  //   4. substring fallback（按名称长度降序）\n",
    "216": " * 在指定 team 内匹配 agent。轮2 form 把 team 已经定死，没有跨 team 误匹配的可能。\n",
    "218": " * 匹配策略（agent 选项 label 格式: \"agent名 (id尾8位)\"）：\n",
    "219": " *   1. 精确匹配完整 label（含 id 后缀）\n",
    "220": " *   2. 精确匹配纯 agent_name\n",
    "221": " *   3. 按 id 后缀匹配 \"(xxxxxxxx)\" 部分\n",
    "222": " *   4. substring fallback（按 agent_name 长度倒序避免短名误匹配）\n",
    "252": " * 在指定 team 内匹配 task。task 选项 label 格式: \"任务名 (id尾8位)\"。\n",
    "253": " *   - 先尝试精确匹配完整 label（含 id 后缀）\n",
    "254": " *   - 再尝试仅匹配 task_name\n",
    "255": " *   - substring fallback（按名称长度降序，优先长名）\n",
    "265": "  // 1) 精确匹配完整 label: \"任务名 (xxxxxxxx)\"\n",
    "269": "  // 2) 精确匹配纯 task_name（可能有多个同名，返回第一个）\n",
    "273": "  // 3) 按 id 后缀精确匹配 \"(xxxxxxxx)\" 部分\n",
    "280": "  // 4) substring fallback：按 task_name 长度降序\n",
    "286": "  // 5) 宽松匹配：trimmed 中任意 8 字符子串匹配 id 后缀\n",
    "294": "/** @deprecated 旧扁平结构，仅保留供老测试调用。 */\n",
    "305": "/** @deprecated 旧扁平结构，仅保留供老测试调用。 */\n",
    "317": " * 轮2 提取：从用户答复中识别 agent + task，**强制限定在已选定的 team 内**。\n",
    "318": " * 跨 team 错配从协议层杜绝（轮1 form 已经把 team 定死）。\n",
    "320": " * 解析策略按 agentSource 分叉：\n",
    "321": " * - **CodeBuddy**: 用户选择在 `role: \"user\"` 消息中，走 `<question_answer>` XML 解析\n",
    "322": " *   + substring fallback。不做 JSON 解析，避免误读 tool 空壳。\n",
    "323": " * - **Claude Code**: 用户选择在 `role: \"tool\"` 消息中（`multi_question_result` JSON\n",
    "324": " *   或 `AskUserQuestion` tool_result），走 JSON 解析 + substring fallback。\n",
    "327": " * @returns `{ agent_id: BYPASS_MARKER }` 表示用户选了\"本次不关联\"。\n",
    "335": "  // 必须有已选 team 才进入轮2 解析；否则视为非法状态。\n",
    "402": "  // 检测 Agent 选了\"更多 →\"→ 翻页（仅在已提取到 agentText 时判断，\n",
    "403": "  // 避免 content 中表单选项文本里的 label 误触发）\n",
    "408": "  // 检测 Agent 选了\"本次不关联\"→ bypass（同上，仅在明确提取到 agentText 时判断）\n",
    "413": "  // Resolve agent —— 严格只在 team.agents 内匹配\n",
    "419": "  // Resolve task —— 同 team 内匹配；显式 skip 时返回 undefined\n",
    "450": " * 数字按所选 team 的 agents 1-based 取索引；字符串原样返回。\n",
    "462": "  // 仅在 rawAgentId 是 **纯数字**（\"1\" / \"2\" 这种序号回复）时按 1-based 索引解析。\n",
    "463": "  // 严禁用 parseInt 容忍前缀数字 —— ULID 以 \"01...\" 开头，parseInt 会拿到 1，\n",
    "464": "  // 把所有真实 agent_id 错误地映射成 team.agents[0]（团队首个 agent）。\n",
    "476": " * 数字按所选 team 内的 task 列表 1-based 取索引，agentHintId 优先；字符串原样返回。\n",
    "490": "  // 同 resolveAgent：只接受纯数字序号，避免 parseInt 容忍 \"01KV...\" 前缀的 \"0\" 误判。\n"
  },
  "MemoryProxy/src/session/form.ts": {
    "28": " * Form 标题：两轮分开。第一轮选 team，第二轮选 agent+task。\n",
    "29": " * extractor 不依赖标题判别，只用作 retry 提示 + UI 文案。\n",
    "33": "/** 跳过本次关联的选项文本，extractor 识别后直接 bypass 整个 session-init。 */\n",
    "37": " * 资产关联前置对话框选项。\n",
    "38": " * 用户选\"是\" → 继续 team/agent/task 流程\n",
    "39": " * 用户选\"否\" → 直接 bypass\n",
    "45": " * Claude Code 分页模式专用：当 team 下 agent 数量超过 3 时，最后 1 个槽位\n",
    "46": " * 用作\"更多 →\"按钮（非末页）。用户点击后 handler 把 agentPageIndex+1 重发 form。\n",
    "47": " * extractor 识别此 label 后返回 MORE_MARKER 信号。\n",
    "55": " * contain \"跳过 / 不关联 / skip\" (would fire SKIP_RE on unrelated text).\n",
    "59": "/** 兼容旧测试的总标题（cleaner.ts 检测用）。 */\n",
    "63": " * 选项 label 的分隔符。第二轮的 agent / task 选项不再带团队前缀\n",
    "64": " * （已被轮1 选定），仅 task 用 \"Agent / Task\" 体现 agent 归属。\n",
    "100": " * 两轮 form 共用的 props：\n",
    "101": " *   - stage=\"team\": 仅渲染 team 列表\n",
    "102": " *   - stage=\"agent_task\": 渲染所选 team 下的 agent + task\n",
    "103": " * 之所以拆两轮：单一 form 路径下用户可能在 agent 列表挑了 team_A 的 agent，\n",
    "104": " * 又在 task 列表挑了 team_B 的 task —— 跨 team 错配是合法的字符串组合，\n",
    "105": " * 但语义不一致。拆两轮把 team 维度提前定死，从协议层杜绝跨 team 错配。\n",
    "110": "  /** 控制面 /resources 返回的完整嵌套结构。 */\n",
    "112": "  /** 当前轮渲染哪个 stage 的 form。 */\n",
    "114": "  /** stage=\"agent_task\" 时必填，限定渲染哪个 team 下的 agent/task。 */\n",
    "117": "   * Claude Code 分页：stage=\"agent_task\" 时，按此页码切片 agents。\n",
    "118": "   * 每页显示 3 个 agent + 1 个\"更多 →\"或 SKIP_LABEL。\n",
    "119": "   * CodeBuddy 路径忽略此字段（ask_followup_question 没有选项数上限）。\n",
    "138": " *   - 轮1 (team): 单 question，列出所有 team。\n",
    "139": " *   - 轮2 (agent_task): 双 question，仅列出所选 team 下的 agent / task。\n",
    "141": " * extractor 通过 question_item 的 id 识别（\"team\" / \"agent\" / \"task\"），\n",
    "142": " * 标题仅作为 UI 文案。\n",
    "165": "      question: \"本次对话是否要关联团队资产？\",\n",
    "175": "      question: \"请选择本次会话所属的 Team：\",\n",
    "189": "    // agent 选项 label 格式: \"agent名 (id尾8位)\" 避免同名无法区分\n",
    "195": "      question: `请选择「${team.team_name}」下要使用的 Agent：`,\n",
    "201": "  // 该 team 下所有 tasks 的完整列表（不可跳过）\n",
    "202": "  // label 格式: \"任务名 (id尾8位)\" 避免同名 task 无法区分\n",
    "211": "      question: `请选择「${team.team_name}」下关联的任务：`,\n",
    "315": "    // label 格式: \"team名 (id尾8位)\" 避免同名无法区分\n",
    "316": "    // description 留空 —— label 已含 team 名 + id 后缀，重复即噪音。\n",
    "335": "  // Claude Code 分页：4 选项硬限制 → 每页 3 agents + 1 个\"更多→\"（非末页）。\n",
    "336": "  // 末页不再放 SKIP，只放剩余 agent。如果末页只有 1 个 agent，补占位保证 ≥2 options。\n",
    "347": "  // agent label 格式: \"agent名 (id尾8位)\" 避免同名无法区分。\n",
    "348": "  // description 只保留 agent 自身描述（有信息量），删掉尾部 \"(Team 共 N 个任务)\"\n",
    "349": "  // / \"(无任务)\" 提示 —— 用户此时正在选 agent，任务数量与他无关。\n",
    "355": "  // 非末页：放\"更多→\"；末页：不放任何额外选项（移除 SKIP）\n",
    "364": "  // AskUserQuestion 要求 options ≥ 2，极端情况下 slice 为空时兜底：\n",
    "365": "  // 用\"（无更多选项）\"占位，用户选它 → extractor 未识别 → init.ts bypass。\n",
    "370": "  const pageSuffix = totalPages > 1 ? `（第 ${pageIndex + 1}/${totalPages} 页）` : \"\";\n",
    "372": "    question: titlePrefix + `请选择「${team.team_name}」下要使用的 Agent${pageSuffix}：`,\n",
    "496": "    lines.push(\"请选择本次会话所属的 Team（回复编号或名称）：\", \"\");\n",
    "498": "      lines.push(`${i + 1}. **${t.team_name}** — ${t.agents.length} 个 Agent`);\n",
    "507": "  lines.push(`请选择「${team.team_name}」下要使用的 Agent（回复编号或名称）：`, \"\");\n",
    "547": "  const lines: string[] = [`### ${title}`, \"\", \"| # | 名称 | 说明 |\", \"|---|------|------|\"];\n",
    "550": "      lines.push(`| ${i + 1} | ${t.team_name} | ${t.agents.length} 个 Agent |`),\n",
    "552": "    lines.push(`| 0 | ${SKIP_LABEL} | 跳过本次注入 |`);\n",
    "559": "      lines.push(`| 0 | ${SKIP_LABEL} | 跳过本次注入 |`);\n",
    "698": "      if (isLast) opts.push({ label: SKIP_LABEL, description: \"本次不关联，直接放行\" });\n",
    "703": "          titlePrefix + `请选择本次会话所属的 Team（第 ${p + 1}/${totalPages} 页）：`,\n",
    "725": "    if (isLast) opts.push({ label: SKIP_LABEL, description: \"本次不关联，直接放行\" });\n",
    "726": "    if (opts.length < 2) opts.push({ label: \"其它\", description: \"需要其他选项\" });\n",
    "730": "        `请选择「${team.team_name}」下的 Agent（第 ${p + 1}/${totalPages} 页）：`,\n"
  },
  "MemoryProxy/src/session/index.ts": {
    "3": " * 拆分后的架构：\n",
    "4": " *   - codebuddy/    → CodeBuddy 专属 session-init（ask_followup_question form, XML extractor）\n",
    "5": " *   - claude-code/  → Claude Code 专属 session-init（AskUserQuestion form, JSON extractor, 分页）\n",
    "6": " *   - 共享模块：store.ts, types.ts, context-injector.ts, registrar.ts\n",
    "8": " * 内核 API 调用统一走 src/meta/client.ts (MetadataClient)。\n",
    "11": "// ── 共享模块 ──────────────────────────────────────────────────────────────────\n",
    "34": "// ── CodeBuddy 专属模块 ─────────────────────────────────────────────────────────\n",
    "48": "// ── Claude Code 专属模块 ───────────────────────────────────────────────────────\n",
    "63": "// ── 旧兼容 API（向后兼容 handler.ts 旧调用方式）─────────────────────────────────\n",
    "98": " * @deprecated 请使用 handleCodeBuddySessionInit() 或 handleClaudeCodeSessionInit()。\n",
    "99": " * 此函数根据 agentSource 参数路由到对应的实现。\n",
    "120": "      // 直接透传整个 reqCtx，避免手抠字段时把新加字段（如 codex 的\n",
    "121": "      // codexAnswerInput）漏掉。protocol MUST be forwarded — without it,\n",
    "136": "  // 同上：整个 reqCtx 透传给 CB 状态机。codexHandler 会把 body.input[] 塞在\n",
    "137": "  // reqCtx.codexAnswerInput 里，供 CB 状态机内部的 codex-only pre-checks 段\n",
    "138": "  // （session/codebuddy/init.ts 里 detectCodexDefaultGate + detectCodexMore）\n",
    "139": "  // 识别 Default gate 与 MORE 翻页。手抠字段会把它丢掉 → codex 分页失效。\n",
    "150": "  // WorkBuddy 客户端复用 CB 状态机（uninitialized → pending_team → pending_agent_task\n",
    "151": "  // → initialized 全套流程），但 form 渲染需要用 CC 的 `AskUserQuestion` shape +\n",
    "152": "  // OpenAI chat/completions SSE tool_calls 传输（WB 客户端是 CC tool 语义 +\n",
    "153": "  // OpenAI 协议的混合体，抓包 [wb-ask-user-schema] 已实证）。\n",
    "155": "  // 参照 codex 的模式（`session/codex/form.ts::buildFormResponse`），这里在 CB\n",
    "156": "  // 状态机产出 formData 后**外层重渲染**：丢掉 result.response（CB 的\n",
    "157": "  // ask_followup_question SSE），改用 workbuddy/form.ts 生成 AskUserQuestion SSE。\n",
    "158": "  // 好处：CB 状态机代码零改动，10 处 intercepted 站点无需逐个分派。\n",
    "163": "      // CB 状态机的 stage 值 (asset_confirm | team | agent_select | task_select |\n",
    "164": "      // agent_task) 与 WB form 的 FormStage 完全一致；直接透传。\n",
    "168": "      // CB 携带的翻页字段有 teamPage / agentPage / taskPage（codex 透传用），\n",
    "169": "      // WB form 用单个 pageIndex —— 根据当前 stage 挑对应页码。\n",
    "182": "  // dsh (deepseek-harness) 客户端复用 CB 状态机 + 自己的 ask_user_question 载体。\n",
    "183": "  // 与 workbuddy 完全对称：CB 状态机产出 formData 后外层重渲染 response,不共用\n",
    "184": "  // CB 的 ask_followup_question(dsh preset 挂的是 dsh 原生 ask_user_question,\n",
    "185": "  // 抓包实证 fixtures/dsh-tool-catalog-schema.json)。stage 值完全一致直接透传;\n",
    "186": "  // dsh form 用 CC-shape 的单一 pageIndex,同 workbuddy 挑分页字段。\n",
    "206": "  // opencode 客户端（sst/opencode CLI，Bun 打包二进制）在 agent-loop 里维护一个\n",
    "207": "  // 硬白名单 tools 集合：`bash, edit, glob, grep, invalid, question, read,\n",
    "208": "  // skill, task, todowrite, webfetch, write`。任何名字不在白名单里的 tool_call\n",
    "209": "  // 都会被客户端拒收并渲染成 `invalid [tool=xxx, error=Model tried to call\n",
    "210": "  // unavailable tool]`（抓包 2026-08-19 实证：CB 的 `ask_followup_question`\n",
    "211": "  // 被拒 3 次后 CB 状态机 abandon → 走 bypass）。\n",
    "213": "  // 解决：与 workbuddy / dsh 完全对称——CB 状态机产出 formData 后**外层重渲染**，\n",
    "214": "  // 换成 opencode 原生 `question` 工具的 tool_call SSE。stage 值直接透传，\n",
    "215": "  // 分页字段按当前 stage 挑对应页码。\n",
    "217": "  // 详见：session/opencode/form.ts 头部注释（schema 依据 / 三处 shape 差异）。\n"
  },
  "MemoryProxy/src/session/init-telemetry.ts": {
    "1": " * Session-init 埋点装饰器（内部使用埋点 §7.2 Chunk 2 A/B）。\n",
    "3": " * 设计：不改 handleSessionInit 内部 30+ 个 `store.set(status:\"initialized\")` 点位，\n",
    "4": " *      而是在**顶层入口的前后各读一次 state**，只在\n",
    "5": " *      `prev !== \"initialized\" && after === \"initialized\"` 时发一条埋点。\n",
    "7": " * 硬约束（§7.-1）：\n",
    "8": " *   - 同步返回 void，永远不抛\n",
    "9": " *   - sink 内部异常静默吞掉\n",
    "10": " *   - 无 state / 状态未变 / pending → pending 场景 no-op\n",
    "16": "/** 埋点装饰器输入。sink 默认为真实的 writeSessionInitRow，可注入用于测试。 */\n",
    "20": "  /** handleSessionInit 进入时的 store.get(...)?.status（如 undefined 传 \"uninitialized\"） */\n",
    "24": "  /** 走 bypass 分支时从 log 抓的原文首句（可选） */\n",
    "26": "  /** 测试注入点；默认调用 clickhouse.writeSessionInitRow */\n",
    "31": " * 若状态刚从非 initialized 迁移到 initialized，发一条 session_init 埋点。\n",
    "32": " * 其他情况全部 no-op。\n",
    "38": "    if (!state) return; // 未 set 过 → 例如 sessionKey=unknown\n",
    "39": "    if (state.status !== \"initialized\") return; // 中途 pending → 未完成\n",
    "58": "      // sink 抛异常 → 装饰器静默；埋点绝不阻塞业务\n",
    "61": "    // 装饰器自身也不允许 throw\n"
  },
  "MemoryProxy/src/session/model-intent-telemetry.ts": {
    "1": " * 模型意图 (tool_use) 埋点 helper —— handler.ts + anthropicHandler.ts 共用。\n",
    "3": " * 设计：SSE 流关流后，一次性把当轮 toolCallAccumulators 里所有 tool_use 转成\n",
    "4": " *      kind='model_intent' 的埋点条目发出去。\n",
    "6": " * 硬约束（§7.-1）：\n",
    "7": " *   - 同步返回 void，绝不 throw\n",
    "8": " *   - sink 单条抛异常不影响其他条\n",
    "9": " *   - 空 intents / 空 name 直接 skip\n",
    "13": "/** 一个模型工具调用意图的最小描述。 */\n",
    "17": "  /** function.arguments / JSON.stringify(tool_use.input) —— 原文，未截断 */\n",
    "31": " * 一次性把当前 turn 累积的所有 tool_use 都发一条 model_intent 埋点。\n",
    "32": " * 空数组或空 name 直接跳过；sink 单条异常不影响后续。\n",
    "42": "      if (!intent || !intent.name) continue; // 空 name → partial SSE frame，跳过\n",
    "60": "        // 单条失败 → 继续下一条；埋点绝不阻塞业务\n",
    "64": "    // 顶层兜底\n"
  },
  "MemoryProxy/src/session/opencode/form.ts": {
    "1": " * opencode Session Init Form — `question` tool_call 载体。\n",
    "3": " * opencode CLI（sst/opencode，Bun 打包二进制）在 agent-loop 里维护一个**硬白名单**\n",
    "4": " * tools 集合：`bash, edit, glob, grep, invalid, question, read, skill, task,\n",
    "5": " * todowrite, webfetch, write`。任何名字不在白名单里的 tool_call 都会被客户端\n",
    "6": " * 拒绝并渲染成 `invalid [tool=xxx, error=Model tried to call unavailable tool]`。\n",
    "8": " * 因此 proxy 侧的 session-init form **不能沿用** CB 的 `ask_followup_question`\n",
    "9": " * 或 CC 的 `AskUserQuestion`——必须映射到 opencode 原生 `question` 工具。\n",
    "11": " * # `question` 工具的 schema 依据\n",
    "13": " * 来源：sst/opencode `packages/opencode/src/tool/question.ts` + Question v1\n",
    "14": " * schema（`Question.Prompt`）。核心结构：\n",
    "18": " *         question: string,             // 题干（必填）\n",
    "19": " *         header:   string,             // ≤30 字符标题（必填）\n",
    "20": " *         options:  [{label, description?}, ...],  // 至少 1 项\n",
    "21": " *         multiple?: boolean,           // 是否多选（可选，默认 false）\n",
    "27": " * # 与 workbuddy form 的差异（3 处 + tool name）\n",
    "29": " *   - multiSelect (camelCase) → **multiple**（opencode 独有命名，既不是 CC 的\n",
    "30": " *     `multiSelect` 也不是 dsh 的 `multi_select`）\n",
    "31": " *   - header 长度约束：opencode ≤30 字符（wb/CC 无限制）→ 用 `.slice(0, 30)`\n",
    "32": " *   - tool_call id 前缀：`call_oc_session_init_`\n",
    "34": " * # 传输\n",
    "35": " *   - 协议 = **OpenAI /v1/chat/completions**（opencode 底层用\n",
    "37": " *   - SSE stream 或 non-stream（与请求 `body.stream` 保持一致）\n",
    "38": " *   - 骨架完全照抄 workbuddy（chunk 1 = role+tool_call decl / chunk 2 =\n",
    "41": " * # 状态机\n",
    "42": " *   - 完全复用 CB 状态机（session/codebuddy/init.ts），同 workbuddy/dsh 模式；\n",
    "43": " *     stage 值（asset_confirm / team / agent_select / task_select / agent_task）\n",
    "44": " *     与 CB 完全一致，直接透传。\n",
    "46": " * # 分页\n",
    "47": " *   - opencode `question.options` 未见硬上限，但为了 UI 观感与 CC/WB 保持一致，\n",
    "48": " *     沿用 `computePagination` + `MORE_LABEL` 尾槽方案（≤4 option/页）。\n",
    "50": " * # 用户答复的回传格式（供 extractor 参考）\n",
    "51": " *   opencode `question` 工具在用户选完后，会给 model 回一段文本形式的 tool-result：\n",
    "52": " *     `User has answered your questions: \"问题1\"=\"答案1\", \"问题2\"=\"答案2, 答案3\"`\n",
    "53": " *   现有 extractor 的 substring fallback（按 team_name / agent_name / 后 8 位 id\n",
    "54": " *   做 `hay.includes`）能覆盖这个格式，无需另加 opencode 专用分支。\n",
    "62": "/** opencode 原生 tool 名，硬白名单成员。不要改成 `AskUserQuestion` 或 `ask_followup_question`。 */\n",
    "78": " * 附在每步 question 文末的通用备注：告诉用户\"选择跳过 = 本次 session init 跳过、\n",
    "79": " * 不注入任何团队资产\"。opencode `question` 工具的 UI 也支持用户自由输入答复\n",
    "80": " * （非选项文字），回复\"跳过 / skip / 不关联\"会走 SKIP_RE bypass。\n",
    "81": " * 文案与 claude-code / workbuddy / codex / codebuddy / dsh 六端统一。\n",
    "85": "/** opencode question header 硬上限。schema 校验 ≤30 字符，超长会被客户端拒收。 */\n",
    "90": "/** 把任意 header 文本裁到 opencode schema 允许的 ≤30 字符。 */\n",
    "119": "  /** 分页：当前页码 (0-based)；对齐 CC/WB 只使用一个 pageIndex（team/agent/task 单题） */\n",
    "135": "  /** opencode 硬约束：≤30 字符。 */\n",
    "138": "  /** opencode 独有命名：`multiple`（既不是 CC 的 `multiSelect` 也不是 dsh 的 `multi_select`）。 */\n",
    "149": "      question: titlePrefix + \"本次对话是否要关联团队资产？\" + SKIP_HINT,\n",
    "150": "      header: clipHeader(\"关联资产\"),\n",
    "152": "        { label: ASSET_CONFIRM_YES, description: \"选择 Team / Agent / Task，注入团队上下文\" },\n",
    "153": "        { label: ASSET_CONFIRM_NO, description: \"本次不注入任何内容，直接放行\" },\n",
    "172": "      question: titlePrefix + \"请选择本次会话所属的 Team：\" + SKIP_HINT,\n",
    "195": "      combinedOptions.push({ label: MORE_LABEL, description: `查看下一批（还剩 ${remaining} 个 Agent）` });\n",
    "205": "    const pageSuffix = page.totalPages > 1 ? `（第 ${pageIndex + 1}/${page.totalPages} 页）` : \"\";\n",
    "207": "      question: titlePrefix + `请选择「${team.team_name}」下要使用的 Agent${pageSuffix}：` + SKIP_HINT,\n",
    "231": "        description: `查看下一批（还剩 ${remaining} 个任务）`,\n",
    "242": "    const taskPageSuffix = page.totalPages > 1 ? `（第 ${taskPageIndex + 1}/${page.totalPages} 页）` : \"\";\n",
    "244": "      question: titlePrefix + `请选择「${team.team_name}」下要关联的任务${taskPageSuffix}：` + SKIP_HINT,\n",
    "260": " * 传输：**OpenAI chat/completions**（stream 或 non-stream）。\n",
    "261": " * arguments shape：opencode 原生 `{questions: [{question, header, options, multiple}]}`。\n"
  },
  "MemoryProxy/src/session/preset.ts": {
    "112": "  // This matches the kernel's own semantics and the interactive \"本次不关联\n",
    "113": "  // 任务\" / defaultTaskId path.\n"
  },
  "MemoryProxy/src/session/registrar.ts": {
    "1": " * Session registration — 本地构建 SessionInfo，不再调用 TMC。\n",
    "3": " * proxy 通过 SessionStore（L1 内存 + L2 Redis/SQLite）独立持久化 session\n",
    "4": " * 状态，不再需要 POST /api/v1/proxy/sessions 写入 TMC。用户身份直接从\n",
    "5": " * apiKey → auth/verify 拿到。\n",
    "11": " * 本地构建 SessionInfo（不调用 TMC）。\n",
    "13": " * @param spaceId 来自请求 URL path `/proxy/<spaceId>/...` 的内核实例 ID\n",
    "14": " *   （如 `mem-example001`）。注入器构造 MetadataClient 时会用它做\n",
    "15": " *   `x-tdai-service-id` header —— 若为空字符串，内核会返回\n",
    "16": " *   `invalid_user_key`，属于预期行为（caller 已在 session init bypass\n",
    "17": " *   中处理）。\n"
  },
  "MemoryProxy/src/session/store.ts": {
    "37": " * `spaceId` 是 P4 (kernel-sts) 新增字段，用于 STS 权限按 space 隔离时的 key 拼接。\n",
    "38": " * 老 caller 不传时视作 `\"\"`（空串），Repo 内部会用 `_default` 兜底段处理。\n",
    "92": "   * 让 skill/memory bridge 拿到同一份 BindingRepo 实例。\n",
    "94": "   * bridge L2 fallthrough 从这里拿 —— 保证 injection pipeline 装配时的 Kv/Redis\n",
    "95": "   * 实例、单元测试注入的 mock 都能被 bridge 直接读到,不用重新构造。\n",
    "135": "   * 把 recovery source 挂到 state 的**非可枚举**字段上——handler 侧读取\n",
    "136": "   * `state.__recoverySource` 语义不变，但 `deepEqual` / `JSON.stringify` /\n",
    "137": "   * `Object.keys` 都不会看到这一枚 transient marker，避免测试断言\"恢复后\n",
    "138": "   * state 与原 state 完全等价\"因新增字段挂掉。\n",
    "157": "   * ⚠ 契约：`await store.set(...)` 完成时，L2a repo 已被 await（成功或静默失败）。\n",
    "158": "   * 见 2026-07-13 修复：原来 fire-and-forget 语义在多节点部署下会让 pod A\n",
    "159": "   * 关流时 COS PUT 还在飞，pod B 的 turn-2 因 L2a miss 直接掉进 tryHistoryScan\n",
    "160": "   * 兜底 → bypass → 请求透传 LLM。\n",
    "162": "   * L2b binding 仍是 fire-and-forget —— 只在 `initialized` 状态写入，属于\n",
    "163": "   * \"小纸条\"型持久化，用于长睡对话唤醒；写延迟不影响 pending 状态跨节点恢复。\n",
    "169": "    // pollute the repo (业务 caller 目前都从 store.get() 拿 state，本身没有该\n",
    "170": "    // 字段；这里是最后一道兜底).\n",
    "176": "    // resetFlow / resetEpoch 自动继承 —— pre-hook 写入这两个字段后,form 流程会\n",
    "177": "    // 经过多次 state 转换（pending_asset_confirm → pending_team_select → ...）,\n",
    "178": "    // 每次转换点若手工 new 一个 state 对象很容易漏掉这俩字段,导致 completeRegistration\n",
    "179": "    // 拿到 resetFlow=undefined,handler 侧就无法识别\"这是 reset 引导的完成回合\"→\n",
    "180": "    // 请求被透传给 LLM 产生幻觉响应。\n",
    "181": "    // 保守做法：只在新 state 未显式声明这俩字段（值为 undefined）时,从旧 state\n",
    "182": "    // 继承一次。显式传 false / 具体值的 caller 不会被覆盖。\n",
    "200": "    // L2a write-through —— MUST await；见方法头注释。\n",
    "201": "    // 二次防御性 catch：契约要求实现方（KvSessionRepo / RedisSessionRepo /\n",
    "202": "    // SqliteSessionRepo）内部静默降级不抛，但接口层再兜一层，保证任何后来\n",
    "203": "    // 新增的 repo 或 test-mock 都不会把异常泄给 44 处 `await store.set(...)`\n",
    "204": "    // caller —— L1 已成功写入，主流程不因 L2a 写失败挂掉。\n",
    "216": "    // await 而非 fire-and-forget，保持与 L2a 一致的契约：\n",
    "217": "    // `await store.set(...)` return 时，L1 / L2a / L2b 三层都已 durable。\n",
    "218": "    // 每个 session 只会在初始化终态触发一次，成本可控。\n",
    "220": "      // agentSource / userKey 现在存进 binding 内部字段(不再在 key 里),\n",
    "221": "      // 让 bridge 只凭 (spaceId, sessionId) 就能反查回完整身份。\n",
    "222": "      // 见 docs/design/2026-08-03-binding-flatten.md。\n",
    "333": "    // ⚠ Terminal 状态（`initialized`，含 `bypassed`）L1 才权威 —— 一旦定型\n",
    "334": "    // 不会再改；pending_* 状态**不能**在 L1 命中就短路，否则会踩到多节点跨\n",
    "335": "    // pod 陈旧读 bug（2026-07-14）：\n",
    "336": "    //   turn-1 打 pod A → 写 L1(A)=pending_asset_confirm + L2a\n",
    "337": "    //   turn-2 打 pod B → L2a probe 读到 pending_asset_confirm → advance 到\n",
    "338": "    //                     pending_agent_select → 写 L1(B) + L2a\n",
    "339": "    //   turn-3 又打 pod A → L1(A) 仍然是 pending_asset_confirm（pod 间无\n",
    "340": "    //                       cache-invalidation 通知）→ 若这里短路就用陈旧\n",
    "341": "    //                       state 去处理 turn-3 的 agent 答复 → extract 拿\n",
    "342": "    //                       \"agent 选项文本\" 去 asset_confirm 分支 →\n",
    "343": "    //                       unrecognized → session bypass → 请求原样透传给\n",
    "344": "    //                       LLM（用户观感：不选 task 就走了）。\n",
    "346": "    // 修法：pending_* 无论 L1 是否命中，都必须走 probeL2a 拿权威值；probeL2a\n",
    "347": "    // 内部会 promote 覆盖 L1，之后 init.ts 的 `store.get(compositeKey)` 就能\n",
    "348": "    // 读到最新状态。L1 pending 命中作为 L2a 失败/miss 时的 last-resort fallback\n",
    "349": "    // 保留（见 Step 2 后的分支），保证同 pod 场景下 L2a 尚未落盘时不倒退。\n",
    "351": "    // 代价：每轮多一次 storage GET（~50ms COS）。\n",
    "352": "    // 多节点无状态设计：L1 仅作为 L2a miss 时的降级 fallback（COS 抖动），\n",
    "353": "    // 不作为权威源。session-reset 可能在任意 pod 执行，L1 initialized 不可信。\n",
    "355": "    // 不对 initialized 做 L1 短路 —— 一律走 L2a 拿权威状态。\n",
    "362": "    // agentDetail/taskDetail is sitting in the storage layer. Pending 状态也\n",
    "363": "    // 必须命中就返回 —— 见上面 Step 1 的多节点陈旧 L1 注释。\n",
    "367": "        // 只有 L1 存在**且**status 不同才算真\"override stale L1\"。相同 status\n",
    "368": "        // 只是 L2a 权威读回来复核一遍，属正常路径（pending_* 每轮都会走 L2a\n",
    "369": "        // probe），日志里没必要拉警报，之前的无条件 \"override stale L1\" 会误\n",
    "370": "        // 导观察者以为有一致性问题。\n",
    "377": "    // Step 2.5: L2a miss 时的 L1 降级兜底。\n",
    "379": "    // L2a(COS) 抖动 / 短暂不可用时，继续走 L2b 只能拿到 binding（只在\n",
    "380": "    // initialized 写），最终 `tryHistoryScan` 无条件 bypass —— 反而更糟。\n",
    "381": "    // 这里回退到 L1 是 \"宁可用略旧但可用的状态\" 的 graceful degradation。\n",
    "383": "    // zombie / user-mismatch 已在 `this.get()` 与 `probeL2a` 内部各自 invalidate，\n",
    "384": "    // 走到这里的 l1 一定是 fresh + user 匹配的。\n",
    "463": "      // 诊断: probeL2a 报错时也 log，之前静默吞掉导致多节点 L2 miss 无迹可循\n",
    "470": "      // 诊断: 打出实际用来查的 4 段, 方便对着 COS 里的 key 手工比对\n",
    "622": "    // user_key / space_id 从 binding 恢复(2 段拍平后 binding 里也存了),\n",
    "623": "    // 让 bridge L2 fallthrough 恢复出的 SessionInfo 字段完整,memory-bridge\n",
    "624": "    // 恢复 chat_memory 检索时不再降级为 self-only。\n",
    "650": "    // await write-through 与 SessionStore.set 保持一致契约（见其头注释）：\n",
    "651": "    // 让恢复出的 rebuilt 状态在返回前已落 L2a，避免同 session 后续轮次\n",
    "652": "    // 若又打到别的 pod 时再走一次 rebuildFromBinding 的开销。\n",
    "653": "    // 防御性 catch 见 `set()` 头注释。\n",
    "699": "    // dsh (deepseek-harness) 首帧 body 里塞 3 条**非用户输入**的 role=user 元数据:\n",
    "700": "    //   - <system-reminder> 工作区指令\n",
    "701": "    //   - \"Current runtime context.\" 快照\n",
    "702": "    //   - <available_skills> 列表\n",
    "703": "    // 若原样计数 userCount>1 会把 dsh 首帧误判为\"有历史\",触发 markerless\n",
    "704": "    // one-shot bypass,session-init form 永远不弹。这里跳过 dsh 元数据 user 消息,\n",
    "705": "    // 只计\"真用户输入\"。见 docs/dsh-recon/2026-08-14-dsh-capture-analysis.md §2.3。\n",
    "715": "      // dsh 元数据签名:content 是 str 且以已知锚点开头(dsh 内部固定文本)。\n",
    "716": "      // 只在有明确签名时跳过,避免误伤客户端真用户输入。\n",
    "752": "              if (question.includes(\"关联\") || question.includes(\"资产\") || /associat|asset|link/i.test(question)) {\n",
    "753": "                // asset_confirm form — check if the next user message said \"否\"\n",
    "757": "                options?.includes(\"否，本次不关联\") ||\n",
    "759": "                options?.includes(\"跳过\") ||\n",
    "779": "        content.includes(\"否，本次不关联\") ||\n",
    "781": "        content.includes(\"本次不关联\")\n",
    "794": "      // 无论是\"历史里选了否\"还是\"完全没有 form marker\"，只要无法恢复 agent\n",
    "795": "      // 身份就视为未初始化 → 返回 undefined 让上层走 session-init 弹表单。\n",
    "796": "      // 与 mem:session-reset 语义对齐：session 未 initialized = 必须 init。\n"
  },
  "MemoryProxy/src/session/types.ts": {
    "5": " * 用于 `config.defaultTaskId` 兜底关联时，fetchTeamsAndAgents 在每个 team 的\n",
    "6": " * tasks 列表头部注入的虚拟条目 label。\n",
    "8": " * 通过在源头注入而不是在 form / extractor 侧加分支，让分页 total、auto-select\n",
    "9": " * 级联、extractor 匹配全部走既有 tasks.length 路径，避免\"分页真相分散\"型 bug\n",
    "10": " * （见 docs 里 defaultTaskId 相关记录 & 2026-07-29 issue）。\n",
    "12": "export const DEFAULT_TASK_LABEL = \"本次不关联任务\";\n",
    "15": " * Session-init 状态机：\n",
    "16": " *   uninitialized           → 第一次进来，控制面拉 teams[]\n",
    "17": " *   pending_asset_confirm   → 已发轮0 form（是否关联团队资产），等用户答\n",
    "18": " *   pending_team_select     → 已选\"是\"，已发轮1 form（只问 team），等用户答\n",
    "19": " *   pending_agent_task      → 已选 team，已发轮2 form（agent + task），等用户答\n",
    "20": " *   initialized             → 已识别完整三元组，登记完成\n",
    "22": " * 当 teams.length === 1 时跳过 pending_team_select，直接进入 pending_agent_task。\n",
    "23": " * 用户在 pending_asset_confirm 阶段选\"否\"时直接 bypass。\n",
    "33": "  // legacy（一期单 form），保留以兼容旧测试 / 旧 store 数据\n",
    "44": "  /** 内核 /teams 返回的嵌套结构，用于渲染 form 与解析用户答复。 */\n",
    "47": "   * 用户已在轮1 选定的 team_id（pending_agent_task 阶段才有意义）。\n",
    "48": "   * 轮2 form 仅渲染该 team 下的 agents/tasks；extractor 也只在该 team 内匹配。\n",
    "52": "   * Claude Code 分页模式下的当前 agent 页码（0-based）。\n",
    "54": "   * 背景：Claude Code 的 AskUserQuestion 单 question 限制 2–4 选项，当某 team\n",
    "55": "   * 下 agent 数量超过 3 个时无法一次性铺开。我们沿用现有\"多轮拦截\"机制，每\n",
    "56": "   * 渲染 3 个 agent + 1 个\"更多→\"或\"本次不关联\"槽位，用户点\"更多\"则 pageIndex++\n",
    "57": "   * 再发下一页 form。详见 docs/reports/2026-06-19-cc-form-mode-experiment.md §4.4。\n",
    "59": "   * - 仅 Claude Code（agentSource=\"claude-code\"）使用，CodeBuddy 走 ask_followup_question\n",
    "60": "   *   没有 4 选项限制，无需分页。\n",
    "61": "   * - 仅在 status=\"pending_agent_task\" 期间有效。\n",
    "62": "   * - 默认 0（首页）；每次用户选\"更多\"，handler 把它 +1 重发 form。\n",
    "65": "  /** CC: 用户在 agent_select 阶段选定的 agent_id（用于 pending_task_select 阶段）。 */\n",
    "72": "   * 用户明确选择了\"跳过\"（本次不关联）。状态设为 initialized 防止重复弹窗，\n",
    "73": "   * 但 agentDetail/taskDetail 为 null，后续请求只 strip 不 inject。\n",
    "77": "   * codex 客户端专属分页页码（0-based），只在 agentSource=\"codex\" 场景写入。\n",
    "79": "   * 背景：codex 的 request_user_input 单 question 最多 3 个 option（客户端会\n",
    "80": "   * 自动追加\"其他\"占 1 位，proxy 不能自己塞）。当候选 > 3 时按 3-slot 上限\n",
    "81": "   * 分页：前 N-1 页各 2 real + \"更多...\"，末页 2~3 real。\n",
    "83": "   * 独立于 CC 侧的 `agentPageIndex` —— CC 有自己的 4-slot 分页语义 (agent /\n",
    "84": "   * task 共用一个字段，靠 stage 区分)，codex 需要 team / agent / task 三段\n",
    "85": "   * 独立页码, 复用会互串。\n",
    "87": "   * ── 谁写入 ──\n",
    "88": "   * CB 状态机在 agentSource===\"codex\" + reqCtx.codexAnswerInput 命中 MORE 时\n",
    "89": "   * bump 对应字段（团队规范 2026-08-08 codex session-init 重构：MORE 拦截从\n",
    "90": "   * codexHandler 收敛到 CB 状态机内部）。CC/普通 CB 场景永远不写。\n",
    "118": "  /** mem:session-reset 触发时写入，标记本次 init 是 reset 流程。 */\n",
    "120": "  /** mem:session-reset 触发的时间戳，跨节点一致性校验用。 */\n",
    "125": " * 来自控制面 `/api/v1/proxy/resources` 的嵌套结构：\n",
    "128": " * agents 和 tasks 是该 team 下的完整列表，平级展示。\n",
    "129": " * session init 时用户自由选择 agent + task，task_agents 关联关系\n",
    "130": " * 在 init 完成后由页面管理，不影响 init 时的选项列表。\n",
    "136": "   * 标识该条目是 `config.defaultTaskId` 兜底注入的虚拟 task（source: proxy）\n",
    "137": "   * 而非内核里真实存在的 task。form 侧看到该字段会跳过 `(id-suffix)` 的拼接，\n",
    "138": "   * 显示更干净的 label —— 反正虚拟 task 只有一个，不存在重名歧义。\n",
    "156": "/** @deprecated 旧扁平结构，保留以兼容旧测试；新代码用 TeamOption。 */\n",
    "164": "/** @deprecated 旧扁平结构，保留以兼容旧测试；新代码用 TaskInTeam。 */\n"
  },
  "MemoryProxy/src/session/workbuddy/extractor.ts": {
    "3": " * WorkBuddy 客户端复用 CC 的 `AskUserQuestion` tool 契约（同 tool name、同\n",
    "4": " * questions/answers 结构，抓包 [wb-ask-user-schema] 已实证）。因此用户答复\n",
    "5": " * 的 tool_result JSON 结构 与 CC **完全一致**。\n",
    "7": " * 本文件直接 re-export CC 的 extractor —— 保持\"WB 由独立模块承接\"的架构语\n",
    "8": " * 义，同时避免复制粘贴 400 行匹配逻辑；未来任一侧行为分叉，只需在此文件\n",
    "9": " * 加 wrapper 即可，无需回改上游。\n",
    "11": " * 注意：CC extractor 里的 `SKIP_LABEL / MORE_LABEL / ASSET_CONFIRM_*` 常量\n",
    "12": " * 是从 `./claude-code/form.js` 引入的。WB form.ts 里定义了同名常量，但\n",
    "13": " * 值完全一致（\"本次不关联（跳过注入，直接放行）\" / \"更多 →\" 等），因此\n",
    "14": " * extractor 用哪个都行；沿用 CC 的即可。\n"
  },
  "MemoryProxy/src/session/workbuddy/form.ts": {
    "3": " * WorkBuddy 客户端复用 CC 的 `AskUserQuestion` tool（抓包 [wb-ask-user-schema]\n",
    "4": " * 已实证），但底层协议是 **OpenAI /v1/chat/completions**（非 Anthropic）。\n",
    "6": " * 因此本 form：\n",
    "7": " *   - questions[] shape 与 CC 完全一致（{question, header, options:[{label,description}], multiSelect}）\n",
    "8": " *   - 传输侧走 OpenAI chat/completions SSE `tool_calls` chunk 流（CB 那套骨架）\n",
    "9": " *   - Tool name: `AskUserQuestion`（同 CC）\n",
    "10": " *   - ID prefix: `call_wb_session_init_`（区分 CB 的 `call_session_init_`）\n",
    "11": " *   - 分页: 每页 3 option + 1 个\"更多→\"槽位（对齐 CC，避免超过硬上限）\n",
    "13": " * 不含任何 CodeBuddy XML 逻辑；也不共用 CB 的 form builder（CB 用\n",
    "14": " * `ask_followup_question` XML 语义，WB 用 CC 的 AskUserQuestion 语义）。\n",
    "37": " * 附在每步 question 文末的通用备注：告诉用户\"选择跳过 = 本次 session init 跳过、不注入任何团队资产\"。\n",
    "38": " * AskUserQuestion 会给用户一个 \"Other\" 输入框，回复\"跳过 / skip / 不关联\" 就\n",
    "39": " * 走 SKIP_RE bypass；文案与 claude-code/codex/codebuddy/dsh 五端统一。\n",
    "69": "  /** 分页：当前页码 (0-based)；对齐 CC 只使用一个 pageIndex（team/agent/task 单题） */\n",
    "76": "// ── AskUserQuestion input schema (与 CC 完全一致) ──────────────────────────────\n",
    "92": "      question: titlePrefix + \"本次对话是否要关联团队资产？\" + SKIP_HINT,\n",
    "93": "      header: \"关联资产\",\n",
    "95": "        { label: ASSET_CONFIRM_YES, description: \"选择 Team / Agent / Task，注入团队上下文\" },\n",
    "96": "        { label: ASSET_CONFIRM_NO, description: \"本次不注入任何内容，直接放行\" },\n",
    "115": "      question: titlePrefix + \"请选择本次会话所属的 Team：\" + SKIP_HINT,\n",
    "138": "      combinedOptions.push({ label: MORE_LABEL, description: `查看下一批（还剩 ${remaining} 个 Agent）` });\n",
    "148": "    const pageSuffix = page.totalPages > 1 ? `（第 ${pageIndex + 1}/${page.totalPages} 页）` : \"\";\n",
    "150": "      question: titlePrefix + `请选择「${team.team_name}」下要使用的 Agent${pageSuffix}：` + SKIP_HINT,\n",
    "174": "        description: `查看下一批（还剩 ${remaining} 个任务）`,\n",
    "185": "    const taskPageSuffix = page.totalPages > 1 ? `（第 ${taskPageIndex + 1}/${page.totalPages} 页）` : \"\";\n",
    "187": "      question: titlePrefix + `请选择「${team.team_name}」下要关联的任务${taskPageSuffix}：` + SKIP_HINT,\n",
    "203": " * 传输：**OpenAI chat/completions**（stream 或 non-stream）。\n",
    "204": " * questions shape：同 CC AskUserQuestion —— `{questions: [{question, header, options, multiSelect}]}`。\n"
  },
  "MemoryProxy/src/session/workbuddy/index.ts": {
    "1": " * WorkBuddy session-init 层统一 export。\n",
    "3": " * ⚠️ 独立性铁律：本 barrel 只对外暴露 workbuddy 自己的类型与入口；不再re-export\n",
    "4": " * 其他 client 的 session-init 内容。caller（workbuddyHandler）从此处一次拿到\n",
    "5": " * 所有必要接口。\n"
  },
  "MemoryProxy/src/session/workbuddy/init.ts": {
    "1": " * WorkBuddy session-init —— **header-preselect-only** 独立实现。\n",
    "3": " * ⚠️ 独立性铁律：本文件不 import 任何 sibling handler / sibling session 模块\n",
    "4": " * (session/codebuddy/*, session/codex/*, session/claude-code/*)。只依赖\n",
    "5": " * **多 client 共用的通用基础设施**：\n",
    "6": " *   - session/store.ts        （通用 KV store）\n",
    "7": " *   - session/preset.ts       （header解析 + 校验，client-agnostic）\n",
    "8": " *   - session/registrar.ts    （SessionInfo 构造）\n",
    "9": " *   - session/context-injector.ts（session_context XML 生成）\n",
    "10": " *   - session/types.ts（通用类型）\n",
    "11": " *   - meta/client.ts          （TDAI kernel API客户端）\n",
    "14": " * WorkBuddy 客户端语义\n",
    "16": " * 抓包结论（详见 docs/workbuddy-recon/）：\n",
    "17": " *   - Wire protocol：OpenAI Responses API（同 codex 底层协议）\n",
    "18": " *   - **系统提示词不含 request_user_input 类 form 工具**（真实抓包证实）\n",
    "19": " *   - 客户端无法弹交互式选择表单让用户选 team/agent/task\n",
    "21": " * 因此本 session-init 的行为退化为纯**入口守门**：\n",
    "22": " *   - 请求 header 含完整 team+agent+task ID → 校验后直接注册 → 继续注入\n",
    "23": " *   - 缺任一 → **静默 bypass**（透传上游，不注入资产）\n",
    "24": " *   - 已有 session state（同 sessionKey 复用）→ 走recovered 快通道\n",
    "26": " * 与 CB/codex/CC 三家 session-init 状态机的**关键差异**：\n",
    "27": " *   1. 不返回 `intercepted=true`（本客户端不能接收 form response）\n",
    "28": " *   2. 不做 Default gate 检测（客户端本身不发 gate 信号）\n",
    "29": " *   3. 不做 MORE 分页（不需要交互）\n",
    "30": " *   4. 不做 form 拦截（客户端无 form 工具）\n",
    "54": " * WorkBuddy session-init 结果。与 CB/CC/codex 三家的 SessionInitResult 语义\n",
    "55": " * 对齐（sessionInfo/agentDetail/taskDetail/systemAppend/bypassed 字段名一致），\n",
    "56": " * 但**独立类型**避免跨 handler 类型共享。\n",
    "60": "   * 会话是否已成功注册。false 时caller 应跳过 injection、直接透传上游。\n",
    "63": "  /** 注册成功时的 SessionInfo；bypass 分支为 null。 */\n",
    "65": "  /** Agent 详情（含 prompt / persona）；bypass 分支为 null。 */\n",
    "67": "  /** Task 详情（含 description / goal）；bypass 分支为 null。 */\n",
    "70": "   * 预构造的 `<session_context>` block；供injection 阶段合成 body 时预填\n",
    "71": "   * system message。bypass 分支为 null。\n",
    "75": "   * 本轮是否**新注册**（recovered 快通道复用 = false, 首次注册 = true）。\n",
    "76": "   * 用于决定是否触发 injection prewarm。\n",
    "80": "   * 走 bypass 时的原因，用于日志/埋点分析：\n",
    "81": "   *   - \"no-header\"：请求未带 x-tdai-team-id 头（客户端无 form 兜底 → 直接绕过）\n",
    "82": "   *   - \"incomplete-header\"：header 有 team 但缺 agent或 task\n",
    "83": "   *   - \"mismatch\"：header值与 kernel 返回的 team/agent 列表不一致\n",
    "84": "   *   - \"kernel-error\"：调用 kernel API 失败\n",
    "85": "   *   - \"config-disabled\"：sessionInit.enabled=false 或 headerAutoSelect.enabled=false\n",
    "98": " * 调用 handleWorkbuddySessionInit 时透传的运行时上下文（与请求无关的参数从\n",
    "99": " * 独立参数走）。\n",
    "102": "  /** SSE stream 标志——供未来 form 场景使用；本 handler 当前不构造 form 响应。 */\n",
    "104": "  /** 客户端请求的模型 ID；透传给日志/埋点。 */\n",
    "111": " * WorkBuddy session-init 入口。\n",
    "113": " * 流程（详见 module doc 顶部）：\n",
    "115": " *   2. 从 store 复用已有 state → 直接 recovered 快通道\n",
    "116": " *   2.5 **DEBUG**：`sessionInit.debugForceIdentity` 齐全（team+agent+task）→\n",
    "117": " *      跳过 header 与 kernel listTeams 校验，直接用固定三元组注册（本地/e2e 专用）\n",
    "118": " *   3. 解析 preset identity（三header）：\n",
    "119": " *      - 无 team header → bypass(no-header)\n",
    "120": " *      - 有 team header 但缺 agent 或 task → bypass(incomplete-header)\n",
    "121": " *      - team+agent+task 齐全 → 拉 kernel team list 校验\n",
    "122": " *        - resolvePresetIdentity.canRegister=true → 完成注册\n",
    "123": " *        - 校验失败 → bypass(mismatch)\n",
    "124": " *   4. 注册成功后落 store（下一轮走 recovered 分支）\n",
    "126": " * @param sessionKey     去重键（一般是客户端 session_id 或 fallback keyId:traceId）\n",
    "127": " * @param userId         apiKey 解析出的用户 ID（可为 null）\n",
    "128": " * @param config         SessionInitConfig（含 headerAutoSelect 配置）\n",
    "129": " * @param store          通用 SessionStore（多 client 共用，`workbuddy:` 前缀隔离）\n",
    "130": " * @param reqCtx         请求运行时上下文\n",
    "131": " * @param headers        小写化后的请求头 map\n",
    "132": " * @param agentSource    固定为 \"workbuddy\"（compositeKey 前缀）\n",
    "133": " * @param metadataClient TDAI kernel 客户端；bypass 分支可传 undefined\n",
    "134": " * @param userKey        原始 apiKey，落 SessionInfo.user_key\n",
    "135": " * @param spaceId        URL path 里的 spaceId\n",
    "149": "  void reqCtx; // 保留供未来扩展（form / MORE 分页时会用到 stream/modelId）\n",
    "151": "  // ── 1. 配置门控 ────────────────────────────────────────────────────────────\n",
    "164": "  // ── 2. 复用已有 state（recovered 快通道）─────────────────────────────────\n",
    "165": "  // getOrRecover 内部自动 bind 身份，无需另外调store.bind\n",
    "168": "    messages: [], // WorkBuddy 无form → 不需要历史扫描回收\n",
    "196": "  // ── 2.5 DEBUG BYPASS：debugForceIdentity 强制注入（本地开发 / e2e）─────────\n",
    "197": "  // 目的：绕过 header 解析 + kernel listTeams 校验，用配置里指定的固定三元组\n",
    "198": "  //       (team_id, agent_id, task_id) 直接完成注册。仅用于本地跑通/联调。\n",
    "199": "  // 语义与 CC 版对齐：需要 team+agent+task 三者齐全才能走这条路（缺任一即忽略\n",
    "200": "  // debug 配置、退回正常 preset 流程），保证 injection 需要的 task 一定存在。\n",
    "207": "    // 提到局部常量，让 TS 把 task_id 收窄成 string（原类型是 string | undefined）\n",
    "217": "    // 尝试补 agent/task detail（失败降级为空）—— 与主路径 5. 段落一致\n",
    "289": "  // ── 3. 解析 preset identity ────────────────────────────────────────────────\n",
    "292": "    // 无 team header → WorkBuddy 客户端无 form 兜底 → 直接 bypass\n",
    "297": "    // team 齐了但缺 agent 或 task → 无法完整注册，直接 bypass\n",
    "298": "    // （resolvePresetIdentity 也会返回 canRegister=false，此处提前 return 减少 kernel 调用）\n",
    "303": "  // ── 4. 拉 kernel team list 校验 preset ────────────────────────────────────\n",
    "305": "    // 没有 kernel 客户端（config 未配置 tdai.endpoint / apiKey 不完整）→ bypass\n",
    "306": "    // 不落 store bypass —— 配置修复后可自动恢复\n",
    "310": "  // 通过 kernel 拉取当前用户可见的 team 列表，并fan-out 补齐 agents / tasks\n",
    "311": "  // （resolvePresetIdentity 需要 TeamOption[] 结构）\n",
    "339": "    // 不落 store bypass —— kernel 错误可能是临时的，下一轮请求可以重试\n",
    "345": "    // 校验失败：header 值与用户可见 team/agent/task 不一致 → 长期 bypass\n",
    "346": "    // （客户端下一轮请求还是会带同一 header，重试无益）\n",
    "351": "  // ── 5. 拉 agent / task detail ─────────────────────────────────────────────\n",
    "384": "  // ── 6. 构造 SessionInfo & 落 store ────────────────────────────────────────\n",
    "444": " * 把 bypass 决定落进 SessionStore（terminal `initialized` + `bypassed=true`），\n",
    "445": " * 下一轮请求会走 recovered 快通道直接返回 bypass。\n",
    "447": " * bypassReason 不落 store（SessionInitState 无此字段）；仅在本 handler\n",
    "448": " * return value 侧携带。\n"
  },
  "MemoryProxy/src/skill/core-client.ts": {
    "4": " *   - POST /v3/skill/search              → SkillInjector RAG 检索.\n",
    "6": " *   - POST /v3/skill/conversation/add    → handler-glue 新链路, 每轮真人对话结束推送.\n",
    "7": " *   - POST /v3/skill/extract 等其他方法保留在类里, 供 agent 通过 skill-bridge\n",
    "8": " *     反代时透传使用 (agent 通过 curl 直接命中, 不由 proxy 主动触发)。\n",
    "32": " * 身份字段。\n",
    "34": " * 重要：`user_id` 是 **可选** 的——skill 表上的 `user_id` 列只是 audit\n",
    "35": " * （\"上一次写入的 caller 是谁\"），而非 ownership。skill 的归属维度由 schema\n",
    "36": " * 的唯一索引 `(team_id, owner_agent_id, name)` 决定，与 user_id 无关。\n",
    "38": " * 因此 read 路径（search / list）**不应该** 传 user_id 过滤，否则会把所有\n",
    "39": " * \"非当前 caller 写入\"的 skill 全部毙掉（包括同 team / 同 agent 共享的 skill）。\n",
    "40": " * write 路径（extract / save / update）才需要 user_id 用于审计。\n",
    "42": " * Plugin 端 store 已经是 \"传啥过滤啥\" 的语义（`if (opts.user_id) WHERE user_id=?`），\n",
    "43": " * 这里把字段改成可选即可让 read 路径自然跳过该过滤。\n",
    "46": "  /** 可选 — 仅 audit 用。read 路径不要传，否则会过滤掉团队共享 skill。 */\n",
    "90": " * `/v3/skill/conversation/add` 请求体里单条消息形状。\n",
    "92": " * 相较于 `ExtractMessage`：\n",
    "93": " *   - 允许 `system` role（跟设计 §11.1 的 5 种 role 对齐）\n",
    "94": " *   - `tool_call` / `tool_result` 必须携带 `tool_name` + `tool_call_id`\n",
    "95": " *   - `timestamp` 可以是数字（ms epoch）或 ISO 8601 字符串\n",
    "121": " * `/v3/skill/conversation/add` 输入。\n",
    "123": " * 强约束：\n",
    "124": " *   - session_id / space_id / user_id / team_id / agent_id 全部必填\n",
    "125": " *   - ID 字段不能包含 `|`（Core 拒绝，返回 400）\n",
    "126": " *   - messages 是本轮增量（user + 中间 tool_call/tool_result + assistant 总结），\n",
    "127": " *     不重传历史（Core 不去重，重传会造成 buffer 重复）\n",
    "128": " *   - 同 session 必须严格串行（一轮 200 之后才发下一轮）\n",
    "130": " * 详见 `2026-07-15-skill-trigger-in-core-design.md` §11.1 & §13。\n",
    "139": " * 归档触发时的元数据；`status: \"ok\"` 时不带此字段。\n",
    "153": "/** Input for /v3/skill/conversation/force-archive — 手动强制归档。 */\n",
    "176": " * Used by skill-bridge team-search to build the \"agent 自有全量\" 部分的\n",
    "177": " * whitelist（见 `docs/design/2026-08-10-skill-search-scope-fix.md` §4）。\n",
    "178": " * limit 走 core schema 上限 1000，一次拿完，不分页。\n",
    "268": "   * `POST /v3/skill/conversation/add` — 新链路：每轮增量推送到 core，\n",
    "269": "   * core 内部决定归档 + 抽取时机。见 §21.2。\n",
    "271": "   * **同步等待**：本方法内部 `await` fetch → envelope 解析。调用方必须\n",
    "272": "   * 也 `await` 本方法，保证同 session 严格串行（Core 侧的核心前提）。\n",
    "282": "   * `POST /v3/skill/conversation/force-archive` — 手动强制归档当前 session buffer。\n",
    "283": "   * 跳过阈值判断，直接调 trigger.archive()。\n",
    "293": "   * `POST /v3/skill/list` — 枚举 agent 自有 skill 的 skill_id / 元数据。\n",
    "295": "   * skill-bridge team-search 用来扩大 whitelist：把 agent 自己（含 private）\n",
    "296": "   * 的 skill 也纳入检索池，避免 session-init `<available_skills>` 20 条上限\n",
    "297": "   * 之外的私有 skill 永远搜不到。见\n",
    "300": "   * 语义：默认只返回 head + active。owner 归属由 (team_id, agent_id) 决定。\n",
    "323": "   * plugin 端 Zod schema 要求 team_id 和 agent_id 互绑：\n",
    "324": "   * 要么都传（有值），要么都不传（undefined/空）。\n",
    "325": "   * 如果 agent_id 为空但 team_id 有值，触发 \"must both be provided or both be omitted\"。\n",
    "327": "   * 修复策略：当 team_id 有值而 agent_id 为空时，填充 \"default\" 作为 agent_id。\n",
    "328": "   * plugin core 层本身也会对 undefined agent_id fallback 到 \"default\"（见 skill-core.ts）。\n",
    "336": "    // 如果两个都为空，清理 key（不带到 plugin 端）\n",
    "349": "    // 浅拷贝 body，避免 normalizeTeamAgent 副作用污染调用者传入的对象。\n",
    "350": "    // 原先直接修改 body 会导致调用方的输入对象被意外改写（例如 agent_id\n",
    "351": "    // 被填入 \"default\"），引发跨调用或重试时的数据错乱。\n"
  },
  "MemoryProxy/src/skill/handler-glue.ts": {
    "4": " * proxy 只走**新链路**: 每次真人对话 round 结束 (agent 给出无 tool_use 的\n",
    "5": " * 最终回复) 时, 把本 round 的 conversation 切片 push 给 core, core 累积\n",
    "6": " * 到阈值时自己决定何时归档 (进 skill 抽取管线)。\n",
    "8": " * 历史: 曾经存在老链路 SkillExtractTrigger → /v3/skill/extract, 由 proxy\n",
    "9": " * 每 turn 触发一次抽取, 依赖 proxy 侧 KvExtractStore 存 buffer。已删除,\n",
    "10": " * 详见 commit 历史。agent 主动通过 skill-bridge 触发抽取的入口 (/v3/skill/extract)\n",
    "11": " * 也一起注释掉了 —— core 侧规划中会出一个\"手动归档\"接口, 到时候 agent 工具\n",
    "12": " * 会重新指向那个接口。\n",
    "14": " * 触发时机: **round-level** —— 只有 agent 给出最终回复（无 tool_use /\n",
    "15": " * tool_calls）时才 push 到 core。中间态（agent 在调工具，客户端还会带\n",
    "16": " * tool_result 回来继续这一轮）直接跳过，等 round 真正结束再一起发。\n",
    "18": " * 为什么不是 turn-level (每次 HTTP 都发):\n",
    "19": " *   proxy 视角一次 HTTP 是一\"turn\"，但 Claude Code / CodeBuddy 一次真人\n",
    "20": " *   提问会引发 N 次 HTTP (tool-use 循环)。如果每 turn 都发, core buffer\n",
    "21": " *   累计极快 —— 默认 10 个 tool_use 就触发一次归档, 生产环境几轮对话\n",
    "22": " *   就能打出 30+ 归档。round-level 触发保证 \"1 次真人问答 = 1 次 add\",\n",
    "23": " *   语义清晰, RPC 数下降 ~N 倍。详见\n",
    "37": "/** loose message shape 供本模块内部用 */\n",
    "47": "  /** Client type (URL path 第一段)，用于三段隔离键；缺省 `claude-code`。 */\n",
    "53": "   * 请求走的协议 —— 决定 messages/assistantMessage 的解析规则。\n",
    "54": "   *   \"anthropic\" → anthropicHandler.ts (/v1/messages), content 可能是 blocks 数组,\n",
    "55": "   *                 tool_result 藏在 role=user 里, tool_use 藏在 role=assistant 里\n",
    "56": "   *   \"openai\"    → handler.ts (/v1/chat/completions), content 一般是字符串,\n",
    "57": "   *                 tool_result 是独立 role=tool 消息, tool_calls 是 assistant 独立字段\n",
    "58": "   *   \"responses\" → codexHandler.ts (/responses), 顶层是 input[] 而非 messages[],\n",
    "59": "   *                 item 按 type 判别: message / function_call / function_call_output /\n",
    "60": "   *                 reasoning (详见 normalize-conversation.ts 顶部)\n",
    "62": "   * proxy 已经通过路由 whitelist 明确知道每条请求走哪个协议（见 routes/whitelist.ts）,\n",
    "63": "   * 所以由调用方 (handler.ts / anthropicHandler.ts / codexHandler.ts) 显式传入, 不做推断。\n",
    "100": "    // ── round-level 触发 gate ──\n",
    "101": "    // 只有 final answer 才继续；含 tool_use / tool_calls 的中间态直接返回。\n",
    "102": "    // 判定依赖 assistantMessage 里 tool_use / tool_calls 数量;\n",
    "103": "    // stream 分支 assistantMessage.content 被拉平成 string 丢了 blocks 信息,\n",
    "104": "    // 此时 input.toolCallCountOverride 才是权威 (见 anthropicHandler.ts:1592-1597).\n",
    "107": "    // ── 本 round 切片 ──\n",
    "108": "    // slice 起点 = 上一次 \"final assistant\" 之后 = 本 round 的 user 输入起点。\n",
    "109": "    // 找不到 (首轮 / 历史全是中间态) → -1 + 1 = 0, 天然发整段, 语义正确。\n",
    "113": "    // 按协议分支做 5-role 规范化 —— 展开 anthropic content blocks / openai tool_calls,\n",
    "114": "    // 识别 tool_use / tool_result 分别转成 role=tool_call / role=tool_result。\n",
    "115": "    // agentSource 用于按客户端规则提取 user text (详见 agent-adapters/)\n",
    "116": "    // 详见 normalize-conversation.ts 和 docs/design/2026-07-17-conversation-normalize.md\n",
    "138": "        // core Shark 走 x-tdai-service-id = 真实内核实例 ID\n",
    "148": "        // status=ok: 未触发归档 —— core 已把本 round 累计到 buffer, 等下次\n",
    "149": "        // 或再几次 round 累够阈值才会归档。\n",
    "157": "      // core 返回失败不影响主响应链; 观察指标看看再决定升级为 error\n",
    "164": "    // 保守：任何异常都吞掉，避免影响主响应链。\n",
    "172": "// 兼容: 部分老代码从 handler-glue 里 import countToolCalls\n"
  },
  "MemoryProxy/src/skill/kv-version-pin-repo.ts": {
    "1": " * KvVersionPinRepo —— skill 版本锁 (ProxyStorage-backed).\n",
    "3": " * 见 docs/design/2026-07-12-cos-shark-sts-credential-plan.md §3.2 §3.6。\n",
    "5": " * Key 路径：\n",
    "8": " * spaceId 是 P4 (kernel-sts) 新增的隔离段。老 caller 传空字符串时用 `_default` 兜底。\n",
    "10": " * 关键设计 vs 原 Redis 版本：\n",
    "11": " *   1. **拆 key**：从 map-in-one-object 改成 \"一 skill 一对象\"，消除 R-M-W 写放大\n",
    "12": " *      与并发覆盖风险\n",
    "13": " *   2. **CAS 语义**：`pinMany` 走 `putTextIfAbsent`（COS If-None-Match / SQLite\n",
    "14": " *      INSERT OR IGNORE / Fs O_EXCL / Memory Map.has），首次写入权威，\n",
    "15": " *      **无需依赖 sticky routing**\n",
    "17": " * `upsertVersion` 是覆盖写（对应 post-write 场景，plugin 返回 v+1）。\n",
    "67": "   * 首次访问快照 —— 每对 (skillId, version) 走 putTextIfAbsent，天然是 HSETNX 语义。\n",
    "68": "   * 静默降级：任一写失败不抛，与原 Redis 版本一致。\n",
    "90": "  /** 写操作后的强制覆盖 —— 覆写不需要 CAS。 */\n"
  },
  "MemoryProxy/src/skill/normalize-conversation.ts": {
    "3": " * 把 client 原始请求的 messages[] + upstream 返回的 assistantMessage 转换成\n",
    "4": " * core `/v3/skill/conversation/add` 需要的 5-role 规范化数组:\n",
    "10": " * 明确按协议分支:\n",
    "12": " *     - user 里 content 可能是 blocks 数组, tool_result 藏在 role=user 的 blocks 里\n",
    "13": " *     - assistant 里 tool_use 藏在 content blocks 里\n",
    "15": " *     - tool_result 是独立 role=tool 消息 (只有 tool_call_id, 没有 tool_name)\n",
    "16": " *     - assistant.tool_calls 是独立数组字段\n",
    "18": " *     - 顶层字段是 `input[]` 而非 `messages[]`, item 由 `type` 判别:\n",
    "20": " *         · type=\"function_call\" → 独立顶层项, 含 call_id / name / arguments\n",
    "21": " *         · type=\"function_call_output\" → 独立顶层项, 含 call_id / output\n",
    "22": " *         · type=\"reasoning\" → 内部思考, 一律丢弃 (跟 anthropic thinking 对齐)\n",
    "23": " *     - developer message 等同 openai role=system, 丢弃 (不进 skill 语料)\n",
    "24": " *     - assistant 侧 tool 调用不是 embedded 到 message.content, 而是独立 item, 因此\n",
    "25": " *       \"本轮 assistantMessage\" 抽出来时可能包含前面若干 function_call/function_call_output\n",
    "26": " *       跟 text 混在一起 —— caller 传 assistantMessage 时只放\"响应的 assistant text 段\n",
    "27": " *       + 本轮 stream 累积的 tool_use count override\", 已存在的 input[] 里的 function_call\n",
    "28": " *       仍由 normalizeConversation 遍历时处理。\n",
    "30": " * 关键规则 (对应 docs/design/2026-07-17-conversation-normalize.md):\n",
    "31": " *   - Anthropic user 里全是 tool_result blocks 时 → 输出 role=tool_result, 不输出 user\n",
    "32": " *   - Anthropic user 里 text + tool_result 混合 → 拆成两条 (role=user, role=tool_result)\n",
    "33": " *   - Anthropic assistant 里 thinking block → 目前**完全丢弃**\n",
    "34": " *     TODO: 后续如果需要保留 thinking, 加个 `keepThinking: boolean` 参数,\n",
    "35": " *     保留形式为 role=assistant + content 前缀 [thinking] xxx (对齐 Opik flatten)\n",
    "36": " *   - Anthropic image block → 丢弃 (对 skill 抽取无价值)\n",
    "37": " *   - Anthropic assistant content 数组为空 → 该消息整个丢弃\n",
    "38": " *   - OpenAI role=tool 消息 → tool_name 保持 undefined (core schema 已放宽为 optional)\n",
    "40": " * assistantMessage 是本轮 upstream 返回的响应, 已被 proxy 拆出来单独传:\n",
    "41": " *   - anthropic 形态: { role: \"assistant\", content: <blocks 数组> }\n",
    "42": " *   - openai 形态:    { role: \"assistant\", content: string | null, tool_calls?: [...] }\n",
    "44": " * 该模块不做\"本轮切片\"—— 调用方要发全历史还是本轮增量由 handler-glue 决定, 这里只做格式规范化。\n",
    "46": " * 除 normalizer 外, 本文件还导出两个 round 边界判定 helper:\n",
    "48": " *       判本次响应是不是 agent 的最终回复 (无 tool_use / tool_calls)\n",
    "50": " *       在 messages[] 里找上一次 final assistant 的 index (round 起点定位)\n",
    "52": " * 这两个 helper 由 handler-glue 用来实现 round-level 触发: 只有 final answer\n",
    "53": " * 时才 push 到 core, 中间态跳过, 避免\"每次 HTTP 都发一次增量\"导致 core buffer\n",
    "54": " * 累计爆炸 (10 次 tool_use 或 40KB 就触发一次归档)。\n",
    "63": "/** 输出格式, 对应 core conversationMessageSchema。 */\n",
    "71": "/** raw message shape - loose 因为三种协议的原始形态都要能塞进来。 */\n",
    "78": "   * codex Responses API input[] 的判别字段:\n",
    "80": "   * anthropic/openai 的 message 里无此字段。\n",
    "89": " * 数 assistant 响应里 tool_use / tool_calls block 的个数, 用于 isFinalAnswer\n",
    "90": " * / round 边界判定。跨协议兼容:\n",
    "91": " *   - OpenAI: 顶层 `tool_calls: [...]` 数组长度\n",
    "92": " *   - Anthropic: content blocks 里 `type === \"tool_use\"` 的数量\n",
    "93": " * 其他形态一律 0 (纯 text 或空响应)。\n",
    "107": "      // Codex Responses API: assistant \"message\" 只含 output_text; function_call\n",
    "108": "      // 是 sibling item, 不在 assistant.content 里出现。所以 responses 场景\n",
    "109": "      // countToolCalls(assistantMessage) 恒 0 —— 真正的 tool call 数由 stream\n",
    "110": "      // accumulator 的 toolCallCountOverride 传入 (跟 anthropic stream 一致)。\n",
    "118": " * 判本次 assistantMessage 是不是 agent 的最终回复 (round 结束).\n",
    "120": " * 判定标准：**没有 tool_use / tool_calls** —— 有 text 无所谓，因为可能出现\n",
    "121": " * \"text + tool_use 混合\"的中间态（agent 边说话边调工具）。这种情况下 round\n",
    "122": " * 还没结束，text 部分会作为历史保留到 messages[]，下次真正 final 时再一起\n",
    "123": " * slice 进本 round。\n",
    "125": " * 两个协议的区分:\n",
    "126": " *   - anthropic non-stream: 直接看 asst.content blocks 里有没有 tool_use\n",
    "127": " *   - anthropic stream:     content 被 proxy 拉平成 string 了（丢了 blocks 信息），\n",
    "128": " *                           调用方必须传 toolCallCountOverride\n",
    "129": " *   - openai:               看 asst.tool_calls[] 长度\n",
    "131": " * 复用本文件 countToolCalls 处理协议差异, 这里只加 override 优先级.\n",
    "138": "  // stream 场景: override 是权威来源, content 已丢 blocks 结构不能靠它判\n",
    "146": " * 找 messages[] 里最近一条\"最终回复形态\"的 assistant index —— 定义为\n",
    "147": " * role=assistant 且响应内没有 tool_use / tool_calls。返回 -1 表示历史里全是\n",
    "148": " * 中间态 / 没有 assistant, 调用方应从 index 0 开始 slice。\n",
    "150": " * 用途: 在 handler-glue 里定位本 round 起点 —— 从\"上一次 final assistant\"\n",
    "151": " * 之后 slice, 就是本 round 完整对话 (user + 中间 tool_use/tool_result... +\n",
    "152": " * 本次 assistantMessage)。\n",
    "154": " * 协议差异:\n",
    "155": " *   - anthropic: 历史 messages 里 assistant.content 可能是 string (纯 text final)\n",
    "156": " *                或 blocks 数组 (混合)。只要 blocks 里没 tool_use 就算 final。\n",
    "157": " *   - openai:    历史 messages 里 assistant 可能有 tool_calls 字段。只要\n",
    "158": " *                tool_calls 为空/缺失就算 final。\n",
    "165": "    // Codex `input[]` 里 assistant 用 `{type:\"message\", role:\"assistant\"}`;\n",
    "166": "    // 它的 tool 调用是 sibling item `{type:\"function_call\", ...}` 而非\n",
    "167": "    // 内嵌 block。判 \"本条 assistant 是 final\" 的规则：从该 index 往后扫,\n",
    "168": "    // 只要在遇到下一条 role=user 的 message 之前**没有** function_call\n",
    "169": "    // (以及配套的 function_call_output) 出现, 就算 final answer, 本轮结束。\n",
    "173": "      // 扫 tail 看有没有 function_call\n",
    "179": "        if (next.type === \"message\" && next.role === \"user\") break;   // 到了下一轮\n",
    "189": "      // isFinalAnswer 对两种协议都靠 countToolCalls 判定, 覆盖:\n",
    "190": "      //   anthropic: content 是 string → tool_use=0 → final\n",
    "191": "      //              content 是 blocks 且无 tool_use → tool_use=0 → final\n",
    "192": "      //   openai:    tool_calls=[] 或缺失 → tool_use=0 → final\n",
    "193": "      // (protocol 参数保留是为将来扩展更细粒度的判定, 目前不用)\n",
    "208": "  // 兜底: protocol=responses 但 agentSource 默认为 claude-code (老调用方没传)\n",
    "209": "  // → 强制切到 codex adapter, 否则 extractUserText 会用错客户端规则返 null,\n",
    "210": "  // 归档链路整个断掉。\n",
    "224": "    // 对 responses 协议, upstream 只回 assistant text (function_call 在流里\n",
    "225": "    // 单独识别并已进入 input[] 下一轮请求, 归档时通过 rawMessages 遍历覆盖),\n",
    "226": "    // 所以这里只补一条 role=assistant + text —— 复用 convertCodexInputItem\n",
    "227": "    // 走同一路径, 保持与另外两协议对称。\n",
    "231": "    // 保险: 显式设 role=assistant 以防调用方传入的 assistantMessage 缺 role;\n",
    "232": "    // convertOne 依赖 effectiveAgentSource, 上面已覆盖 protocol=responses 兜底。\n",
    "245": "  // role=system: 丢弃, 不发给 core。\n",
    "246": "  // 客户端固定 agent instruction (CodeBuddy 26KB / Claude Code 也不小) 跟\n",
    "247": "  // skill 抽取无关, 计入 40KB bytes 阈值只会让归档节奏乱套。两侧协议统一处理:\n",
    "248": "  //   - Anthropic: system 本来就在 body.system 顶层, messages 里出现是少数场景\n",
    "249": "  //   - OpenAI:    system 在 messages[0], 每次 request 都带一份\n",
    "250": "  // 详见 handler-glue.ts 上游 normalize 调用点。\n",
    "263": "  // 其它 role 一律 drop\n",
    "268": "  // content 可能是 string / array of blocks\n",
    "273": "    // 兜底: 结构化对象但不是数组 → 序列化\n",
    "290": "      // 转成 role=tool_call, tool_name 从 name, tool_call_id 从 id\n",
    "302": "    // 其它 block type (thinking / redacted_thinking / image / ...) 一律丢弃\n",
    "323": "  // 收集所有 tool_result（单独输出成 role=tool_result 消息）\n",
    "329": "    // tool_result.content 可能是 string / array of {type:\"text\",text} / other\n",
    "335": "      // 注意: anthropic tool_result 没有 tool_name 字段, proxy 也不做反查\n",
    "336": "      // (core schema 已放宽为 optional)\n",
    "340": "  // text 部分：通过 agentAdapter 按客户端规则提取\"用户真正键入的文本\"：\n",
    "341": "  //   - claude-code: 取最后一个 text block（跳过 <system-reminder> 前缀元数据）\n",
    "342": "  //   - codebuddy / unknown: 走保守的\"拼接所有 text\"（等价改造前老逻辑）\n",
    "362": "      // 忽略 image 等其它 block type\n",
    "375": "  // role=system: 丢弃, 不发给 core。跟 anthropic 分支一致, 详见那边注释。\n",
    "380": "    // 通过 adapter 按客户端规则提取\"用户真正键入的文本\"，跟 anthropic 侧对称：\n",
    "381": "    //   - codebuddy: 走 <user_query> 抽取（剥掉 <user_info> / <additional_data> /\n",
    "382": "    //     <question_answer> 等 CB pseudo-XML wrapper），返回 null 时该轮不进\n",
    "383": "    //     skill buffer（避免污染 skill 抽取语料）\n",
    "384": "    //   - unknown / cursor 等: 走 default 兜底（拼接所有 text，等价改造前老逻辑）\n",
    "385": "    // 详见 agent-adapters/codebuddy.ts + common/user-query-extractor.ts。\n",
    "400": "      // tool_name 未提供 —— core schema 已 optional\n",
    "446": " * 把 codex `input[]` 的一个 item 翻译成 5-role NormalizedMessage[]。\n",
    "448": " * item 类型清单 (docs/2026-08-07-codex-integration-plan.md §7.2 / agent-adapters/codex.ts):\n",
    "454": " * 映射:\n",
    "455": " *   message.developer         → 丢弃 (等同 openai role=system, 不进 skill 语料)\n",
    "456": " *   message.user              → role=user, content = 用户真键入文本 (走 codex adapter)\n",
    "457": " *   message.assistant         → role=assistant, content = output_text 拼接\n",
    "460": " *   reasoning                 → 丢弃 (跟 anthropic thinking 对齐; 不进 skill 语料)\n",
    "461": " *   其它未知 type              → 丢弃 (偏严不偏宽, 未来 codex 加新 item type 也不会误采)\n",
    "468": "    // developer / system / 其它 role 一律丢\n",
    "503": "  // reasoning / 其它一律 drop\n",
    "507": "/** codex user message 里 content 是 [{type:\"input_text\",text}, ...] */\n",
    "509": "  // 优先复用 codex agent adapter 的 extractUserText —— 它跟 codexHandler\n",
    "510": "  // 里 `extractCodexUserText(input)` 是同一份实现 (agent-adapters/codex.ts:81),\n",
    "511": "  // 语义一致, 未来任何抽取规则升级只需改那一份。这里传单条 message 是包一层\n",
    "512": "  // adapter 期望的 [message] 数组即可 (它按 role=user 过滤)。\n",
    "513": "  //   注: adapter.extractUserText 期望的入参是完整 input[] 数组, 但内部只按\n",
    "514": "  //   条 iterate 挑最后一条 user; 传单条也能命中。\n",
    "524": "/** codex assistant message 里 content 是 [{type:\"output_text\",text}, ...] */\n",
    "534": "    // codex assistant text 用 output_text; 兜底 text 万一 codex 用了别的写法\n"
  },
  "MemoryProxy/src/skill/skill-bridge.ts": {
    "25": "// getSkillExtractTrigger / KvExtractStore 已随老链路一起删除。\n",
    "26": "// 详见 handler-glue.ts 顶部注释 —— skill_extract 触发路径当前不可用,\n",
    "27": "// core 侧后续会出手动归档接口, 到时 agent 工具再重新指向那个接口。\n",
    "38": " * 二选一的 pin repo（KvVersionPinRepo 或 VersionPinRepo）——\n",
    "39": " * 接口对齐（getVersion/pinMany/upsertVersion），业务代码无需感知具体实现。\n",
    "41": " * 见 docs/design/2026-07-10-cos-ttl-nottl-split-plan.md §4.1：所有方法加\n",
    "42": " * `userId + agentSource` 两个必填参数。\n",
    "45": " * 之后又追加了 `spaceId` 首参 —— KvVersionPinRepo 的所有方法都要 5 个 seg\n",
    "46": " * 才能拼出正确的 COS key。老 VersionPinRepo (Redis) 用 wrapper 适配到同\n",
    "47": " * 一个 5 参签名，spaceId 段直接吞掉即可（Redis 版 key 里不带 spaceId）。\n",
    "49": " * 之前 4 参签名 + 直接把 KvVersionPinRepo 塞给 PinRepoLike 会导致：\n",
    "54": " * → key 完全错乱且丢租户隔离；节点 B 读永远 miss，症状匹配\n",
    "55": " *   \"写到 COS 但另一节点看不到\"。见 docs/design/2026-07-13-proxy-multinode-state-audit.md P0-1.\n",
    "83": " * 把 4 参 Redis-based VersionPinRepo 包成 5 参 PinRepoLike。\n",
    "84": " * Redis key schema (skill:vpin:<userId>:<agentSource>:<sessionId>) 里本来\n",
    "85": " * 就不带 spaceId —— 单实例 Redis 一般跟 kernel-sts space 是一对一或不重叠，\n",
    "86": " * 沿用旧行为，spaceId 参数直接丢弃。\n",
    "97": " * 装配决策 —— 单一入口，让 skill-bridge 拿到\"当前生效的\"存储组合。\n",
    "99": " *   storage.enabled + mode!=off → 走 ProxyStorage (KvVersionPinRepo)\n",
    "100": " *   否则如果 redis.enabled       → 走 Redis (VersionPinRepo)\n",
    "101": " *   否则                          → null (in-memory 兜底)\n",
    "103": " * 曾经这里还装配 KvExtractStore / RedisExtractStore 给老链路\n",
    "104": " * SkillExtractTrigger 用, 已随老链路一起删除。\n",
    "110": "   * BindingRepo 直接从 SessionStore 拿(injection pipeline 装配时已注入),\n",
    "111": "   * 不重新构造,保证 bridge L2 反查用的实例和 handler 写 binding 的实例一致\n",
    "112": "   * —— 单元测试注入 SessionStore.setBindingRepo 后,bridge 也读同一个 mock。\n",
    "154": "  // agent 侧 tool 名叫 skill_extract, bridge 转发到 core force-archive\n",
    "155": "  // (不依赖 messages, core 从 conversation buffer 拿)。见下方 sub === \"extract\" 分支。\n",
    "169": "// Note: 曾经有 RESET_EXTRACT_SUBPATHS 用来在 write 成功或 extract 完成后\n",
    "170": "// 清零 proxy 侧 buffer 计数器 (老链路 KvExtractStore)。老链路删除后计数器\n",
    "171": "// 也没了, 该常量随之删除。\n",
    "196": "   * URL 路径侧的 agentSource（`claude-code` / `codebuddy` ...）—— 用于\n",
    "197": "   * Repo 三段隔离键。从 SessionStore 里存储 session 的 keyId 反解出来\n",
    "198": "   * （keyId 形如 `${agentSource}:${sessionId}`）。\n",
    "219": "   * (`${agentSource}:${sessionId}`). 用于埋点侧对齐 session_init_logs 的\n",
    "220": "   * session_key —— 埋点不能猜前缀，必须用真实命中的 key。\n",
    "226": " * Bridge 只吃 2 个 header:\n",
    "227": " *   - x-conversation-id (或 x-session-id / x-chat-id / x-thread-id) → sessionId\n",
    "230": " * 不再依赖 Authorization 反查 userId —— 见 docs/design/2026-08-03-binding-flatten.md,\n",
    "231": " * L2 fallthrough 走拍平的 (spaceId, sessionId) → binding.json 直接 stamp。\n",
    "250": "  // agentSource 从 matchedKey 反解(命中的 L1 key 形如 `${agentSource}:${sessionId}`);\n",
    "251": "  // L2b 分支直接从 binding.agentSource 拿(见 bindingToIdFields)。\n",
    "285": " * L1: 先按 bare sessionId 试(handler.ts 存的 keyId 是 `${agentSource}:${sessionId}`,\n",
    "286": " * bridge curl 拿不到 agentSource,所以按候选前缀顺序探)。\n",
    "288": " * ⚠️ 候选轮询是过渡期兼容:同 pod 内主对话链路建过 session, L1 Map 里的 key 带\n",
    "289": " * agentSource 前缀,bare sessionId 命中不到。方案 B 拍平后 L2b binding 直接命中\n",
    "290": " * 2 段 key,不再需要前缀轮询;这里 L1 保留是为了 L2b 出问题时,仍能从内存 L1\n",
    "291": " * 恢复而不 401。\n",
    "308": " * L2 fallthrough —— 拍平后只吃 (spaceId, sessionId)。见\n",
    "311": " * 不再走 verifyUserKey + getOrRecover 那条 4 段路径。原因:\n",
    "312": " *   1) bridge curl 模板没塞 Authorization: Bearer,verify 拿不到 userId\n",
    "313": " *   2) 拍平后 binding.json 里已经存了 user_id/team_id/agent_id/agent_source/user_key,\n",
    "314": " *      一次 GET 就够,不需要再补 kernel getAgent/getTask\n",
    "397": " * TODO(upgrade): 长期来看，如果观察到 team search 过滤后剩余条数常 < 用户\n",
    "398": " * top_k（whitelist merged 显著大于 50，或 filter 命中率 P95 < 0.2），说明\n",
    "399": " * 该 team 的 skill 池已把 top-50 撑爆，overfetch 兜不住 → 升级为请求侧\n",
    "400": " * 过滤（proxy 传 `skill_ids: [...]` → core 在 pool 内精确检索）。\n",
    "401": " * 见 `docs/design/2026-08-10-skill-search-scope-fix.md` §3 Plan A。\n",
    "454": "    // 前置校验早退埋点: 每个 return 前都发一条 reject_reason 非空的 bridge_call。\n",
    "455": "    // sessionKey 此刻可能还没派生, 允许传 \"\" (helper 兜底 agentSource='unknown')。\n",
    "491": "    // curl 模板只带 (x-conversation-id, x-tdai-service-id) 两个 header;\n",
    "492": "    // L1 miss 时用它俩去 nottl/<spaceId>/<sessionId>/binding.json 反查。\n",
    "527": "    // backing.redis 之前给老链路 SkillExtractTrigger 用, 老链路已删,\n",
    "528": "    // 本函数体内不再直接使用 redis; backing 结构上保留是因为 pinRepo\n",
    "529": "    // 走 redis 的分支还需要它。\n",
    "531": "    // 消融实验：allowLlmWrite=false 时拒绝写操作\n",
    "602": "        // 埋点补齐: 与主路径 :822 对称, upstream 未响应也算一次调用。\n",
    "603": "        // 之前这个 catch 分支静默 return, 导致 curl 视角\"打了 N 次\" CH 少一条。\n",
    "683": "     * upstream 路径若与 `/v3/skill/${sub}` 不一致(如 extract → force-archive)\n",
    "684": "     * 由分支写这个变量;默认沿用 sub。\n",
    "688": "      // agent 侧 tool 叫 skill_extract, 语义\"立即归档当前对话触发一次 skill 抽取\"。\n",
    "689": "      // 转发到 core `/v3/skill/conversation/force-archive` —— 该接口不吃 messages,\n",
    "690": "      // 从 conversation buffer(proxy 主对话链路每轮推的 /v3/skill/conversation/add)\n",
    "691": "      // 拿累积的完整对话。见 core skill-schemas.ts forceArchiveRequestSchema。\n",
    "693": "      // outbound 只需 (space_id, user_id, team_id, agent_id, session_id) + 可选 reason;\n",
    "694": "      // agent 传的 messages / task_id 一律不透传(agent 视角无关,session 内隐含)。\n",
    "749": "        // Whitelist = A ∪ B（见 docs/design/2026-08-10-skill-search-scope-fix.md §4）：\n",
    "751": "        //   B = core /v3/skill/list(agent 自有全量)   — 含 private\n",
    "753": "        // 原来有 C = listing \"本会话已注入\" 做减法，但 C 是实时 listing 结果，\n",
    "754": "        // 会话内新建的 skill 会出现在 C 中被减掉 → 搜不到 (Issue #1006)。\n",
    "755": "        // 去掉 C 减法：代价是已注入 skill 可能重复出现在搜索结果中（无害），\n",
    "756": "        // 但不会有\"永远搜不到\"的盲区。\n",
    "758": "        // 失败降级策略：\n",
    "759": "        //   A 失败 → fail-closed 返回空（安全兜底：绝不让 LLM 看到未过滤结果）\n",
    "760": "        //   B 失败 → 当空集，退化为纯 A（约等于修复前行为）\n",
    "773": "        // B limit=1000 是 core listRequestSchema 上限（paginationSchema.limit.max(1000)）。\n",
    "774": "        // 单 agent 自有 skill 到不了 1000 量级，一次拿完不分页。\n",
    "791": "          // Fail-closed: A 挂掉不能降级到不过滤搜索。\n",
    "883": "      // 埋点：upstream 未响应也算一次调用（方案 §5.1\"每次都要记录\"）\n",
    "884": "      // 用 ids.composite_key 保证 session_key 与 session_init_logs 对齐。\n",
    "908": "    // 埋点：upstream 已响应（含 4xx/5xx），记录实际状态与耗时\n",
    "924": "    // 曾经这里会在写操作 / extract 成功时清零 proxy 侧 buffer 计数器,\n",
    "925": "    // 避免重复触发自动 extract。老链路 (SkillExtractTrigger + KvExtractStore)\n",
    "926": "    // 已删除, 该逻辑不再需要。\n"
  },
  "MemoryProxy/src/storage/cos-storage.ts": {
    "1": " * CosStorage —— ProxyStorage 的对象存储实现（COS / S3-compatible）。\n",
    "3": " * 定位：生产多实例首选。\n",
    "5": " * 依赖注入：不直接 `import COS from \"cos-nodejs-sdk-v5\"`，而是接一个最小\n",
    "6": " * `CosLikeBackend` 接口（PUT / GET / HEAD / DELETE / LIST），由 factory 层\n",
    "7": " * 用真实 SDK 实例注入。这样单元测试可以用 mock backend 全流程验证 CosStorage\n",
    "8": " * 逻辑，不打真桶；生产环境注入的是 openclaw 插件的 SharedCosClient wrapper。\n",
    "10": " * 语义要点：\n",
    "11": " *   - putIfAbsent 走 COS `If-None-Match: \"*\"` header —— 存在返回 412\n",
    "12": " *   - TTL 由桶 lifecycle rule 兜底（按 lastModified）\n",
    "13": " *   - 凭据 403 / 重试逻辑由底层 backend 负责，不在这层处理\n",
    "18": " * CosLikeBackend 契约 —— **定义在 cos-types.ts，这里 re-export 保持向后兼容**。\n",
    "20": " * 生产实现：cost-guard submodule 里的 CosStorageBackendMultiSpace（走\n",
    "21": " * `await import(\"@context-proxy/cost-guard\")`）。测试：用 in-memory mock\n",
    "22": " * （见 cos-storage.test.ts）。\n",
    "24": " * 2026-07-13 抽 submodule 后新增 cos-types.ts 承载 CosLikeBackend +\n",
    "25": " * KernelStsCosOptions；本文件只 re-export，签名不变，避免破坏老 caller\n"
  },
  "MemoryProxy/src/storage/cos-types.ts": {
    "1": " * CosLikeBackend / KernelStsCosOptions 主仓侧类型定义 —— **权威源**。\n",
    "3": " * cost-guard 侧 `packages/cost-guard/src/storage/cos-types.ts` 有一份结构等价的\n",
    "4": " * 独立定义（避免反向依赖主仓）。两份都是 5 个方法接口，结构类型下天然兼容 ——\n",
    "5": " * 主仓通过 `await import(\"@context-proxy/cost-guard\")` 拿到装配函数的返回值\n",
    "6": " * （cost-guard 侧的 CosLikeBackend 实例），赋给主仓期望的 CosLikeBackend\n",
    "7": " * 变量时 TypeScript 会自动接受。\n",
    "9": " * 让 tsc 编译**不依赖 submodule 是否存在** —— 开源用户 clone 后即使\n",
    "10": " * `packages/cost-guard/` 目录为空，主仓 `tsc` 依然能过（因为 CosLikeBackend\n",
    "11": " * 类型定义在这里，不 import 自 cost-guard）。\n",
    "13": " * 详见 docs/design/2026-07-11-cos-submodule-extraction-plan.md §4.2 决策 1 + §4.4。\n",
    "17": " * 最小 COS 后端契约 —— CosStorage 会向下调这 5 个方法。\n",
    "22": "   * @param headers 额外 header —— CAS 场景传 `{ \"If-None-Match\": \"*\" }`；\n",
    "23": "   *                后端遇到 412 应该抛 `{ statusCode: 412 }`\n",
    "32": "   * 可选：踢掉某个 spaceId 的 per-space backend + STS 凭证缓存。\n",
    "34": "   * 仅 kernel-sts 装配（cost-guard `CosStorageBackendMultiSpace`）会实现；\n",
    "35": "   * 单-space 或无 pool 的实现（例如未来主仓可能挂的 mock backend）应保持\n",
    "36": "   * 不定义。`/v3/instance/proxy-destroy` handler 通过 optional-call 检测。\n",
    "38": "   * 命中返回 `true`，未命中或不支持返回 `false`。\n",
    "44": " * `openKernelStsCosBackend` 的入参 —— 跟 `StorageConfig.cos` 结构镜像\n",
    "45": " * （去掉了跟 kernel-sts 无关的字段）。\n",
    "48": "  /** COS key 业务命名空间前缀，例如 `\"proxy_cache/\"`（跟 core 的 memory_v2/cos_data 隔离）。 */\n",
    "51": "   * 可选：强制走 VPC 内网 / 自定义域名（例：`\"cos.example.com\"`）。\n",
    "52": "   * 空则用 Shark 返回 CosUrl 里的 host。\n",
    "56": "   * Shark 拉临时凭证 —— 每个 spaceId 独立 STS，权限严格绑到\n",
    "57": "   * `proxy_cache/{ttl|nottl}/{spaceId}/*` 两个前缀。\n"
  },
  "MemoryProxy/src/storage/factory.ts": {
    "1": " * ProxyStorage 工厂 —— 单例 + 降级链。\n",
    "3": " * 见 docs/design/2026-07-09-redis-to-cos-migration-plan.md §3.2.\n",
    "5": " * ⚠ cos 是多节点部署下唯一正确的共享后端：**配了 cos 就绝不降级**，装配失败\n",
    "6": " *   直接抛错让进程起不来。否则悄悄退到 process-local（sqlite/fs/memory）会造成\n",
    "7": " *   多节点各写各的本地存储、跨节点状态互相读空（session-init 表单丢失等）。\n",
    "8": " *   见 docs/design/2026-07-13-proxy-multinode-state-audit.md P0-2。\n",
    "10": " * 非 cos 后端（本地 / 离线开发）保留降级链 sqlite → fs → memory：\n",
    "11": " *   - 每次降级 console.error 一条 `!!! DEGRADED !!!`\n",
    "12": " *   - `/health` 通过 `getEffectiveBackend()` 暴露 requested vs effective\n",
    "13": " *   - memory 永不失败但明确不持久化，仅兜底以保证进程不 crash。\n",
    "27": " * 用户配置结构 —— kernel-sts 段见\n",
    "30": " * COS 后端只支持 kernel-sts —— static AK/SK 已删除（正式环境禁止）。\n",
    "31": " * 本地/离线开发用 `backend: sqlite`（或 fs / memory）。\n",
    "35": "  /** `ttl/` 前缀下对象的生存期（天）。只对 ttl 前缀生效。 */\n",
    "40": "     * 业务命名空间前缀（跟 core 的 memory_v2/cos_data 隔离）。\n",
    "41": "     * 注意：Shark 返回的 CosUrl 已经带 bucket/region/endpointDomain，\n",
    "42": "     * 这里不用配 bucket/region。\n",
    "46": "     * 可选：强制走 VPC 内网 / 自定义域名（例：`cos.example.com`）。\n",
    "47": "     * 空则用 Shark 返回 CosUrl 里的 host。\n",
    "50": "    /** Shark 拉临时凭证的配置。 */\n",
    "77": " * 当前生效的 CosLikeBackend 引用（仅 cos 装配成功时非 null）。\n",
    "78": " * 保留是为了让 `evictCosSpace()` 能通过 optional `evictSpace(spaceId)` 钩子\n",
    "79": " * 踢掉 kernel-sts pool 里的 per-space backend；其它后端 / 未装配场景为 null。\n",
    "83": " * kernel-sts COS 装配入口 —— 从 cost-guard submodule dynamic import。\n",
    "84": " * `initProxyStorage()` 时填一次，之后 tryCreate.case(\"cos\") 用它装配。\n",
    "85": " * null 表示 submodule 未加载或不可用 —— cos 分支会走降级链。\n",
    "90": " * 进程启动时调用一次 —— 做 cost-guard 的 dynamic import 后走同步 getProxyStorage。\n",
    "92": " * cost-guard 不可用（开源用户无 submodule / 内部环境镜像构建漏做 submodule update）\n",
    "93": " * 时静默跳过 —— getProxyStorage 里 cos 分支会抛错，走降级链 (sqlite → fs → memory)。\n",
    "95": " * 见 docs/design/2026-07-11-cos-submodule-extraction-plan.md §4.2 决策 3。\n",
    "122": "  // ── cos：唯一正确的多节点共享后端，装配失败硬失败，绝不降级 ──────────────\n",
    "124": "  // 一旦配了 cos，就不给降级链任何机会。cos 装配失败（cost-guard submodule\n",
    "125": "  // 缺失/版本不匹配、shark 不可达、STS 拉取失败等）必须直接抛错让进程起不来，\n",
    "126": "  // k8s 探活失败自然把这个坏 pod 摘掉。绝不能悄悄退到 process-local\n",
    "127": "  // （sqlite/fs/memory）——那会让多节点各写各的本地存储，跨节点 session\n",
    "128": "  // 状态互相读空，症状是\"写到 COS 了但另一节点看不到 / session-init 表单被跳过\"。\n",
    "129": "  // 见 docs/design/2026-07-13-proxy-multinode-state-audit.md P0-2。\n",
    "151": "  // ── 非 cos：本地/离线开发，保留降级链 sqlite → fs → memory ────────────────\n",
    "158": "        // 降级 = 高危事件，用 error 级别（老代码是 warn，被淹没了）\n",
    "177": "      // 装配失败也升级为 error（之前是 warn）\n",
    "182": "  // 理论上到不了这里 —— MemoryStorage 不会抛\n",
    "196": "/** 测试专用 —— 重置单例 + 停 sweeper。 */\n",
    "210": " * 踢掉 kernel-sts pool 里某个 spaceId 的 per-space backend + STS 凭证缓存。\n",
    "212": " * 触发场景：`/v3/instance/proxy-destroy` 清完 COS 数据后，主动回收该 space\n",
    "213": " * 的 provider + cos client，避免 pool 里留着\"已经无权限\"的凭证等 LRU 淘汰。\n",
    "215": " * 语义（永不抛，用返回值区分）：\n",
    "216": " *   - `\"evicted\"`      —— 成功从 pool 摘下并调度延迟 close\n",
    "217": " *   - `\"not-cached\"`   —— cos 生效但 pool 里当前没有该 space（从没访问过 / 已淘汰）\n",
    "218": " *   - `\"unsupported\"`  —— 当前 effective 不是 cos，或 cos backend 未暴露\n",
    "219": " *                         `evictSpace` 钩子（例如未来主仓换 mock backend）\n",
    "260": "      // 存下 backend 引用，供 evictCosSpace() 走 optional evictSpace(spaceId) 钩子\n",
    "261": "      // （kernel-sts 装配的 CosStorageBackendMultiSpace 会实现它）。\n",
    "269": "  // Lazy require —— better-sqlite3 是 optional dependency，某些镜像加载会失败。\n",
    "277": "  // 默认路径复用 db/index.ts 的 resolveDbPath 逻辑：环境变量 > ~/.tdai-memory-proxy/proxy.db。\n",
    "278": "  // 不通过 require ../db/index.js —— tsx 下 ESM 会失败 —— 直接内联一次。\n",
    "291": "// kernel-sts COS 装配逻辑已迁到 cost-guard submodule\n",
    "293": "// 主仓通过 initProxyStorage() dynamic import 拿到 _kernelStsFactory 使用。\n",
    "301": "      // 只清 ttl bucket；nottl 永久保留（与 COS lifecycle rule 的语义一致）。\n"
  },
  "MemoryProxy/src/storage/fs-storage.ts": {
    "1": " * FsStorage —— ProxyStorage 的本地文件系统实现。\n",
    "3": " * 定位：离线/私有部署或 docker 只读兜底。生产不推荐（多进程共享目录会 race）。\n",
    "5": " * 语义：\n",
    "6": " *   - key 是相对路径，禁止绝对路径 / 路径穿越（安全守卫）\n",
    "7": " *   - putText/putJSON 用 tmp + rename 保证原子；putIfAbsent 用 O_EXCL 保证 CAS\n",
    "8": " *   - 目录自动创建\n",
    "10": " * TTL：不实现 sweeper（`fs.stat().mtime` 在某些容器 FS 上不可靠）——\n",
    "11": " * 由运维用 tmpwatch / systemd-tmpfiles 清理。见方案 §3.3.3。\n",
    "117": "    // prefix 是目录形式；把它当目录 rmdir -r。为了统计删除数量，先 list 再逐个 unlink 目录。\n",
    "124": "    // 顺带清理空目录（best-effort），不影响计数。\n",
    "133": "    // prefix 通常以 \"/\" 结尾，代表一个目录；否则视为\"目录 basename\"过滤器。\n"
  },
  "MemoryProxy/src/storage/key-utils.ts": {
    "1": " * key-utils —— ProxyStorage key 路径生成 & 校验工具。\n",
    "3": " * 见 docs/design/2026-07-12-cos-shark-sts-credential-plan.md §3.2\n",
    "4": " * （原方案 2026-07-10-cos-ttl-nottl-split-plan.md §4.2 的目录布局在此扩展了 spaceId 层）。\n",
    "6": " * 目录方案：<ttl|nottl>/<spaceId>/<userId>/<agentSource>/<sessionId>/<data-type>[/subpath]\n",
    "8": " * spaceId 层在 P4 (kernel-sts) 引入 —— STS 权限按 spaceId 隔离，路径也随之带上\n",
    "9": " * spaceId 段，key layout 与 STS resource `proxy_cache/{ttl|nottl}/{spaceId}/*` 对齐。\n",
    "11": " * 四段隔离键的合法性由本文件统一校验，各 Repo 拼 key 时透过 `sessionDirOf` 生成\n",
    "12": " * 前缀（末尾一定带 `/`），再追加自己的数据类型段。写入前对易被注入的段\n",
    "13": " * （spaceId / userId / agentSource / sessionId / hookId / skillId）做校验，防止\n",
    "14": " * `../` 路径穿越、空段导致的 key 冲突以及 agentSource 混入非法字符打穿目录层。\n",
    "20": " * 通用段校验：非空 + 不含 `/` + 不含 `..` 子串。\n",
    "22": " * `..` 校验直接拒绝所有含子串的场景（\"..\", \"foo..bar\", \"..a\" 全拒），比只拒\n",
    "23": " * 完整 \"..\" 更保守 —— 生产 sessionId / userId / hookId 里理论上不会出现连续\n",
    "24": " * 两个点，误伤成本极低而防注入价值高。\n",
    "33": " * agentSource 值域校验：`^[a-z0-9-]+$`。\n",
    "35": " * 允许 lowercase + digit + hyphen，跟 URL path 第一段格式一致\n",
    "36": " * （handler.ts / anthropicHandler.ts 从路径解析出来的就是这种格式）。\n",
    "37": " * 大写、下划线、点、斜杠一律拒绝。\n",
    "46": " * 生成 session 级目录前缀：`<bucket>/<spaceId>/<userId>/<agentSource>/<sessionId>/`。\n",
    "48": " * 末尾**保留斜杠**，让调用方直接 `${sessionDirOf(...)}<datatype>` 拼即可；\n",
    "49": " * 目录级操作（`listNames` / `delPrefix`）也直接把返回值当 prefix 用。\n",
    "51": " * spaceId 段的位置固定在 bucket 之后（ttl/nottl 之后），这是 lifecycle rule\n",
    "52": " * 用一条前缀匹配所有 space 的关键约束 —— rule 不支持路径中间的通配符。\n",
    "69": " * 2 段版:`<bucket>/<spaceId>/<sessionId>/`。\n",
    "71": " * 给 BindingRepo 用 —— bridge 只凭 (spaceId, sessionId) 就要能读回身份,\n",
    "72": " * userId/agentSource 挪到 JSON 内部字段。见 docs/design/2026-08-03-binding-flatten.md。\n",
    "73": " * 老的 4 段 `sessionDirOf` 继续给 SessionRepo (ttl inj-sess.json) 用,不动。\n"
  },
  "MemoryProxy/src/storage/memory-storage.ts": {
    "1": " * MemoryStorage —— ProxyStorage 的进程内 Map 实现。\n",
    "3": " * 定位：兜底后端。当 cos/sqlite/fs 都不可用时保证 proxy 不 crash，等价于当前\n",
    "4": " * \"什么都没配\"的行为。**不适合生产**。\n",
    "6": " * TTL：暂不实现 sweeper（memory 后端只用于兜底 / 单元测试，进程重启就没）。\n",
    "8": " * 生产可观测性：每次 write 会以 60s 节流的频率打一条 error 日志，提醒运维当\n",
    "9": " * 前是危险的兜底状态。见 docs/design/2026-07-13-proxy-multinode-state-audit.md P0-2。\n",
    "28": "   * 60s 一次的节流告警 —— 每次写都调用，累加计数，够 60s 才实际打日志。\n",
    "29": "   * 目的：让运维在多节点静默降级到 memory 时能在日志里看到大量 write，不会\n",
    "30": "   * 因为 warn-once 而错过（老实现是 factory 只 warn 一次就静默）。\n",
    "55": "    // JS 单线程 —— has/set 之间没有 race\n",
    "57": "    // 走 putText 会重复 warn 计一次，改直接 set 并计一次\n"
  },
  "MemoryProxy/src/storage/per-key-mutex.ts": {
    "1": " * per-key mutex —— 让同一个 key 的 async 操作串行执行，不同 key 完全并发。\n",
    "3": " * 用于 KvBindingRepo.touchLastSeen / KvExtractStore.incrBy 等 R-M-W 操作，\n",
    "4": " * 在**单节点内**消除竞争。跨节点场景见迁移方案 §6.2 的分析（业务可接受精度损失）。\n",
    "6": " * 实现思路：给每个 key 维护一个 Promise 链，withPerKeyLock 在旧链尾追加新任务。\n",
    "7": " * 任务完成后如果自己还是链尾就删掉——避免长期持有 dead entry。\n",
    "13": "  // 两个位置都用 fn —— reject/fulfill 都要串行等待，防止一个抛出后\n",
    "14": "  // 后来的任务 leak 到旧链之前。\n",
    "16": "  // tail 用于登记链尾 & 清理；把 rejection 转成 resolved 避免\"未处理拒绝\"警告。\n",
    "17": "  // 真实错误仍会通过 cur 返回给调用方。\n",
    "25": "/** 测试专用：清空全部 in-flight 锁（不等待完成，用完请自己 await）。 */\n"
  },
  "MemoryProxy/src/storage/proxy-storage.ts": {
    "1": " * ProxyStorage — 统一 KV 抽象，代替原本散落在注入层 / Skill 层的 Redis 直调。\n",
    "3": " * 见 docs/design/2026-07-09-redis-to-cos-migration-plan.md §3.\n",
    "5": " * 语义：\n",
    "6": " *   - key 是相对路径字符串，如 \"short/inj-sess/abc.json\"。禁止绝对路径 / 路径穿越\n",
    "7": " *   - putJSON/putText 覆盖写；写成功等价于\"续期\"（更新 lastModified，只对 ttl bucket 有意义）\n",
    "8": " *   - putJSONIfAbsent/putTextIfAbsent CAS：仅当 key 不存在时写入，返回是否写成功\n",
    "9": " *   - getJSON/getText 找不到返回 null，不抛\n",
    "10": " *   - delPrefix 用于 clearBySession —— 后端各自实现\n",
    "11": " *   - listNames 返回 prefix 下所有对象的 basename（不含 prefix）\n",
    "13": " * 所有方法 async；覆盖写可 fire-and-forget（`.catch(() => {})`）。\n",
    "14": " * putIfAbsent 必须 await（返回值决定后续分支）。\n",
    "24": "  /** 原子 \"put if absent\"。返回 true 表示本次成功写入；false 表示 key 已存在。 */\n",
    "38": "/** 判断 key 属于哪档 bucket —— sweeper 与 lifecycle rule 生成器用。 */\n"
  },
  "MemoryProxy/src/storage/sqlite-storage.ts": {
    "1": " * SqliteStorage —— ProxyStorage 的 better-sqlite3 实现。\n",
    "3": " * 定位：单实例本地开发 / CI / 兜底。生产多实例必须走 CosStorage。\n",
    "5": " * 表结构（proxy_kv）：\n",
    "7": " *   v          BLOB NOT NULL          -- 存 utf-8 字节\n",
    "8": " *   bucket     TEXT NOT NULL          -- \"ttl\" 或 \"nottl\"（从 key prefix 推断）\n",
    "9": " *   updated_at INTEGER NOT NULL       -- ms epoch, put 时刷新（续期）\n",
    "11": " * TTL：由 sweep() 显式触发（只清 ttl 桶），或由 factory 起定时器。\n",
    "12": " * CAS：`INSERT OR IGNORE` 天然原子，无需事务。\n",
    "34": "  /** ttl bucket 生存期（毫秒）。nottl 桶不清。 */\n"
  },
  "MemoryProxy/src/tdai/capabilities.ts": {
    "17": "   * Per-call override for `x-tdai-service-id`，优先于 `serviceId`。\n",
    "18": "   * 多租户部署下，调用方应传从请求路径解析出的 spaceId。\n"
  },
  "MemoryProxy/src/tdai/client.ts": {
    "61": "   * 请求发起者的 user_key（原始 `sk-mem-...`）。\n",
    "63": "   * tdai `/v3/meta/*` 路由要求 `x-tdai-user-key` header 才能通过 Layer 3\n",
    "64": "   * 用户鉴权（否则 401 missing_user_key）。这里传入的 user_key 会：\n",
    "65": "   *   1. 作为 `x-tdai-user-key` header 走鉴权（\"你是谁\"）\n",
    "66": "   *   2. tdai 服务端会用它解析出 user_id，用于 checkAssetPermission 判定\n",
    "68": "   * 因此 body 里**不再需要** user_id —— tdai 从 header 自解析。\n",
    "71": "  /** 目标资产 id，如 `chat_memory-{team}-{agent}`。 */\n",
    "74": "  /** 可选：显式限定检查作用于哪个 agent。 */\n",
    "137": "   *   - 用于\"自有 + 借入\"召回中的某一个 agent\n",
    "138": "   *   - 不带 query 直接返空（与原行为一致）\n",
    "305": "  // 与 memory 数据面调用（postForCtx）**语义相反**：\n",
    "306": "  //   - postForCtx 网络/HTTP/envelope 错都吞掉返回空 —— 让注入路径静默降级\n",
    "307": "  //   - checkAcl 网络/HTTP/envelope 错要抛出 —— 让上层 fail-closed 拒绝注入\n",
    "308": "  //     并打 error 日志（否则 acl 服务挂了会静默变成\"全部允许\"，越权）\n",
    "310": "  // 因此本方法不复用 postForCtx，独立实现 —— 但仍然复用同一份 config\n",
    "314": "   * 校验 user_id 对某 asset 的权限。\n",
    "316": "   * 抛错场景（fetch/超时/HTTP 非 2xx/envelope code≠0/data.allowed 非 boolean）\n",
    "317": "   * 应当由调用方 catch 后按业务需要处理 —— 注入路径请用 checkAclOrDeny 便捷函数。\n",
    "321": "      // 未启用 tdai 时按\"通过\"处理，与其他 memory 方法在 disabled 下的返回一致。\n",
    "335": "          // Layer 3 用户鉴权：tdai v3 meta 路由要求此 header 才能通过，否则\n",
    "336": "          // 401 missing_user_key。Proxy 侧的当前请求发起者 user_key 直接用作\n",
    "337": "          // 调用者身份。\n",
    "341": "          // body user_key = 要**检查权限的目标用户**（这里跟调用者是同一个人 —— proxy\n",
    "342": "          // 场景下永远是自己给自己校验）。schema (userIdOrKeyFields.refine)\n",
    "343": "          // 要求 user_id 或 user_key 至少一个 —— 我们复用 header 那个 key。\n",
    "370": " * 注入路径便捷函数：包一层 try/catch + error 日志。\n",
    "372": " * 语义：\n",
    "373": " *   - allowed=true                  → 放行\n",
    "374": " *   - allowed=false                 → 拒绝（正常，由调用方决定是否 warn）\n",
    "375": " *   - 底层调用抛错                  → 拒绝 + error 日志（fail-closed）\n",
    "377": " * 使用场景：resolveFixedAssetCtxs 里逐个 ctx 过滤时，不希望一次网络错就\n",
    "378": " * 让整个注入路径抛异常，用这个便捷函数把异常转成\"拒绝\"信号即可。\n",
    "400": "/** 打印敏感 userKey 时脱敏：只保留前 6 位 + 后 4 位。 */\n"
  },
  "MemoryProxy/src/tdai/identity.ts": {
    "15": "  /** 请求发起者 user_key（来自 Authorization: Bearer；ACL 校验用）。 */\n"
  },
  "MemoryProxy/src/tdai/pending-writes.ts": {
    "1": " * pending-writes — 追踪 in-flight 的 L0 写入 + 提供 SIGTERM flush 钩子 + 重试。\n",
    "3": " * 背景：streaming 场景下 recordTdaiTurn 必须 fire-and-forget（不能阻塞 SSE\n",
    "4": " * 关流，否则拖慢首字后收尾体感）。但 fire-and-forget 有两个丢包场景：\n",
    "5": " *   1. pod rolling update 收到 SIGTERM 时 event loop 里还有 promise 未 flush\n",
    "6": " *      → node 进程直接退出 → L0 丢\n",
    "7": " *   2. tdai kernel 短暂 503 / 网络抖动 → 单次 POST 失败没重试 → L0 丢\n",
    "9": " * 本模块两条对策：\n",
    "10": " *   - `trackWrite(promise)`：把在飞的 promise 注册到 pending set，settle 时移除。\n",
    "11": " *     `flushPendingWrites(deadlineMs)` 等待所有 in-flight 全部结束或超时。\n",
    "12": " *     index.ts SIGTERM handler 在 shutdown 前调一次。\n",
    "13": " *   - `withL0Retry(fn)`：给单条写包一层最多 3 次的指数退避重试。挡内核瞬断。\n",
    "15": " * 不改变的：\n",
    "16": " *   - 非流式路径仍然 await（本模块的 track 也对 await 生效，但没多余开销）。\n",
    "17": " *   - `recordTdaiTurn(client, identity=null | userMessage=null)` 时 client 侧直接\n",
    "18": " *     return，本模块不介入。\n",
    "20": " * 重复写风险：如果第一次 POST 已到达 tdai kernel 但客户端读 5xx 超时后重试，\n",
    "21": " * kernel 可能收到两条同样内容的 L0（tdai `/v3/conversation/add` 目前没有\n",
    "22": " * idempotency-key）。可接受：宁可重复也不要丢；且重试的两次 POST payload 完全\n",
    "23": " * 一致，L1/L2/L3 蒸馏管线幂等（同一 hash 一条），观测上仅 L0 冗余。\n",
    "29": " * 注册一个 in-flight 写。返回同一个 promise 便于链式使用。\n",
    "30": " * settle 时自动从 set 移除。\n",
    "32": " * 注意：`.finally` 会返回新 promise，如果原 p 被 reject 且新链无 catch，\n",
    "33": " * Node 会报 UnhandledRejection（甚至根据 --unhandled-rejections=strict 直接\n",
    "34": " * 让进程 exit）。用 catch(noop) 把 cleanup 链 swallow 掉；原 p 交由 caller\n",
    "35": " * 自己 catch（handler 侧确实 .catch 了 pipe.error(\"TDAI_L0\", ...)）。\n",
    "43": "/** 当前 in-flight 写数量（观测/测试用）。 */\n",
    "49": " * 等待所有 in-flight 写结束或 deadline 到期。\n",
    "50": " * SIGTERM handler 调；超时后返回未完成数便于日志观测（不阻塞退出）。\n",
    "65": " * 指数退避重试包装。用于 tdai kernel 瞬断场景。\n",
    "67": " * 默认参数：3 次总尝试，间隔 500ms → 1s → 2s（含 jitter）。\n",
    "68": " * 总最长 ~3.5s，pod SIGTERM grace period 一般是 30s，flushPendingWrites\n",
    "69": " * 默认 10s 兜底，够跑完 3 次重试。\n",
    "71": " * 只重试\"值得重试\"的错误（网络、5xx、408、429）；4xx 客户端错误直接抛。\n",
    "94": " * 简单判定：网络错、5xx、408/429 值得重试；其它（400/401/403/404/422）直接放弃。\n",
    "95": " * TdaiClient 的错误目前是 `throw new Error(\\`tdai POST ... HTTP <code>: <body>\\`)`\n",
    "96": " * 形式，用正则捞状态码。捞不到（网络断/timeout）默认 retry。\n",
    "101": "  // 网络类：AbortError / ENOTFOUND / ECONNRESET / ETIMEDOUT / fetch failed\n",
    "103": "  // HTTP 状态码类\n",
    "105": "  if (!m) return true; // 无状态码信息 → 保守 retry\n"
  },
  "MemoryProxy/src/tdai/recorder.ts": {
    "5": " * 从最后一条 user 消息中抽取「真正的用户提问」，写入 L0。\n",
    "7": " * 背景：CodeBuddy / Claude Code / DSH 等编码 agent 的 user 消息里除了真实问题，\n",
    "8": " * 还塞了大量 harness 上下文（<additional_data>、current_time、<system_reminder>、\n",
    "9": " * DSH 的 `Current runtime context.` 快照等）。如果把整条消息原样写进 L0，记忆\n",
    "10": " * 会被这些噪声污染，而且每轮都不一样、检索价值极低。因此这里只保留真实提问。\n",
    "12": " * 抽取算法在 `common/user-query-extractor.ts` 内实现（tdai / mem-command /\n",
    "13": " * codebuddy adapter 共用同一份，避免语义漂移）；本模块只负责\"取最后一条\n",
    "14": " * user message → 抽 query 文本 → 组装 TdaiMessage\"这几步。\n",
    "17": "// 保留 re-export，避免下游（含单测）import 路径变化引发一次性大改。\n",
    "24": "    // 只取真实 user_query，避免把 harness 上下文写进 L0\n"
  },
  "MemoryProxy/src/tdai/types.ts": {
    "29": "   * 请求发起者 user_key（原始 `sk-mem-...`）。用于 tdai `/v3/meta/*` 路由的\n",
    "30": "   * Layer 3 用户鉴权（`x-tdai-user-key` header）—— ACL 校验路径必须。\n",
    "32": "   * 数据面（`/v3/conversation/*`）走 team/user/agent header 三元组，不用此字段。\n"
  },
  "MemoryProxy/src/turnSeq.ts": {
    "1": " * Turn 序号计数 —— 宿主侧无状态推导。\n",
    "3": " * 一个 trace = 一个 turn（一次用户输入）。一个 turn 内的工具循环会产生多次 upstream\n",
    "4": " * 请求，它们必须算出**相同**的 turn 序号，才能在 Langfuse 中归并到同一个 trace。\n",
    "6": " * 由于宿主侧没有逐请求的持久状态（turn 计数器在私有模块内部，不对外\n",
    "7": " * 暴露逐 turn 序号），这里直接从 `messages` 历史推导：统计消息序列里\"人类输入轮次\"的\n",
    "8": " * 数量。规则与私有模块的 turn 检测逻辑对齐：\n",
    "9": " *   - Anthropic：user 消息含非 <system-reminder> 的 text block 即为人类输入；\n",
    "10": " *     纯 tool_result / 纯 system-reminder 是工具循环延续。\n",
    "11": " *   - OpenAI：role=user 且含非 <system-reminder> 文本为人类输入；role=tool 是工具循环。\n",
    "13": " * 因此：一个 turn 的首次请求与其后续工具循环请求，因为\"人类轮次数\"相同，turnSeq 一致。\n",
    "14": " * 下一个 turn 的请求会多出一条人类输入 → turnSeq +1 → 新 trace。\n",
    "16": " * 注意：依赖客户端发送完整历史（Claude Code / CodeBuddy 均如此）。若客户端截断历史，\n",
    "17": " * turnSeq 可能偏移，但同一 turn 内仍保持一致（只是绝对值漂移），不影响\"同 turn 归一 trace\"。\n",
    "20": "/** 判断单条 user 消息内容是否为人类输入（非工具循环延续）。 */\n",
    "39": " * 统计 messages 中\"人类输入轮次\"的数量，作为当前 turn 序号。\n",
    "41": " * 返回值 ≥ 1（至少当前这一轮）；空消息或无人类输入时返回 0。\n",
    "48": "    // OpenAI 的工具响应是 role=tool（不会进入这里）；user 消息按内容判断。\n"
  },
  "MemoryProxy/src/types.ts": {
    "35": "   * Anthropic-specific upstream override（用于 anthropic 协议请求的全局兜底上游）。\n",
    "36": "   * Per-agent override（upstream.agents[agent].url）优先级更高。\n",
    "53": " * ProxyStorage configuration —— injection/skill 数据从 Redis 迁到 COS/SQLite/FS\n",
    "54": " * 的统一存储抽象层。见 docs/design/2026-07-10-cos-ttl-nottl-split-plan.md\n",
    "56": " * 当 `enabled: true` 时，注入层与 Skill 层会用 ProxyStorage 替换 Redis repo；\n",
    "57": " * 否则完全走原 Redis 路径。CostGuard 的 `cg:sess:*` 不受影响。\n",
    "59": " * 存储 key 前缀分两档：\n",
    "60": " *   - `ttl/` —— 热缓存（Session Init State / Injection Hook 预热），配 COS\n",
    "61": " *     lifecycle rule `ttlDays` 天未修改自动删。丢了能重建，业务无感。\n",
    "62": " *   - `nottl/` —— 业务态（Binding / Skill 抽取 / Skill 版本锁），**不配** rule，\n",
    "63": " *     永久保留。\n",
    "66": "  /** 总开关。false = 完全走原 Redis 路径，本次迁移代码等同于未加载。 */\n",
    "68": "  /** 优选后端；init 失败按 cos → sqlite → fs → memory 顺序降级。 */\n",
    "71": "   * `ttl/` 前缀下对象的生存期（天）。只对 ttl 前缀生效，nottl 完全不受影响。\n",
    "72": "   * 默认 7 天，与 COS lifecycle rule 的粒度对齐（COS 天级扫描）。\n",
    "78": "     * 业务命名空间前缀（跟 core 的 memory_v2/cos_data 隔离）。\n",
    "79": "     * bucket/region/endpointDomain 都由 Shark 返回的 CosUrl 解析，不用配。\n",
    "83": "     * 可选：强制走 VPC 内网 / 自定义域名（例：`cos.example.com`）。\n",
    "84": "     * 空则用 Shark 返回 CosUrl 里的 host。\n",
    "88": "     * Shark 拉临时凭证 —— 每个 spaceId 独立 STS，权限严格绑到\n",
    "89": "     * `proxy_cache/{ttl|nottl}/{spaceId}/*` 两个前缀。\n",
    "90": "     * 见 docs/design/2026-07-12-cos-shark-sts-credential-plan.md §3.1。\n",
    "93": "      /** Shark base URL，例如 `http://gateway.example.com:8000`。 */\n",
    "95": "      /** shark HTTP 请求超时。默认 10s。 */\n",
    "97": "      /** 5xx / 429 / 网络错 / 超时的重试次数。默认 2。 */\n",
    "99": "      /** STS 到期前多少 ms 提前刷新。默认 2min。 */\n",
    "101": "      /** per-spaceId backend 池上限（LRU）。默认 100。 */\n",
    "103": "      /** LRU evict 时延迟关闭旧 backend 的 ms。默认 30_000。 */\n",
    "109": "    /** 空 = 用 PROXY_DB_PATH 或 ~/.tdai-memory-proxy/proxy.db。 */\n",
    "150": " * 通过 Langfuse 官方 SDK 上报。一个 trace = 一个 turn（一次用户输入），\n",
    "151": " * 同一 turn 内的工具循环请求归并到同一个 trace 下的多个 generation。\n",
    "155": "  /** Langfuse 实例 base URL，例如 http://localhost:3000。 */\n",
    "162": "   * Debug 模式：上报 generation 时保留原始 Anthropic body 结构\n",
    "163": "   * （含 `cache_control` marker / `thinking` block / tool_use 原生形态），\n",
    "164": "   * 而不是走 `flattenAnthropicMessagesForOpik` 压平。\n",
    "166": "   * 用途：排查请求分类（Fork vs SideQuery vs Main）、cache 命中率、\n",
    "167": "   *      thinking 签名合法性等需要看原生结构才能判断的问题。\n",
    "169": "   * 代价：上报体积增大 2-5x（cache_control block、thinking block 全带过去），\n",
    "170": "   *      Langfuse 存储成本增加。**线上默认关闭**，只在排障时打开。\n",
    "174": "  // ── 批量上报调优（防高并发丢 span）──\n",
    "177": "   * 内存队列最大深度（超出即丢弃）。\n",
    "178": "   * 映射 OTel BatchSpanProcessor 的 maxQueueSize。默认 8192。\n",
    "182": "   * 每批导出的最大 span 数。\n",
    "183": "   * 映射 LangfuseSpanProcessor 的 flushAt / OTel maxExportBatchSize。默认 256。\n",
    "187": "   * 定时 flush 间隔（秒）。\n",
    "188": "   * 映射 LangfuseSpanProcessor 的 flushInterval / OTel scheduledDelayMillis。默认 2。\n",
    "239": "   * DEBUG：把请求识别到的 userId（来自 auth verify / x-user-id 头等）强制\n",
    "240": "   * 覆盖为指定值。用于本地联调时——客户端传的 tokenhub-uid 与 kernel-uid 不一致\n",
    "241": "   * （kernel 侧资产挂在另一个真实 user_id 下），无法通过 kernel /team/list 拉到\n",
    "242": "   * 资产，导致 CB 状态机走 \"no active agents, passing through\" bypass。\n",
    "244": "   * 配置此字段后，handler 层会用此 user_id 替换识别结果，让 CB 状态机以真实\n",
    "245": "   * kernel 用户身份拉资产列表，弹出完整 team→agent→task 表单流程。\n",
    "247": "   * 仅供本地/e2e 联调，生产环境务必留空。\n",
    "251": "   * DEBUG：开启详细诊断日志（包括请求 tools schema、system prompt 摘要、\n",
    "252": "   * 用户输入文本等）。仅用于本地联调排查 session-init 表单交互问题。\n",
    "253": "   * 生产环境务必保持 false 或不配置（默认关闭）。\n",
    "257": "   * 从请求头自动预选 team/agent/task 身份。\n",
    "259": "   * 当首轮请求头已带上身份字段时，先去（当前认证用户可见的）team/agent/task\n",
    "260": "   * 列表里校验其是否存在：命中则跳过对应的交互式选择步骤，缺失/校验失败时按\n",
    "261": "   * `onMismatch` 处理。与 control-plane token 反查并存 —— header 只是「快捷路径」，\n",
    "262": "   * 不改变原有的表单/反查流程。\n",
    "264": "   * 决策规则（见 session/preset.ts）：\n",
    "265": "   * - 只命中 team（未带 agent）           → 跳到 agent 选择阶段\n",
    "266": "   * - 命中 team + agent（task 可选）        → 直接登记，跳过所有表单\n",
    "267": "   * - 任一「已提供」的字段在列表中查不到     → 视为 mismatch，按 onMismatch 处理\n",
    "268": "   * - 未带 team header                      → 完全走原有流程（零行为变化）\n",
    "271": "   * 当用户在 task_select 阶段选择\"跳过\"时，使用此 task_id 作为默认关联。\n",
    "272": "   * 该 task_id 不需要在控制面元数据中真实存在——仅作为标签记录，不影响\n",
    "273": "   * 检索隔离（主维度为 team/user/agent/session）。\n",
    "275": "   * 默认 \"default\"（开启）。若想关闭，在 YAML 中配为空字符串 `defaultTaskId: \"\"`。\n",
    "279": "    /** 是否启用 header 自动预选。默认 true。 */\n",
    "281": "    /** 携带 team_id 的请求头名（小写）。默认 \"x-team-id\"。 */\n",
    "283": "    /** 携带 agent_id 的请求头名（小写）。默认 \"x-agent-id\"。 */\n",
    "285": "    /** 携带 task_id 的请求头名（小写）。默认 \"x-task-id\"。 */\n",
    "287": "    /** header 值在用户可见列表中查不到时：'form' 回退交互表单（默认）| 'bypass' 直接跳过 session init。 */\n",
    "352": "   * 是否允许主模型创建/修改 skill。默认 false。\n",
    "353": "   * 主模型的质量不可控，默认关闭写入能力以避免低质量 skill 被创建。\n",
    "354": "   * 显式设为 true 后：\n",
    "355": "   *   - <skill_tools> 注入全部 10 个工具（含写操作）\n",
    "356": "   *   - /skill-bridge 放行写操作（create/update/patch/delete/files_write/files_remove）\n",
    "357": "   * false 时：\n",
    "358": "   *   - <skill_tools> 只注入只读工具（search/list/view/files_read）\n",
    "359": "   *   - /skill-bridge 拒绝写操作返回 403\n",
    "363": "  // 历史字段 (已删除):\n",
    "365": "  //     老链路 SkillExtractTrigger 用来控制 proxy 自动 fire /v3/skill/extract 的阈值。\n",
    "366": "  //     老链路整体已下线, core 侧接管归档时机 (自己按 tool_call ≥ 10 或 bytes ≥ 40KB 判)。\n",
    "368": "  //     曾经用作新老链路互斥灰度开关。现在永远走新链路 conversation/add, 该开关废弃。\n",
    "392": " *   1. `costGuard`-provided `target.authHeaders`（cheap-model 兜底路由自带凭据）\n",
    "394": " *   3. `costGuard.anthropicUpstream.url`（仅 Anthropic 协议）\n",
    "395": " *   4. `upstream.url` + `upstream.apiKey`（未命中 agent 时的默认）\n",
    "425": "    apiKey: string; // 若非空则替换请求中的 API Key\n",
    "488": "   * 运维口 shared secret，仅供 `/v3/instance/proxy-destroy` 之类的管控接口。\n",
    "490": "   * 语义与 core gateway 的 `server.apiKey` 一致\n",
    "492": "   *   - 空字符串（默认）= 鉴权关闭，路由公开可访问；启动时打 WARN 提醒\n",
    "493": "   *   - 非空 = 请求必须带 `Authorization: Bearer <apiKey>`，用常量时间比对\n",
    "495": "   * env 覆盖：`TDAI_PROXY_ADMIN_API_KEY`。\n",
    "497": "   * 注意：这个 key **不**参与租户 `verifyUserKey` 流程，跟 upstream.apiKey /\n",
    "498": "   * tdai.apiKey 也没关系。仅门禁 proxy 侧的运维口。\n",
    "504": "   * `mem:` 特殊命令配置。\n",
    "506": "   * 当 enabled=false（默认）时，handler 不会检测 mem: 命令，所有请求走原有链路。\n",
    "507": "   * 启用后，handler 在 session init 之后检测最后一条 user message 是否为 mem: 命令，\n",
    "508": "   * 命中则执行对应操作并直接返回伪造 LLM 响应（不注入 / 不转发 / 不计费）。\n",
    "510": "   * allowedCommands 为命令白名单，空数组表示全部允许。\n",
    "515": "   * CC 请求分流总开关。\n",
    "517": "   * 启用时：根据 `cache_control` marker 位置 + tools/thinking 兜底将请求分为\n",
    "518": "   *   main / fork / sidequery 三类，走差异化路径（fork/sidequery 跳过\n",
    "519": "   *   session-init / mem 拦截 / injection / L0 / skill buffer，credit 仍上报）。\n",
    "520": "   * 关闭时：所有请求视为 main，走原有一刀切链路，行为 100% 等价现状。\n",
    "522": "   * 默认关闭。启用前建议先跑一段\"分类识别但不改路径\"的观察期，见方案 §7。\n",
    "523": "   * 详见 docs/design/2026-07-30-cc-request-routing-plan.md\n",
    "528": "   * WorkBuddy 请求分流总开关（运维 kill switch）。\n",
    "530": "   * 启用时（默认）：调用 `classifyWorkbuddyRequest` 将请求分为 main / auxiliary\n",
    "531": "   *   两类，auxiliary 请求直接 passthrough（跳过 session-init / injection / L0 /\n",
    "532": "   *   skill 归档），credit 仍上报。\n",
    "533": "   * 关闭时：所有请求视为 main，走完整业务链路 —— 等价于 aux 分流未启用的老链路，\n",
    "534": "   *   用于 aux 分类规则出问题时快速回滚。\n",
    "536": "   * 默认启用（与 CC 的 `ccRequestRouting.enabled` 默认 false 不同）：WB 的 aux\n",
    "537": "   *   分流已在生产跑通并有日志验证，此开关是\"保守回滚\"保险而非\"灰度上线\"开关。\n",
    "543": "  /** 是否启用 CC 请求分流。默认 false —— 关闭时走完全等价原有行为的老链路。 */\n",
    "548": "  /** 是否启用 WB 请求分流。默认 true —— 关闭时所有请求视为 main，跳过 aux 分流。 */\n",
    "553": "  /** 是否启用 mem: 命令拦截。默认 false。 */\n",
    "556": "   * 命令白名单。空数组 = 全部允许。\n",
    "557": "   * 例如 [\"sync\", \"help\"] 表示只允许 mem:sync 和 mem:help，其他命令不识别。\n",
    "561": "   * mem:create-task / mem:update-task 使用的 LLM 草稿生成器配置。可选。\n",
    "562": "   * 未配置或 enabled=false 时，task 命令族会返回\"未配置 task_draft\"错误。\n",
    "563": "   * 结构与 packages/cost-guard 的 LLMInferConfig 保持形状一致。\n",
    "579": "   * 对外统一 gateway 地址。LLM 生成的 curl 示例（<skill_tools> /\n",
    "580": "   * <tdai_memory_tools> 段里嵌的路径）都以这个 URL 为 base。\n",
    "582": "   * ⚠️ 多节点部署必配：未配时每个 pod 会用自身 `http://<hostIp>:<port>`\n",
    "583": "   * 兜底，pods 互相覆盖 COS 里同一份 hook cache → md5 震荡 → 上游 Anthropic\n",
    "584": "   * KV cache 每次 miss（费钱 + 首 token 慢）。\n",
    "586": "   * 只需填 gateway 对外域名，**不带端口**（gateway 内部路由到 proxy 的端口\n",
    "587": "   * 是 gateway ops 侧的事，跟这里无关）。示例：\n",
    "590": "   * gateway 侧必须把下面两个前缀原样透传到 proxy pod：\n",
    "594": "   * 未配置时 fallback 到 `http://<local hostIp>:<config.server.port>`（仅\n",
    "595": "   * 单节点 / 本地开发场景可用），启动时 warn 一次。\n",
    "599": "   * 资产反思模式（内部效果评估用）。**默认开启**——为了让运营方零配置即可\n",
    "600": "   * 用 URL marker 观测资产注入效果。marker 本身仍是 opt-in：不带 `/analyse/`\n",
    "601": "   * 段的请求完全无感。\n",
    "603": "   * 开启后，请求路径带 `/analyse` marker（结构同 `/cost-guard`：夹在\n",
    "604": "   * `/{agent}/{spaceId}` 之后，如 `/codebuddy/default/analyse/v1/messages`）\n",
    "605": "   * 时，proxy 会在系统提示词末尾追加一个 `<asset_reflection>` 块，指导\n",
    "606": "   * agent 在回答里点评「本轮调用过的云端资产工具是否起到作用」。\n",
    "608": "   * marker 段列表由本节点上实际注册的资产 injector 决定（skill /\n",
    "609": "   * tdai-memory / knowledge），一个都没注册时 injector 不 emit 任何块。\n",
    "611": "   * 姿势对齐 `costGuard.markerOptIn`，但 default 相反：\n",
    "612": "   *   - `true`（默认）：injector register；仅当 URL 带 `/analyse/` 段时才 emit 块\n",
    "613": "   *   - `false`：injector 不 register 且顶部 gate 把 `/analyse/` 段 404 拒\n",
    "625": " * — core-side buffer + archive threshold decide抽取时机) and TDAI L0\n",
    "695": "   * 语义是「唯一 ID」，如 `ep-pksklwtb` / `deepseek-v4-pro`。\n",
    "701": "   * 写入 usage_logs.model_name / usage_raw.model_name 供前端展示。\n",
    "880": "   * mem: 命令族配置（含 create-task / update-task 的 LLM 草稿生成器）。\n",
    "881": "   * 与 ProxyConfig.memCommand 对应；未配置则命令族按默认禁用行为。\n"
  },
  "MemoryProxy/src/workbuddyHandler.ts": {
    "1": " * WorkBuddy endpoint handler —— 骨架层（helper 函数 + main handler stub）。\n",
    "3": " * WorkBuddy 走 OpenAI Responses API（@openai/agents SDK），wire protocol 与\n",
    "4": " * Codex一致，system prompt XML 结构与 CodeBuddy相似。但本文件**故意与\n",
    "5": " * codexHandler / codebuddyHandler 完全解耦**，不import 任何 sibling handler，\n",
    "6": " * 换WorkBuddy 只动本文件与 injection/agents/workbuddy/，其余客户端不受影响。\n",
    "8": " * 本轮（分层交付第一步）：**只暴露单测友好的 pure function**\n",
    "9": " *   - classifyWorkbuddyRequest：识别 main vs auxiliary 请求\n",
    "10": " *   - extractWorkbuddySessionId：从 header / body 中提取 session id\n",
    "11": " *   - detectWorkbuddyDefaultModeGate：识别客户端 Default mode gate 信号\n",
    "12": " *   - injectWorkbuddyAssets：向 body.input[0].content[] 追加 `<tdai_injections>` wrapper\n",
    "14": " * 完整的 `handleWorkbuddyEndpoint(c, config)` 主 handler（含 auth / session-init /\n",
    "15": " * mem-command / forward+langfuse tap）留到下一轮 server 路由接入时再补——\n",
    "16": " * 那部分需要引入大量 config/session 依赖，先隔离出来降低回归面。\n",
    "32": "// WorkBuddy 走 Responses API，与 codex wire 完全一致 —— 弹窗骨架直接复用\n",
    "33": "// session/codex/form.ts 的 buildFormResponse + codexFormAnswersAsMessages，\n",
    "34": "// 状态机复用 CB 的 handleSessionInit(agentSource=\"codex\")。这样 WorkBuddy\n",
    "35": "// 本身不需要单独做一套 form 骨架。\n",
    "77": " * 与 CodexSessionState 语义一致但独立类型，避免跨 handler 类型共享。\n",
    "79": " * - status: \"initialized\" 表示已完成绑定/引导流程；\"pending\" 表示还在等\n",
    "80": " *   session-init 表单回填\n",
    "81": " * - bypassed: 用户明确选择\"Default mode\"绕过绑定流程后，永久跳过 form注入\n",
    "82": " * - sessionInfo:绑定成功后附带的 { userId, teamId, agentId, ... } 元数据，\n",
    "83": " *   透传给 injection pipeline 做上下文查询\n",
    "94": " * WorkBuddy 客户端 Default mode gate 的特征字符串。\n",
    "95": " * 客户端在用户选 Default mode 后，会在 function_call_output 里输出这个前缀\n",
    "96": " * 提示 \"request_user_input is unavailable in Default mode\"——命中即视为\n",
    "97": " * 用户明确选择绕过绑定流程，session 应永久 bypass。\n",
    "99": " * WorkBuddy 客户端的实际字符串**待抓包验证**，本轮先按 codex 的 gate 字符串\n",
    "100": " * 打（\"request_user_input is unavailable in Default mode\"），等真实客户端\n",
    "101": " * 联调时对齐。\n",
    "102": " * TODO(workbuddy-integration): 抓包确认 WorkBuddy 客户端实际的 gate 字符串。\n",
    "111": " * Auxiliary 请求指客户端自发的辅助调用（memory 生成、trace 汇总、compact\n",
    "112": " * 等），不应触发 session-init form 或 injection，直接转发上游。\n",
    "114": " * 判定顺序（任一命中即返回 auxiliary）：\n",
    "115": " *   1. path中出现 aux 路径片段（/compact, /trace_summarize, /realtime, /memories）\n",
    "116": " *   2. header 出现 memgen 标记（x-openai-memgen-request=true，兼容 SDK 惯例）\n",
    "119": " * 未知的 thread_source 视为 main（偏严——宁可漏 aux 也不误把用户交互当 aux）。\n",
    "126": "  // ① path-based aux 判定\n",
    "132": "  // ② header memgen 标记\n",
    "152": " * 从请求头/请求体中提取 WorkBuddy session id。\n",
    "154": " * 优先级（与 codex 相同）：\n",
    "155": " *   1. header `session-id`（SDK 默认位置）\n",
    "158": " * 两者都缺 → null（上层负责决定是拒绝还是生成新 session）。\n",
    "178": " * 识别 WorkBuddy 客户端的 Default mode gate 信号。\n",
    "180": " * 客户端在用户拒绝 request_user_input 表单（选择 Default mode）时，会在\n",
    "181": " * 下一轮请求的 input[] 里带上 function_call_output.output ~= \n",
    "182": " * \"request_user_input is unavailable in Default mode\"。命中即表示用户\n",
    "183": " * 明确要绕过绑定流程→ session 应标记 bypassed。\n",
    "185": " * 与 codex 版本同结构，字符串前缀独立定义（DEFAULT_GATE_PREFIX），未来客户端\n",
    "186": " * 修改文案时只需改这一个常量。\n",
    "207": " * 与 codex 逻辑同构：把 pipeline 产出的完整 XML 文本挂到 developer message\n",
    "208": " * (input[0]) 的 content 数组末尾。\n",
    "210": " * 防御性 short-circuit：\n",
    "211": " *   - 无 input 或 input 不是数组 → 返回原 body\n",
    "212": " *   - input[0] 不是 message → 返回原 body\n",
    "213": " *   - input[0].content 不是数组 → 返回原 body\n",
    "214": " *   （这些防御分支的意义：客户端非首帧时 input[0] 可能是 function_call 之类，\n",
    "215": " *    只有第一轮 input[0] 才是 developer/user message；错注入 function_call 项\n",
    "216": " *    的 content 会导致上游 400 或语义错乱。）\n",
    "218": " * 返回浅拷贝，不修改原 body（body → input → input[0] → content 全链路浅拷）。\n",
    "243": "// ── Human turn counting (langfuse 埋点辅助) ──────────────────────────────────\n",
    "246": " * 统计 WorkBuddy input[] 里的 \"human turn\" 数量。\n",
    "248": " * 用于 langfuse trace 的 turnSeq——只要客户端主动发出的用户消息（role=user\n",
    "249": " * 且 type=message）参与计数；tool 调用产生的 function_call / function_call_output\n",
    "250": " * / assistant 反馈不计入。这样同一轮内的多次 function_call 会merge 到同一个\n",
    "251": " * trace，方便观测。\n",
    "253": " * 与 codex 的 countHumanTurnsCodex 同逻辑，为了保持\"handler 之间零依赖\"独立\n",
    "254": " * 复制一份。\n",
    "272": " * WorkBuddy L0/Skill 归档上下文, 对齐 codexHandler 的 CodexArchiveCtx 设计:\n",
    "273": " *   - archiveCtx=null 时 forward/session bypass 侧直接跳过 hook\n",
    "274": " *   - 失败静默 (内部 warn), 绝不阻塞上游响应\n",
    "282": "  /** 原始 body.input[] (responses API input items) */\n",
    "288": "   * 资产能力开关（chat_memory / skill / ...）；用于 gate 归档 hook。\n",
    "289": "   * 与 codexHandler.CodexArchiveCtx.assetCapabilities 对齐。\n",
    "295": " * 从 responses API body.input[] 提取 latest user message 用于 L0 write。\n",
    "333": "  // chat_memory=false 时用户显式关闭记忆 → 不创建 tdaiClient；skill 归档仍走。\n",
    "334": "  // 对齐 codexHandler.buildArchiveCtx (line 855-857)。\n",
    "361": " * 流结束后触发 TDAI L0 write + skill 提取, 对齐 codexHandler 的\n",
    "362": " * triggerCodexArchiveHooks 逻辑。失败静默(内部已 warn), 不阻塞下游。\n",
    "364": " * @param ctx         归档上下文 (非 null 时有效)\n",
    "365": " * @param assistantText stream accumulator 累积的 assistant 文本\n",
    "373": "  // 与 codexHandler triggerCodexArchiveHooks 对称:\n",
    "374": "  //   trackWrite 挂全局 in-flight set (index.ts flushPendingWrites 兜底)\n",
    "375": "  //   withL0Retry 3 次退避挡 tdai kernel 瞬断\n",
    "376": "  //   stream 场景不 await, 让归档 hook 提前返回\n",
    "378": "  // 注意：buildWorkbuddyArchiveCtx 已在 chat_memory=false 时把 tdaiClient 置 null，\n",
    "379": "  // 所以此处不需要再判 assetCapabilities.chat_memory；tdaiClient 为 null 时自然跳过。\n",
    "393": "  // 与 codexHandler 对称: 归档写完再返, 保证跨节点下一轮读到最新 buffer。\n",
    "394": "  // assistantMessage 使用 stream accumulator 的 outputText 组装一份\n",
    "395": "  // Responses API 格式的消息 (type:\"message\", role:\"assistant\",\n",
    "396": "  // content:[{type:\"output_text\", text}]) —— 与 codexHandler 一致。\n",
    "398": "  // protocol 必须传 \"responses\"：server.ts 注释明确说明 WorkBuddy 与 Codex 同协议，\n",
    "399": "  // langfuse tag 也用 \"protocol:responses\"。若错传 \"openai\"，skill 提取时\n",
    "400": "  // normalizeConversation 会按 Chat Completions 格式解析 messages[]，\n",
    "401": "  // 与实际 body.input[] (Responses API) 错位。\n",
    "429": " * 把 workbuddy 请求 body 结构化成 langfuse observation 的 `input` 字段。\n",
    "431": " * workbuddy 走 Responses API，请求体形态：\n",
    "432": " *   - body.input:        Array<InputItem>（必有，用户消息 / 工具输出等）\n",
    "433": " *   - body.instructions: string           （可选，system-level 指令）\n",
    "435": " * 组合策略（尽量减少 langfuse UI 嵌套层级）：\n",
    "436": " *   - 有 instructions → 返回 { input, instructions }\n",
    "437": " *   - 仅 input       → 直接返回 body.input\n",
    "438": " *   - 都缺失         → 返回 undefined（langfuse 侧不写 input 字段）\n",
    "488": "  // 对齐 codexHandler: 支持 config.upstream.agents?.workbuddy 单独指 URL/apiKey，\n",
    "489": "  // 未配置时回退到全局 config.upstream.{url,apiKey}。\n",
    "498": "  // 若 per-agent 指定了独立 apiKey，覆盖全局注入的 authorization\n",
    "505": "  // 结构化埋点：与 codex 对齐（forwardStart / forwardDone / info 三段式）\n",
    "508": "  // usage.log 记录请求（方便运营 / 计费统计），对齐 codex writeLog 用法\n",
    "533": "    // 网络层失败 → langfuse failure 上报，让线上可视化能看到\n",
    "564": "  // 上游 4xx/5xx → langfuse failure 上报（body 已被上游消费，不重读，避免破坏流）\n",
    "619": " * WorkBuddy tap context —— consumeWorkbuddyStream 的参数类型。\n",
    "631": "   * 转发到上游的最终 body（含注入后的 input[]）。用于两个地方：\n",
    "633": "   *   2) 兜底 —— 目前未用，但对齐 codex 便于后续扩展\n",
    "636": "  /** 上游 URL，写进 observationMetadata 便于排障 */\n",
    "645": " * 关键机制（对齐 codex 但保留 workbuddy 现有 try/finally 风格）：\n",
    "646": " *   - 5 分钟兜底 setTimeout：客户端断开或上游卡住不释放时强制收尾一次\n",
    "647": " *   - toolUseCount 累积：Responses API 里 `response.output_item.done` +\n",
    "648": " *     `item.type===\"function_call\"` 计一次工具调用；透传给 skill 归档做\n",
    "649": " *     round 边界判据\n",
    "650": " *   - buildWorkbuddyLangfuseInput(inputBody)：把 body.input + instructions\n",
    "651": " *     结构化写入 langfuse observation.input，便于排障\n",
    "665": "  // Q: 累积当前 turn 内的 function_call 次数（round 边界判据）\n",
    "668": "  // P: 5 分钟超时兜底。上游或客户端断链可能让 reader.read() 一直挂起，\n",
    "669": "  // 用 setTimeout 强制 cancel，避免 tap coroutine 泄漏。用 flag 而不是\n",
    "670": "  // 直接 throw，因为 fetch 的 ReadableStream cancel 会让主循环自然退出。\n",
    "678": "      // 主动 cancel reader，读循环会因此收到 done=true 或 error 退出\n",
    "707": "          // Q: 工具调用计数（对齐 codex 的判据）—— 仅在 output_item.done\n",
    "708": "          // 且 item.type===\"function_call\" 时 +1；不要放在 response.completed\n",
    "709": "          // 里，避免多算或漏算。\n",
    "713": "            // response.output_item.done 里的 resp 语义与 codex 保持一致：\n",
    "714": "            // 有些上游会在这里把 usage/response.id 一起吐出（stream 内多次\n",
    "715": "            // done），下面 completed 分支才是权威 usage 来源。\n",
    "748": "    // R: 用结构化 input 上报（body.input + instructions），便于 langfuse UI 排障\n",
    "780": "  // 对齐 codexHandler triggerCodexArchiveHooks: langfuse 上报后触发归档。\n",
    "781": "  // archiveCtx=null (aux/未初始化 session/bypass) 直接跳过。\n",
    "782": "  // Q: toolUseCount 透传给 skill 归档，作为 round 边界判据。\n",
    "800": " * 10-段流程（与 codex/anthropic/openai 三家 handler 对齐，便于对读）：\n",
    "801": " *   1. Auth        - Bearer token / x-api-key 验签\n",
    "802": " *   2. Body- 解析 JSON body\n",
    "803": " *   3. Headers     - 提取小写化的请求头 map\n",
    "805": " *   5. Aux         - 短路透传（不注入、不上报 langfuse）\n",
    "806": " *   6. Session ID  - header/body 提取 session id，构造 langfuse turn ctx\n",
    "807": " *   7. Session init- 复用 CB 状态机 (handleSessionInit, agentSource=\"codex\")\n",
    "808": " *                   + codex form builder 渲染 Responses API SSE 弹窗\n",
    "809": " *   8. Mem command - / 命令拦截（session 已注册时）\n",
    "810": " *   9. Injection   - 通用 injection pipeline，注入到 body.input[0].content[]\n",
    "811": " *   10. Forward    - 转发上游 + tap SSE 上报 langfuse\n",
    "853": "  // 关闭 workbuddyRequestRouting.enabled 时强制视为 main，走完全等价 aux 分流\n",
    "854": "  // 未启用的老链路。运维回滚保险；默认启用。对齐 CC 的 ccRequestRouting.enabled\n",
    "855": "  // 语义，但默认相反（CC 默认 false 是灰度上线；WB 默认 true 是保守回滚）。\n",
    "899": "  // WorkBuddy 与 codex 走同一份 Responses API wire，弹窗骨架直接复用 codex/form.ts\n",
    "900": "  // 的 buildFormResponse + CB 状态机（handleSessionInit + agentSource=\"codex\"）。\n",
    "901": "  // 这里的 agentSource 传 \"codex\" 而非 \"workbuddy\" —— 因为状态机内部靠 source 决定：\n",
    "902": "  //   - 是否走两步式分页 (codex-only)\n",
    "903": "  //   - Default gate 字符串识别\n",
    "904": "  //   - formData.{teamPage,agentPage,taskPage} 是否填充\n",
    "905": "  // 三者都是 codex 客户端专有行为，WorkBuddy 亦然。langfuse tag/日志侧的\n",
    "906": "  // agent_source 保持 \"workbuddy\" 不受影响。\n",
    "930": "        // ── 强制归档旧 agent 的 skill buffer（best-effort）──\n",
    "973": "      // kernel 侧鉴权的 x-tdai-user-key 直接用客户端请求 bearer（与 codexHandler / anthropicHandler 对齐）。\n",
    "974": "      // WorkBuddy / Codex / Claude Code 桌面客户端携带的 bearer 就是用户 key，kernel 能识别；\n",
    "975": "      // 无需 config.tdai.apiKey 兜底（否则 config 里的 \"local\" 会覆盖真实用户 key，导致 401）。\n",
    "988": "        // Responses API 客户端不用 messages[]，传空由 store 走 header/no-message 回收路径\n",
    "994": "      // Recovery hit source 决定是否需要 prewarm（详见 handler.ts 对称位置注释）。\n",
    "1026": "        // 我们用 codexFormAnswersAsMessages 把 output 合成成 minimal messages[]\n",
    "1027": "        // 供 CB 的 extractor 识别（extractor 只看 last user/tool message text）。\n",
    "1050": "            // 把原始 input[] 交给 CB 状态机识别 Default gate 与 MORE 翻页\n",
    "1053": "          \"codex\", // ← 状态机 source: 复用 codex 分支\n",
    "1062": "        // CB 状态机中断 → 用 codex form builder 渲染成 Responses API SSE 弹窗\n",
    "1081": "      // Default gate 首次命中 → 返一次 Plan 模式提示，后续同 session recovered.bypassed=true\n",
    "1085": "        // reset 场景下的 gate: 换成针对性文案,详见 codexHandler 同名段\n",
    "1087": "          ? \"⚠️ mem:session-reset 需要 Plan 模式支持。\\n\\n\"\n",
    "1088": "            + \"workbuddy 客户端当前不在 Plan 模式，无法弹出资产选择表单。\\n\"\n",
    "1089": "            + \"请切到 Plan 模式后再执行 mem:session-reset。\"\n",
    "1090": "          : \"检测到未开启 Plan 模式，本次会话跳过资产注入。\"\n",
    "1091": "            + \"如需管理 Skill / Task / Agent，请切到 Plan 模式后重新开启新会话。\"\n",
    "1092": "            + \"本次消息将直接由 LLM 回答。\";\n",
    "1129": "      // Prewarm 前置短路：mem-command 命中的 turn 不走 forward、不消费 hook-cache，\n",
    "1130": "      // 若照常 prewarm 会白花 2-3s + 3 次网络请求。见 handler.ts 对称位置详注。\n",
    "1189": "          agentName: initResult.agentDetail?.name ?? \"未知\",\n",
    "1207": "  // ── mem:session-reset 完成确认 ─────────────────────────────────────────────\n",
    "1238": "      // ⚠️ 不用 parseMemCommand(body, \"workbuddy\") —— 它只解 body.messages[] (CC/CB 形态),\n",
    "1239": "      // WorkBuddy 用的是 Responses API (body.input[])，传进去永远返 null → 命令静默透传给 LLM。\n",
    "1240": "      // 改用 parseCommandFromText(userText) 直接解析用户文本。对齐 codexHandler 的做法。\n",
    "1242": "      // session-reset 已由 pre-hook 处理，跳过防止重复执行\n",
    "1246": "          const errText = `⚠️ 会话未初始化，命令不可用。请先完成 session 初始化（选择 Team/Agent）后重试。`;\n",
    "1266": "          // ⚠️ WorkBuddy 走 Responses API，与 codex 同协议。传 \"responses\"，\n",
    "1267": "          // executeMemCommand 内部会用对应的 responses SSE 骨架渲染命令响应。\n",
    "1271": "          // task 命令族用最近对话生成草稿。Responses API body.input[] 结构：\n",
    "1273": "          // extractSimpleMessages 已内置对该形态的识别，转成 {role, content} 极简格式。\n",
    "1278": "        // 对齐 codexHandler 的 mem-command 后归档逻辑: 命令执行结果不阻塞响应,\n",
    "1279": "        // 异步触发 L0 write + skill 提取 + langfuse 上报。\n",
    "1281": "        // assistantText 用 memResult.messageText (proxy 给用户的命令响应), 不是\n",
    "1282": "        // userText (用户输入的命令) —— L0 write 把\"用户问了什么 / 系统答了什么\"\n",
    "1283": "        // 配对写入, 用 userText 当 assistant 会颠倒语义。\n",
    "1341": "  // ── 9. Asset injection (每轮都跑) ────────────────────────────────────────\n",
    "1361": "      // 构造 synthetic OpenAI body 供通用 pipeline 处理\n"
  }
}