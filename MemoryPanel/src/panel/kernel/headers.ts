/**
 * Memory kernel v3 `/v3/meta/*` global HTTP headers (aligned with tdai-memory-plugin).
 * The Control panel uses the same set of names for inbound verification and outbound forwarding.
 */
export const META_HEADER_SERVICE_ID = 'x-tdai-service-id';
export const META_HEADER_USER_KEY = 'x-tdai-user-key';
export const META_HEADER_REQUEST_ID = 'x-request-id';
